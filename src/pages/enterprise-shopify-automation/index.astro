---
import BaseLayout from '../../layouts/BaseLayout.astro';

const title = "Enterprise Company Store Management & Automation | Swishtag";
const description = "Manage every Shopify company store from one dashboard. Add order routing, SAGE, PromoStandards, PunchOut, supplier, ERP, and AI automation as needed.";
const canonical = "https://swishtag.com/enterprise-shopify-automation/";
const robots = "index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1";
---
<BaseLayout title={title} description={description} canonical={canonical} robots={robots}>
  <meta slot="head" content="website" property="og:type"/>
  <meta slot="head" content="Enterprise Company Store Management &amp; Automation | Swishtag" property="og:title"/>
  <meta slot="head" content="One dashboard for every Shopify company store, with optional order routing, SAGE, PromoStandards, PunchOut, supplier, ERP, AI, and custom automation." property="og:description"/>
  <meta slot="head" content="https://swishtag.com/enterprise-shopify-automation/" property="og:url"/>
  <meta slot="head" content="Swishtag" property="og:site_name"/>
  <meta slot="head" content="https://swishtag.com/assets/swishtag-enterprise-automation-og.png" property="og:image"/>
  <meta slot="head" content="1200" property="og:image:width"/>
  <meta slot="head" content="630" property="og:image:height"/>
  <meta slot="head" content="Swishtag enterprise company store automation dashboard and connected workflow modules" property="og:image:alt"/>
  <meta slot="head" content="Enterprise Company Store Management &amp; Automation | Swishtag" name="twitter:title"/>
  <meta slot="head" content="One dashboard for every Shopify company store, with optional order routing, SAGE, PromoStandards, PunchOut, supplier, ERP, AI, and custom automation." name="twitter:description"/>
  <meta slot="head" content="https://swishtag.com/assets/swishtag-enterprise-automation-og.png" name="twitter:image"/>
  <meta slot="head" content="summary_large_image" name="twitter:card"/>
  <script slot="head" type="application/ld+json" is:inline>
  {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "Service",
        "@id": "https://swishtag.com/enterprise-shopify-automation/#service",
        "name": "Enterprise Company Store Management & Automation",
        "serviceType": "Multi-store Shopify company store management and automation",
        "provider": {
          "@type": "Organization",
          "name": "Swishtag",
          "url": "https://swishtag.com/"
        },
        "url": "https://swishtag.com/enterprise-shopify-automation/",
        "description": "A base platform for large promotional products distributors to manage multiple Shopify company stores from one dashboard, with paid add-ons for OMS automation, SAGE, PromoStandards, supplier APIs, QuickBooks, Sage Intacct, NetSuite, PunchOut, AI, and custom workflows.",
        "areaServed": "US",
        "audience": {
          "@type": "BusinessAudience",
          "audienceType": "Large promotional products distributors managing multiple company stores"
        }
      },
      {
        "@type": "BreadcrumbList",
        "itemListElement": [
          {
            "@type": "ListItem",
            "position": 1,
            "name": "Home",
            "item": "https://swishtag.com/"
          },
          {
            "@type": "ListItem",
            "position": 2,
            "name": "Shopify Solutions",
            "item": "https://swishtag.com/shopify-solutions/"
          },
          {
            "@type": "ListItem",
            "position": 3,
            "name": "Enterprise Company Store Automation",
            "item": "https://swishtag.com/enterprise-shopify-automation/"
          }
        ]
      }
    ]
  }
  </script>
  <style slot="head" is:inline>
  /* Consolidated production stylesheet. Rule order is preserved intentionally. */
  
  :root {
        --lime: #c2ff02;
        --peach: #ffbd9b;
        --lavender: #d9d6fd;
        --mint: #99feed;
        --white: #ffffff;
        --gray: #f5f5f3;
        --gray-2: #ecece8;
        --ink: #202124;
        --muted: #6c6e73;
        --line: rgba(32, 33, 36, .11);
        --line-strong: rgba(32, 33, 36, .18);
        --shadow-sm: 0 14px 38px rgba(32, 33, 36, .07);
        --shadow-md: 0 22px 58px rgba(32, 33, 36, .10);
        --shadow-lg: 0 34px 90px rgba(32, 33, 36, .14);
        --radius-xl: 42px;
        --radius-lg: 32px;
        --radius-md: 24px;
        --radius-sm: 16px;
        --page: min(1240px, calc(100vw - 48px));
        --ease: cubic-bezier(.22, .8, .24, 1);
        --header-height: 96px;
      }
  
      * { box-sizing: border-box; }
      html { scroll-behavior: smooth; background: var(--white); -webkit-text-size-adjust: 100%; }
      body {
        margin: 0;
        color: var(--ink);
        background: var(--white);
        font-family: "Plus Jakarta Sans", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        overflow-x: hidden;
        text-rendering: optimizeLegibility;
        -webkit-font-smoothing: antialiased;
      }
      body.menu-open { overflow: hidden; }
      img, svg { display: block; max-width: 100%; }
      a { color: inherit; text-decoration: none; }
      button, input, textarea, select { font: inherit; }
      button { color: inherit; }
      :focus-visible { outline: 3px solid rgba(194,255,2,.82); outline-offset: 4px; }
  
      .page-shell { width: var(--page); margin-inline: auto; }
      .section { position: relative; padding: 118px 0; }
      .section--compact { padding: 84px 0; }
      .section--gray { background: var(--gray); }
      .section--soft {
        background:
          radial-gradient(circle at 8% 12%, rgba(217,214,253,.35), transparent 24%),
          radial-gradient(circle at 90% 85%, rgba(153,254,237,.25), transparent 28%),
          #fff;
      }
  
      .eyebrow {
        margin: 0 0 18px;
        color: var(--muted);
        font-size: .74rem;
        font-weight: 800;
        letter-spacing: .14em;
        text-transform: uppercase;
      }
      .eyebrow--ink { color: var(--ink); }
      .section-title {
        max-width: 940px;
        margin: 0;
        font-size: clamp(3rem, 6vw, 6rem);
        line-height: .98;
        letter-spacing: -.062em;
        font-weight: 800;
        text-wrap: balance;
      }
      .section-title--small { font-size: clamp(2.7rem, 4.9vw, 4.9rem); }
      .section-copy {
        max-width: 730px;
        margin: 25px 0 0;
        color: var(--muted);
        font-size: clamp(1rem, 1.3vw, 1.18rem);
        line-height: 1.72;
      }
      .section-heading-split {
        display: grid;
        grid-template-columns: 1.08fr .72fr;
        align-items: end;
        gap: 64px;
      }
      .section-heading-split .section-copy { margin: 0; }
  
      .btn {
        min-height: 56px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 14px;
        padding: 0 24px;
        border: 1px solid transparent;
        border-radius: 999px;
        font-size: .92rem;
        font-weight: 800;
        cursor: pointer;
        transition: transform .26s var(--ease), box-shadow .26s var(--ease), background .22s ease, border-color .22s ease;
      }
      .btn:hover { transform: translateY(-3px); }
      .btn--primary { color: var(--ink); background: var(--lime); box-shadow: 0 14px 30px rgba(194,255,2,.26); }
      .btn--primary:hover { box-shadow: 0 18px 34px rgba(194,255,2,.34); }
      .btn--secondary { color: var(--ink); background: rgba(255,255,255,.8); border-color: rgba(32,33,36,.16); }
      .btn--dark { color: var(--white); background: var(--ink); }
      .btn__arrow { font-size: 1.05rem; transition: transform .24s var(--ease); }
      .btn:hover .btn__arrow { transform: translateX(4px); }
  
      .text-link {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        margin-top: 24px;
        font-size: .9rem;
        font-weight: 800;
      }
      .text-link span:last-child {
        width: 34px;
        height: 34px;
        display: grid;
        place-items: center;
        border-radius: 50%;
        background: var(--lime);
      }
  
      /* Primary navigation */
      .site-header {
        position: fixed;
        z-index: 320;
        inset: 0 0 auto;
        padding: 14px 0;
        background: transparent;
        transition: padding .25s ease;
      }
      .site-header.is-scrolled { padding: 10px 0; }
      .modern-nav {
        width: var(--page);
        min-height: 68px;
        margin-inline: auto;
        display: grid;
        grid-template-columns: auto 1fr auto;
        align-items: center;
        gap: 24px;
        padding: 7px 9px 7px 21px;
        border: 1px solid rgba(255,255,255,.92);
        border-radius: 999px;
        background: rgba(255,255,255,.82);
        box-shadow: 0 14px 42px rgba(32,33,36,.075);
        -webkit-backdrop-filter: blur(18px) saturate(1.08);
        backdrop-filter: blur(18px) saturate(1.08);
        transition: min-height .28s var(--ease), background .22s ease, border-color .22s ease, box-shadow .22s ease;
      }
      .site-header.is-scrolled .modern-nav {
        min-height: 62px;
        background: rgba(255,255,255,.94);
        border-color: rgba(32,33,36,.075);
        box-shadow: 0 14px 38px rgba(32,33,36,.085);
      }
      @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
        .modern-nav { background: rgba(255,255,255,.97); }
      }
      .modern-nav__brand {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        font-size: .96rem;
        font-weight: 800;
        letter-spacing: .055em;
        white-space: nowrap;
      }
      .modern-nav__brand-mark {
        width: 14px;
        height: 14px;
        border-radius: 5px 5px 5px 1px;
        background: var(--lime);
        box-shadow: 9px -7px 0 -4px var(--peach), 11px 7px 0 -5px var(--mint);
        transform: rotate(-12deg);
        flex: 0 0 auto;
      }
      .desktop-nav { display: flex; align-items: center; justify-content: center; gap: 5px; }
      .desktop-nav__item { position: relative; }
      .desktop-nav__trigger,
      .desktop-nav__direct {
        min-height: 40px;
        display: inline-flex;
        align-items: center;
        gap: 7px;
        padding: 0 13px;
        border: 0;
        border-radius: 12px;
        background: transparent;
        font-size: .82rem;
        font-weight: 700;
        cursor: pointer;
        white-space: nowrap;
        transition: background .2s ease;
      }
      .desktop-nav__trigger:hover,
      .desktop-nav__direct:hover { background: rgba(32,33,36,.045); }
      .desktop-nav__trigger svg { width: 14px; fill: none; stroke: currentColor; stroke-width: 1.8; transition: transform .22s ease; }
      .desktop-nav__item.is-open .desktop-nav__trigger svg { transform: rotate(180deg); }
      .modern-nav__cta { min-height: 50px; padding-inline: 21px; }
  
      .mega-menu {
        position: fixed;
        z-index: 360;
        top: 94px;
        left: 50%;
        width: min(1180px, calc(100vw - 64px));
        display: grid;
        grid-template-columns: .82fr 1.24fr .82fr;
        gap: 16px;
        padding: 18px;
        border: 1px solid rgba(32,33,36,.08);
        border-radius: 30px;
        background: rgba(255,255,255,.985);
        box-shadow: var(--shadow-lg);
        opacity: 0;
        visibility: hidden;
        transform: translate(-50%, 10px) scale(.99);
        transition: opacity .22s ease, transform .25s var(--ease), visibility .22s ease;
      }
      .desktop-nav__item.is-open .mega-menu { opacity: 1; visibility: visible; transform: translate(-50%,0) scale(1); }
      .mega-menu__intro,
      .mega-menu__content,
      .mega-visual { min-height: 290px; border-radius: 22px; }
      .mega-menu__intro { padding: 26px; background: var(--gray); }
      .mega-menu__eyebrow { display: block; color: var(--muted); font-size: .64rem; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
      .mega-menu__title { margin: 15px 0 0; font-size: 2rem; line-height: 1.04; letter-spacing: -.045em; font-weight: 800; }
      .mega-menu__intro p:not(.mega-menu__title) { margin: 16px 0 0; color: var(--muted); font-size: .76rem; line-height: 1.65; }
      .mega-menu__all-link { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-top: 28px; padding-top: 18px; border-top: 1px solid var(--line); font-size: .74rem; font-weight: 800; }
      .mega-menu__content { padding: 10px; border: 1px solid var(--line); }
      .mega-menu__primary-links { display: grid; gap: 7px; }
      .mega-link {
        display: grid;
        grid-template-columns: 42px 1fr auto;
        align-items: center;
        gap: 13px;
        padding: 13px;
        border-radius: 17px;
        transition: background .2s ease, transform .2s ease;
      }
      .mega-link:hover { background: var(--gray); transform: translateX(2px); }
      .mega-link[aria-current="page"] { background: rgba(217,214,253,.35); }
      .mega-link__icon { width: 42px; height: 42px; display: grid; place-items: center; border-radius: 13px; background: var(--lime); }
      .mega-link__icon--mint { background: var(--mint); }
      .mega-link__icon--lavender { background: var(--lavender); }
      .mega-link__icon--peach { background: var(--peach); }
      .mega-link__icon svg { width: 23px; fill: none; stroke: currentColor; stroke-width: 1.7; stroke-linecap: round; stroke-linejoin: round; }
      .mega-link__copy strong,
      .mega-link__copy small { display: block; }
      .mega-link__copy strong { font-size: .8rem; }
      .mega-link__copy small { margin-top: 4px; color: var(--muted); font-size: .64rem; line-height: 1.42; }
      .mega-link__arrow { font-size: .78rem; }
      .mega-visual {
        padding: 20px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        overflow: hidden;
        background: linear-gradient(145deg, rgba(153,254,237,.58), rgba(217,214,253,.78) 62%, rgba(255,189,155,.62));
        border: 1px solid rgba(255,255,255,.8);
      }
      .mega-visual__art { height: 165px; display: grid; place-items: center; }
      .mega-visual__art svg { width: 100%; max-height: 165px; }
      .mega-svg-window { fill: rgba(255,255,255,.72); stroke: rgba(32,33,36,.13); }
      .mega-svg-soft { fill: rgba(255,255,255,.48); }
      .mega-svg-line { fill: rgba(32,33,36,.16); }
      .mega-svg-lime { fill: var(--lime); }
      .mega-svg-node { fill: var(--ink); }
      .mega-svg-node--mint { fill: #31cdb8; }
      .mega-svg-node--peach { fill: #ed8f62; }
      .mega-svg-path { fill: none; stroke: rgba(32,33,36,.35); stroke-width: 2; stroke-dasharray: 5 7; }
      .mega-visual__caption span,
      .mega-visual__caption strong,
      .mega-visual__caption small { display: block; }
      .mega-visual__caption span { font-size: .58rem; font-weight: 800; letter-spacing: .11em; text-transform: uppercase; opacity: .65; }
      .mega-visual__caption strong { margin-top: 7px; font-size: 1.02rem; }
      .mega-visual__caption small { margin-top: 6px; font-size: .64rem; line-height: 1.45; opacity: .72; }
  
      .menu-toggle {
        width: 48px;
        height: 48px;
        display: none;
        place-items: center;
        border: 1px solid rgba(32,33,36,.10);
        border-radius: 50%;
        background: var(--gray);
        cursor: pointer;
      }
      .modern-menu-toggle__line { width: 18px; height: 2px; display: block; border-radius: 99px; background: var(--ink); transition: transform .24s var(--ease), opacity .2s ease; }
      .modern-menu-toggle__line + .modern-menu-toggle__line { margin-top: 4px; }
      .menu-toggle[aria-expanded="true"] .modern-menu-toggle__line:first-child { transform: translateY(3px) rotate(45deg); }
      .menu-toggle[aria-expanded="true"] .modern-menu-toggle__line:nth-child(2) { transform: translateY(-3px) rotate(-45deg); }
      .menu-toggle[aria-expanded="true"] .modern-menu-toggle__line:nth-child(3) { opacity: 0; }
  
      .mobile-menu {
        position: fixed;
        z-index: 300;
        inset: 0;
        opacity: 0;
        visibility: hidden;
        transition: opacity .25s ease, visibility .25s ease;
      }
      .mobile-menu.is-open { opacity: 1; visibility: visible; }
      .mobile-menu__backdrop { position: absolute; inset: 0; background: rgba(32,33,36,.35); }
      .mobile-menu__panel {
        position: absolute;
        top: 10px;
        right: 10px;
        width: min(520px, calc(100vw - 20px));
        max-height: calc(100dvh - 20px);
        overflow-y: auto;
        border-radius: 28px;
        background: var(--white);
        box-shadow: var(--shadow-lg);
        transform: translateY(-12px) scale(.985);
        transition: transform .3s var(--ease);
      }
      .mobile-menu.is-open .mobile-menu__panel { transform: translateY(0) scale(1); }
      .mobile-menu__top { display: flex; align-items: center; justify-content: space-between; gap: 20px; padding: 20px 20px 10px; }
      .mobile-menu__brand { display: inline-flex; align-items: center; gap: 10px; font-size: .9rem; font-weight: 800; letter-spacing: .055em; }
      .mobile-menu__close { width: 44px; height: 44px; position: relative; border: 0; border-radius: 50%; background: var(--gray); cursor: pointer; }
      .mobile-menu__close span { position: absolute; left: 12px; top: 21px; width: 20px; height: 2px; background: var(--ink); }
      .mobile-menu__close span:first-child { transform: rotate(45deg); }
      .mobile-menu__close span:last-child { transform: rotate(-45deg); }
      .mobile-menu__body { padding: 8px 20px 0; }
      .mobile-menu__intro { padding: 18px 0 22px; border-bottom: 1px solid var(--line); }
      .mobile-menu__intro span { font-size: .62rem; font-weight: 800; letter-spacing: .11em; text-transform: uppercase; color: var(--muted); }
      .mobile-menu__intro p { margin: 9px 0 0; color: var(--muted); font-size: .82rem; line-height: 1.55; }
      .mobile-nav__direct,
      .mobile-nav__toggle {
        width: 100%;
        min-height: 58px;
        display: grid;
        grid-template-columns: 30px 1fr auto;
        align-items: center;
        gap: 10px;
        padding: 0;
        border: 0;
        border-bottom: 1px solid var(--line);
        background: transparent;
        text-align: left;
        font-size: .95rem;
        font-weight: 750;
        cursor: pointer;
      }
      .mobile-nav__index { color: var(--muted); font-size: .6rem; font-weight: 800; }
      .mobile-nav__plus { width: 16px; height: 16px; position: relative; }
      .mobile-nav__plus::before,
      .mobile-nav__plus::after { content: ""; position: absolute; left: 2px; top: 7px; width: 12px; height: 2px; background: currentColor; }
      .mobile-nav__plus::after { transform: rotate(90deg); transition: transform .2s ease; }
      .mobile-nav__toggle[aria-expanded="true"] .mobile-nav__plus::after { transform: rotate(0deg); }
      .mobile-nav__panel { display: grid; grid-template-rows: 0fr; transition: grid-template-rows .25s var(--ease); }
      .mobile-nav__panel > div { overflow: hidden; }
      .mobile-nav__group.is-open .mobile-nav__panel { grid-template-rows: 1fr; }
      .mobile-nav__panel a { display: block; margin: 8px 0; padding: 14px 15px; border-radius: 16px; background: var(--gray); }
      .mobile-nav__panel strong,
      .mobile-nav__panel small { display: block; }
      .mobile-nav__panel strong { font-size: .82rem; }
      .mobile-nav__panel small { margin-top: 5px; color: var(--muted); font-size: .66rem; line-height: 1.45; }
      .mobile-nav__panel a[aria-current="page"] { background: rgba(217,214,253,.42); }
      .mobile-menu__footer { display: flex; align-items: center; justify-content: space-between; gap: 16px; padding: 18px 20px 20px; }
      .mobile-menu__footer .btn { min-height: 50px; }
      .mobile-menu__footer > span { color: var(--muted); font-size: .65rem; }
  
      /* Hero */
      .hero {
        min-height: 100svh;
        display: grid;
        align-items: center;
        padding: 154px 0 92px;
        overflow: hidden;
        background:
          radial-gradient(circle at 81% 10%, rgba(217,214,253,.64), transparent 27%),
          radial-gradient(circle at 75% 76%, rgba(153,254,237,.44), transparent 30%),
          radial-gradient(circle at 7% 88%, rgba(255,189,155,.26), transparent 24%),
          linear-gradient(180deg, #fff 0%, #fbfbf8 100%);
      }
      .hero-grid { display: grid; grid-template-columns: minmax(0,.96fr) minmax(520px,1.04fr); align-items: center; gap: 56px; }
      .breadcrumb { display: flex; flex-wrap: wrap; align-items: center; gap: 7px; margin-bottom: 24px; color: var(--muted); font-size: .68rem; font-weight: 700; }
      .breadcrumb span { opacity: .55; }
      .hero-title {
        max-width: 760px;
        margin: 0;
        font-size: clamp(3.7rem, 6.25vw, 6.75rem);
        line-height: .94;
        letter-spacing: -.066em;
        font-weight: 800;
        text-wrap: balance;
      }
      .hero-title .accent { position: relative; z-index: 0; display: inline-block; }
      .hero-title .accent::after { content: ""; position: absolute; z-index: -1; left: -.04em; right: -.04em; bottom: .02em; height: .28em; border-radius: 999px; background: var(--lime); transform: rotate(-1deg); }
      .hero-subheadline { max-width: 660px; margin: 22px 0 0; color: var(--ink); font-size: clamp(1.22rem,1.65vw,1.64rem); font-weight: 750; line-height: 1.38; letter-spacing: -.028em; }
      .hero-copy { max-width: 675px; margin: 14px 0 0; color: var(--muted); font-size: clamp(1rem,1.25vw,1.14rem); line-height: 1.7; }
      .hero-actions { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 31px; }
      .hero-meta { display: flex; flex-wrap: wrap; gap: 9px; margin-top: 26px; }
      .meta-pill { min-height: 37px; display: inline-flex; align-items: center; gap: 8px; padding: 8px 12px; border: 1px solid rgba(32,33,36,.09); border-radius: 999px; background: rgba(255,255,255,.72); color: var(--muted); font-size: .67rem; font-weight: 750; }
      .meta-pill::before { content: ""; width: 7px; height: 7px; border-radius: 50%; background: var(--lime); box-shadow: 0 0 0 4px rgba(194,255,2,.14); }
  
      .enterprise-art { min-height: 690px; position: relative; isolation: isolate; }
      .enterprise-art::before {
        content: "";
        position: absolute;
        z-index: -2;
        inset: 4% 2% 3% 5%;
        border-radius: 44% 56% 48% 52%;
        background: radial-gradient(circle at 50% 44%, rgba(255,255,255,.98), rgba(217,214,253,.62) 42%, rgba(153,254,237,.48) 66%, rgba(255,189,155,.30) 82%, transparent 84%);
        filter: blur(5px);
      }
      .enterprise-orbit { position: absolute; inset: 9% 3% 7% 5%; border: 1px dashed rgba(32,33,36,.16); border-radius: 50%; transform: rotate(-8deg); }
      .enterprise-orbit::before,
      .enterprise-orbit::after { content: ""; position: absolute; border: 1px solid rgba(32,33,36,.10); border-radius: 50%; }
      .enterprise-orbit::before { inset: 15%; }
      .enterprise-orbit::after { inset: 31%; border-style: dashed; }
  
      .enterprise-core {
        position: absolute;
        z-index: 8;
        left: 50%;
        top: 50%;
        width: 184px;
        height: 184px;
        display: grid;
        place-items: center;
        padding: 22px;
        border: 1px solid rgba(255,255,255,.9);
        border-radius: 50%;
        background: var(--ink);
        color: var(--white);
        box-shadow: 0 28px 70px rgba(32,33,36,.22);
        transform: translate(-50%,-50%);
      }
      .enterprise-core::before { content: ""; position: absolute; inset: -12px; border: 1px dashed rgba(32,33,36,.18); border-radius: 50%; animation: spin 28s linear infinite; }
      .enterprise-core__copy { text-align: center; }
      .enterprise-core__copy small { display: block; color: rgba(255,255,255,.6); font-size: .57rem; font-weight: 800; letter-spacing: .11em; text-transform: uppercase; }
      .enterprise-core__copy strong { display: block; margin-top: 8px; font-size: 1.34rem; line-height: 1.05; letter-spacing: -.035em; }
      .enterprise-core__pulse { position: absolute; width: 9px; height: 9px; border-radius: 50%; background: var(--lime); box-shadow: 0 0 0 6px rgba(194,255,2,.12); }
      .enterprise-core__pulse--one { top: 18px; left: 48%; }
      .enterprise-core__pulse--two { bottom: 21px; right: 33px; background: var(--mint); box-shadow: 0 0 0 6px rgba(153,254,237,.13); }
  
      .enterprise-node {
        position: absolute;
        z-index: 6;
        min-width: 178px;
        display: grid;
        grid-template-columns: 44px 1fr;
        align-items: center;
        gap: 12px;
        padding: 13px 15px;
        border: 1px solid rgba(255,255,255,.9);
        border-radius: 18px;
        background: rgba(255,255,255,.88);
        box-shadow: 0 18px 50px rgba(32,33,36,.12);
        -webkit-backdrop-filter: blur(14px);
        backdrop-filter: blur(14px);
        animation: nodeFloat 6.4s ease-in-out infinite;
      }
      .enterprise-node__icon { width: 44px; height: 44px; display: grid; place-items: center; border-radius: 14px; background: var(--lime); font-size: .68rem; font-weight: 800; }
      .enterprise-node__icon svg { width: 22px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
      .enterprise-node__copy strong,
      .enterprise-node__copy small { display: block; }
      .enterprise-node__copy strong { font-size: .74rem; }
      .enterprise-node__copy small { margin-top: 4px; color: var(--muted); font-size: .58rem; line-height: 1.35; }
      .enterprise-node--stores { left: 0; top: 14%; }
      .enterprise-node--stores .enterprise-node__icon { background: var(--lime); }
      .enterprise-node--punchout { right: 0; top: 12%; animation-delay: -1.2s; }
      .enterprise-node--punchout .enterprise-node__icon { background: var(--lavender); }
      .enterprise-node--orders { left: 1%; bottom: 16%; animation-delay: -2.1s; }
      .enterprise-node--orders .enterprise-node__icon { background: var(--peach); }
      .enterprise-node--accounting { right: -1%; bottom: 16%; animation-delay: -3.3s; }
      .enterprise-node--accounting .enterprise-node__icon { background: var(--mint); }
      .enterprise-node--suppliers { left: 50%; top: 2%; transform: translateX(-50%); animation-delay: -4.1s; }
      .enterprise-node--suppliers .enterprise-node__icon { background: var(--mint); }
      .enterprise-node--approvals { left: 50%; bottom: 2%; transform: translateX(-50%); animation-delay: -5.0s; }
      .enterprise-node--approvals .enterprise-node__icon { background: var(--lavender); }
  
      .flow-line { position: absolute; z-index: 2; height: 1px; transform-origin: left center; background: repeating-linear-gradient(90deg, rgba(32,33,36,.30) 0 5px, transparent 5px 10px); opacity: .45; }
      .flow-line::after { content: ""; position: absolute; top: -3px; left: 0; width: 7px; height: 7px; border-radius: 50%; background: var(--lime); animation: packet 4s linear infinite; }
      .flow-line--1 { width: 190px; left: 18%; top: 28%; transform: rotate(28deg); }
      .flow-line--2 { width: 190px; left: 52%; top: 30%; transform: rotate(-31deg); }
      .flow-line--3 { width: 190px; left: 18%; top: 69%; transform: rotate(-29deg); }
      .flow-line--4 { width: 190px; left: 52%; top: 67%; transform: rotate(31deg); }
      .flow-line--5 { width: 150px; left: 50%; top: 22%; transform: rotate(90deg); }
      .flow-line--6 { width: 150px; left: 50%; top: 53%; transform: rotate(90deg); }
      .flow-line--2::after { animation-delay: -1.3s; background: var(--mint); }
      .flow-line--3::after { animation-delay: -2.2s; background: var(--peach); }
      .flow-line--4::after { animation-delay: -3.0s; background: var(--lavender); }
      .flow-line--5::after { animation-delay: -1.8s; }
      .flow-line--6::after { animation-delay: -2.6s; background: var(--mint); }
  
      .enterprise-mini-dashboard {
        position: absolute;
        z-index: 10;
        right: 7%;
        top: 43%;
        width: 210px;
        padding: 14px;
        border: 1px solid rgba(255,255,255,.88);
        border-radius: 18px;
        background: rgba(255,255,255,.92);
        box-shadow: 0 20px 48px rgba(32,33,36,.14);
        transform: rotate(2deg);
      }
      .mini-dashboard__bar { display: flex; align-items: center; justify-content: space-between; gap: 10px; }
      .mini-dashboard__bar span { color: var(--muted); font-size: .53rem; font-weight: 800; text-transform: uppercase; letter-spacing: .08em; }
      .mini-dashboard__bar i { width: 8px; height: 8px; border-radius: 50%; background: var(--lime); box-shadow: 0 0 0 4px rgba(194,255,2,.14); }
      .mini-dashboard__stats { display: grid; grid-template-columns: 1fr 1fr; gap: 7px; margin-top: 12px; }
      .mini-dashboard__stat { padding: 10px; border-radius: 12px; background: var(--gray); }
      .mini-dashboard__stat strong { display: block; font-size: .86rem; }
      .mini-dashboard__stat small { display: block; margin-top: 4px; color: var(--muted); font-size: .48rem; }
  
      @keyframes spin { to { transform: rotate(360deg); } }
      @keyframes nodeFloat { 0%,100% { margin-top: 0; } 50% { margin-top: -9px; } }
      @keyframes packet { from { left: 0; } to { left: calc(100% - 7px); } }
  
      /* Proof strip */
      .proof-strip { padding: 0 0 44px; background: linear-gradient(180deg,#fbfbf8 0%,#fff 100%); }
      .proof-panel { display: grid; grid-template-columns: 1.35fr repeat(3,1fr); overflow: hidden; border: 1px solid var(--line); border-radius: 28px; background: var(--white); box-shadow: var(--shadow-sm); }
      .proof-intro,
      .proof-item { padding: 25px 27px; }
      .proof-intro { background: linear-gradient(145deg,var(--lavender),rgba(153,254,237,.74)); }
      .proof-intro strong { display: block; max-width: 330px; font-size: 1.1rem; line-height: 1.3; letter-spacing: -.025em; }
      .proof-item { border-left: 1px solid var(--line); }
      .proof-item small,
      .proof-item strong { display: block; }
      .proof-item small { color: var(--muted); font-size: .59rem; font-weight: 800; letter-spacing: .1em; text-transform: uppercase; }
      .proof-item strong { margin-top: 8px; font-size: .84rem; line-height: 1.4; }
  
      /* Sticky secondary navigation */
      .page-nav-wrap { position: sticky; z-index: 210; top: 90px; padding: 7px 0 12px; background: transparent; }
      .page-nav-shell { position: relative; border: 1px solid rgba(32,33,36,.08); border-radius: 999px; background: rgba(255,255,255,.9); box-shadow: 0 12px 34px rgba(32,33,36,.07); -webkit-backdrop-filter: blur(16px); backdrop-filter: blur(16px); }
      .page-nav { display: flex; align-items: center; gap: 3px; padding: 7px; overflow-x: auto; scrollbar-width: none; }
      .page-nav::-webkit-scrollbar { display: none; }
      .page-nav a { flex: 0 0 auto; min-height: 38px; display: inline-flex; align-items: center; padding: 0 13px; border-radius: 999px; color: var(--muted); font-size: .68rem; font-weight: 750; white-space: nowrap; transition: background .2s ease,color .2s ease; }
      .page-nav a:hover,
      .page-nav a.is-active { color: var(--ink); background: var(--gray); }
      .page-nav__cta { margin-left: auto !important; color: var(--ink) !important; background: var(--lime) !important; }
      .page-nav-toggle { display: none; }
  
      /* Problem */
      .problem-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 18px; margin-top: 52px; }
      .problem-card { min-height: 350px; display: flex; flex-direction: column; padding: 29px; border: 1px solid var(--line); border-radius: var(--radius-md); background: rgba(255,255,255,.88); box-shadow: 0 14px 40px rgba(32,33,36,.055); transition: transform .3s var(--ease), box-shadow .3s var(--ease); }
      .problem-card:hover { transform: translateY(-7px); box-shadow: var(--shadow-sm); }
      .problem-card__number { width: 44px; height: 44px; display: grid; place-items: center; border-radius: 14px; background: var(--lavender); font-size: .67rem; font-weight: 800; }
      .problem-card:nth-child(2) .problem-card__number { background: var(--peach); }
      .problem-card:nth-child(3) .problem-card__number { background: var(--mint); }
      .problem-card h3 { margin: auto 0 0; font-size: clamp(1.6rem,2.35vw,2.35rem); line-height: 1.03; letter-spacing: -.045em; }
      .problem-card p { margin: 16px 0 0; color: var(--muted); font-size: .88rem; line-height: 1.66; }
      .problem-closer { max-width: 980px; margin: 68px auto 0; text-align: center; font-size: clamp(2.5rem,5vw,5rem); line-height: .99; letter-spacing: -.06em; font-weight: 800; }
      .problem-closer span { position: relative; z-index: 0; display: inline-block; }
      .problem-closer span::after { content: ""; position: absolute; z-index: -1; left: -.04em; right: -.04em; bottom: .02em; height: .29em; border-radius: 999px; background: var(--lime); }
  
      /* Platform */
      .platform-layout { display: grid; grid-template-columns: .82fr 1.18fr; gap: 72px; align-items: start; }
      .platform-sticky { position: sticky; top: 178px; }
      .platform-list { border-top: 1px solid var(--line); }
      .platform-item { display: grid; grid-template-columns: 50px 1fr; gap: 18px; padding: 27px 0; border-bottom: 1px solid var(--line); }
      .platform-icon { width: 44px; height: 44px; display: grid; place-items: center; border-radius: 50%; background: var(--white); border: 1px solid var(--line); }
      .platform-icon svg { width: 21px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
      .platform-item h3 { margin: 0; font-size: 1.29rem; letter-spacing: -.03em; }
      .platform-item p { margin: 9px 0 0; color: var(--muted); font-size: .88rem; line-height: 1.65; }
  
      /* Architecture */
      .architecture-panel { margin-top: 50px; padding: 38px; border: 1px solid rgba(255,255,255,.85); border-radius: var(--radius-xl); background: linear-gradient(145deg,rgba(217,214,253,.80),rgba(153,254,237,.57) 54%,rgba(255,189,155,.55)); box-shadow: var(--shadow-md); }
      .architecture-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 16px; }
      .architecture-layer { min-height: 460px; padding: 28px; border-radius: 28px; background: rgba(255,255,255,.82); border: 1px solid rgba(255,255,255,.92); }
      .architecture-layer__top { display: flex; align-items: center; justify-content: space-between; gap: 15px; }
      .architecture-layer__top span:first-child { font-size: .64rem; font-weight: 800; letter-spacing: .1em; text-transform: uppercase; }
      .architecture-layer__top span:last-child { min-width: 35px; height: 35px; display: grid; place-items: center; border-radius: 50%; background: var(--gray); font-size: .62rem; font-weight: 800; }
      .architecture-layer h3 { margin: 38px 0 0; font-size: clamp(1.8rem,2.5vw,2.7rem); line-height: 1; letter-spacing: -.05em; }
      .architecture-layer p { margin: 15px 0 0; color: var(--muted); font-size: .84rem; line-height: 1.65; }
      .architecture-stack { display: grid; gap: 9px; margin-top: 28px; }
      .architecture-chip { min-height: 52px; display: flex; align-items: center; gap: 11px; padding: 10px 12px; border: 1px solid var(--line); border-radius: 15px; background: var(--white); font-size: .72rem; font-weight: 750; }
      .architecture-chip i { width: 9px; height: 9px; border-radius: 50%; background: var(--lime); box-shadow: 0 0 0 4px rgba(194,255,2,.13); }
      .architecture-layer:nth-child(2) .architecture-chip i { background: var(--lavender); box-shadow: 0 0 0 4px rgba(217,214,253,.32); }
      .architecture-layer:nth-child(3) .architecture-chip i { background: var(--mint); box-shadow: 0 0 0 4px rgba(153,254,237,.25); }
      .architecture-note { margin: 22px 0 0; color: rgba(32,33,36,.70); font-size: .78rem; line-height: 1.55; }
  
      /* Capabilities */
      .capability-grid { display: grid; grid-template-columns: repeat(12,1fr); gap: 18px; margin-top: 52px; }
      .capability-card { min-height: 310px; position: relative; overflow: hidden; padding: 28px; border: 1px solid var(--line); border-radius: var(--radius-md); background: var(--white); box-shadow: 0 14px 38px rgba(32,33,36,.05); }
      .capability-card--wide { grid-column: span 7; }
      .capability-card--medium { grid-column: span 5; }
      .capability-card--half { grid-column: span 6; }
      .capability-card--mint { background: linear-gradient(145deg,rgba(153,254,237,.56),#fff 76%); }
      .capability-card--lavender { background: linear-gradient(145deg,rgba(217,214,253,.74),#fff 76%); }
      .capability-card--peach { background: linear-gradient(145deg,rgba(255,189,155,.60),#fff 78%); }
      .capability-card__index { color: var(--muted); font-size: .62rem; font-weight: 800; letter-spacing: .1em; }
      .capability-card h3 { max-width: 540px; margin: 36px 0 0; font-size: clamp(1.8rem,3vw,3.25rem); line-height: .99; letter-spacing: -.052em; }
      .capability-card p { max-width: 540px; margin: 16px 0 0; color: var(--muted); font-size: .86rem; line-height: 1.65; }
      .capability-tags { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 25px; }
      .capability-tags span { padding: 8px 10px; border: 1px solid rgba(32,33,36,.10); border-radius: 999px; background: rgba(255,255,255,.68); font-size: .6rem; font-weight: 750; }
      .capability-visual { position: absolute; right: -18px; bottom: -15px; width: 180px; height: 170px; border-radius: 34px 0 0 0; background: rgba(255,255,255,.56); border: 1px solid rgba(255,255,255,.9); transform: rotate(-4deg); }
      .capability-visual::before,
      .capability-visual::after { content: ""; position: absolute; border-radius: 50%; }
      .capability-visual::before { width: 52px; height: 52px; left: 25px; top: 28px; background: var(--lime); }
      .capability-visual::after { width: 84px; height: 14px; left: 26px; top: 100px; border-radius: 999px; background: rgba(32,33,36,.15); box-shadow: 0 24px 0 rgba(32,33,36,.09); }
  
      /* Systems */
      .systems-grid { display: grid; grid-template-columns: repeat(4,1fr); gap: 14px; margin-top: 48px; }
      .system-group { padding: 25px; border: 1px solid var(--line); border-radius: 22px; background: var(--white); }
      .system-group h3 { margin: 0; font-size: 1rem; letter-spacing: -.02em; }
      .system-group ul { display: grid; gap: 9px; margin: 19px 0 0; padding: 0; list-style: none; }
      .system-group li { min-height: 38px; display: flex; align-items: center; gap: 9px; padding: 0 11px; border-radius: 11px; background: var(--gray); color: rgba(32,33,36,.76); font-size: .7rem; font-weight: 700; }
      .system-group li::before { content: ""; width: 6px; height: 6px; border-radius: 50%; background: var(--lime); }
      .systems-note { max-width: 820px; margin: 34px 0 0; font-size: 1.15rem; line-height: 1.55; letter-spacing: -.02em; font-weight: 700; }
  
      /* Workflow */
      .workflow-board { margin-top: 52px; padding: 24px; border: 1px solid var(--line); border-radius: var(--radius-xl); background: #fff; box-shadow: var(--shadow-sm); }
      .workflow-track { display: grid; grid-template-columns: repeat(6,1fr); gap: 9px; }
      .workflow-step { min-height: 300px; position: relative; padding: 22px; border-radius: 22px; background: var(--gray); overflow: hidden; }
      .workflow-step:nth-child(2) { background: rgba(217,214,253,.52); }
      .workflow-step:nth-child(3) { background: rgba(153,254,237,.48); }
      .workflow-step:nth-child(4) { background: rgba(255,189,155,.45); }
      .workflow-step:nth-child(5) { background: rgba(217,214,253,.38); }
      .workflow-step:nth-child(6) { background: rgba(194,255,2,.40); }
      .workflow-step__index { display: block; color: var(--muted); font-size: .58rem; font-weight: 800; letter-spacing: .1em; }
      .workflow-step h3 { margin: 70px 0 0; font-size: 1.15rem; line-height: 1.08; letter-spacing: -.035em; }
      .workflow-step p { margin: 10px 0 0; color: var(--muted); font-size: .68rem; line-height: 1.55; }
      .workflow-step__dot { position: absolute; left: 22px; bottom: 24px; width: 10px; height: 10px; border-radius: 50%; background: var(--ink); }
      .workflow-step:not(:last-child)::after { content: "→"; position: absolute; z-index: 2; right: -16px; top: 50%; width: 32px; height: 32px; display: grid; place-items: center; border-radius: 50%; background: var(--white); border: 1px solid var(--line); transform: translateY(-50%); font-size: .78rem; font-weight: 800; }
  
      /* Use cases */
      .use-case-grid { display: grid; grid-template-columns: repeat(2,1fr); gap: 18px; margin-top: 50px; }
      .use-case-card { min-height: 310px; padding: 30px; border: 1px solid var(--line); border-radius: 28px; background: var(--white); }
      .use-case-card__top { display: flex; align-items: center; justify-content: space-between; gap: 18px; }
      .use-case-card__icon { width: 48px; height: 48px; display: grid; place-items: center; border-radius: 15px; background: var(--lime); }
      .use-case-card:nth-child(2) .use-case-card__icon { background: var(--lavender); }
      .use-case-card:nth-child(3) .use-case-card__icon { background: var(--mint); }
      .use-case-card:nth-child(4) .use-case-card__icon { background: var(--peach); }
      .use-case-card__icon svg { width: 23px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
      .use-case-card__top small { color: var(--muted); font-size: .6rem; font-weight: 800; letter-spacing: .09em; text-transform: uppercase; }
      .use-case-card h3 { max-width: 500px; margin: 46px 0 0; font-size: clamp(1.7rem,2.6vw,2.8rem); line-height: 1; letter-spacing: -.05em; }
      .use-case-card p { max-width: 520px; margin: 15px 0 0; color: var(--muted); font-size: .84rem; line-height: 1.64; }
  
      /* Proof / case study */
      .case-panel { display: grid; grid-template-columns: .9fr 1.1fr; gap: 40px; padding: 46px; border-radius: var(--radius-xl); background: var(--ink); color: var(--white); overflow: hidden; position: relative; }
      .case-panel::after { content: ""; position: absolute; width: 560px; height: 560px; right: -220px; top: -230px; border-radius: 50%; background: radial-gradient(circle,rgba(194,255,2,.28),rgba(153,254,237,.15) 44%,transparent 70%); }
      .case-panel .eyebrow { color: rgba(255,255,255,.54); }
      .case-panel h2 { max-width: 600px; margin: 0; font-size: clamp(2.9rem,5.2vw,5.4rem); line-height: .98; letter-spacing: -.06em; }
      .case-panel p { max-width: 610px; margin: 23px 0 0; color: rgba(255,255,255,.66); font-size: .95rem; line-height: 1.7; }
      .case-stats { position: relative; z-index: 2; display: grid; grid-template-columns: 1fr 1fr; gap: 12px; align-content: center; }
      .case-stat { min-height: 170px; padding: 24px; border: 1px solid rgba(255,255,255,.12); border-radius: 24px; background: rgba(255,255,255,.07); -webkit-backdrop-filter: blur(12px); backdrop-filter: blur(12px); }
      .case-stat strong { display: block; color: var(--lime); font-size: clamp(2.3rem,4vw,4.1rem); letter-spacing: -.06em; }
      .case-stat span { display: block; margin-top: 12px; color: rgba(255,255,255,.65); font-size: .68rem; line-height: 1.45; }
      .case-note { grid-column: 1/-1; color: rgba(255,255,255,.46); font-size: .58rem; line-height: 1.5; }
  
      /* Process */
      .process-grid { display: grid; grid-template-columns: repeat(4,1fr); gap: 14px; margin-top: 49px; }
      .process-card { min-height: 330px; display: flex; flex-direction: column; padding: 27px; border: 1px solid var(--line); border-radius: 24px; background: var(--white); }
      .process-card__index { width: 44px; height: 44px; display: grid; place-items: center; border-radius: 14px; background: var(--gray); font-size: .68rem; font-weight: 800; }
      .process-card:nth-child(2) .process-card__index { background: var(--lavender); }
      .process-card:nth-child(3) .process-card__index { background: var(--mint); }
      .process-card:nth-child(4) .process-card__index { background: var(--lime); }
      .process-card h3 { margin: auto 0 0; font-size: 1.42rem; letter-spacing: -.035em; }
      .process-card p { margin: 13px 0 0; color: var(--muted); font-size: .82rem; line-height: 1.6; }
  
      /* FAQ */
      .faq-layout { display: grid; grid-template-columns: .78fr 1.22fr; gap: 70px; align-items: start; }
      .faq-heading { position: sticky; top: 175px; }
      .faq-list { border-top: 1px solid var(--line); }
      .faq-item { border-bottom: 1px solid var(--line); }
      .faq-item summary { min-height: 84px; display: grid; grid-template-columns: 1fr auto; align-items: center; gap: 24px; list-style: none; cursor: pointer; font-size: 1.08rem; font-weight: 750; letter-spacing: -.025em; }
      .faq-item summary::-webkit-details-marker { display: none; }
      .faq-item summary::after { content: "+"; width: 36px; height: 36px; display: grid; place-items: center; border-radius: 50%; background: var(--gray); font-size: 1.1rem; transition: transform .22s ease,background .22s ease; }
      .faq-item[open] summary::after { transform: rotate(45deg); background: var(--lime); }
      .faq-item p { max-width: 700px; margin: -5px 0 28px; color: var(--muted); font-size: .87rem; line-height: 1.7; }
  
      /* Final CTA */
      .final-cta { position: relative; overflow: hidden; padding: 70px; border-radius: 44px; background: linear-gradient(135deg,var(--lavender),var(--mint) 56%,var(--peach)); }
      .final-cta::before { content: ""; position: absolute; width: 540px; height: 540px; right: -180px; top: -250px; border-radius: 50%; background: rgba(255,255,255,.33); }
      .final-cta__content { position: relative; z-index: 2; max-width: 820px; }
      .final-cta h2 { margin: 0; font-size: clamp(3rem,6.6vw,6.5rem); line-height: .95; letter-spacing: -.066em; }
      .final-cta p { max-width: 670px; margin: 23px 0 0; color: rgba(32,33,36,.68); font-size: 1.02rem; line-height: 1.68; }
      .final-cta__actions { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 30px; }
      .final-cta__note { margin-top: 20px !important; font-size: .72rem !important; font-weight: 700; }
  
      /* Footer */
      .site-footer { padding: 80px 0 34px; }
      .footer-grid { display: grid; grid-template-columns: 1.25fr repeat(3,.75fr); gap: 50px; padding-bottom: 52px; border-bottom: 1px solid var(--line); }
      .footer-brand p { max-width: 340px; margin: 18px 0 0; color: var(--muted); font-size: .76rem; line-height: 1.65; }
      .footer-column strong { display: block; margin-bottom: 15px; font-size: .72rem; }
      .footer-column a { display: block; padding: 6px 0; color: var(--muted); font-size: .7rem; }
      .footer-column a:hover { color: var(--ink); }
      .footer-bottom { display: flex; align-items: center; justify-content: space-between; gap: 20px; padding-top: 24px; color: var(--muted); font-size: .62rem; }
      .footer-bottom__links { display: flex; flex-wrap: wrap; gap: 18px; }
  
      /* Reveal */
      [data-reveal] { opacity: 0; transform: translateY(20px); transition: opacity .65s ease, transform .65s var(--ease); }
      [data-reveal].is-visible { opacity: 1; transform: none; }
  
      .mobile-sticky-cta { display: none; }
  
      @media (max-width: 1120px) {
        :root { --page: min(100% - 34px, 1120px); }
        .desktop-nav { gap: 1px; }
        .desktop-nav__trigger,
        .desktop-nav__direct { padding-inline: 9px; font-size: .76rem; }
        .hero-grid { grid-template-columns: 1fr; }
        .hero-content { max-width: 860px; }
        .enterprise-art { min-height: 620px; max-width: 820px; width: 100%; margin: 10px auto 0; }
        .section-heading-split { grid-template-columns: 1fr; gap: 24px; }
        .section-heading-split .section-copy { margin-top: 0; }
        .architecture-grid { grid-template-columns: 1fr; }
        .architecture-layer { min-height: 0; }
        .workflow-track { grid-template-columns: repeat(3,1fr); }
        .workflow-step:nth-child(3)::after { display: none; }
        .process-grid { grid-template-columns: repeat(2,1fr); }
        .systems-grid { grid-template-columns: repeat(2,1fr); }
      }
  
      @media (max-width: 900px) {
        .desktop-nav,
        .modern-nav__cta { display: none; }
        .modern-nav { grid-template-columns: auto 1fr auto; }
        .menu-toggle { display: grid; grid-column: 3; }
        .section { padding: 92px 0; }
        .section-title { font-size: clamp(2.9rem,8vw,5.2rem); }
        .proof-panel { grid-template-columns: 1fr 1fr; }
        .proof-intro { grid-column: 1/-1; }
        .proof-item { border-top: 1px solid var(--line); }
        .proof-item:nth-of-type(2) { border-left: 0; }
        .page-nav-wrap { top: 84px; }
        .problem-grid { grid-template-columns: 1fr; }
        .problem-card { min-height: 280px; }
        .platform-layout { grid-template-columns: 1fr; gap: 46px; }
        .platform-sticky { position: static; }
        .capability-card--wide,
        .capability-card--medium,
        .capability-card--half { grid-column: span 12; }
        .case-panel { grid-template-columns: 1fr; }
        .faq-layout { grid-template-columns: 1fr; gap: 44px; }
        .faq-heading { position: static; }
        .footer-grid { grid-template-columns: 1fr 1fr; }
      }
  
      @media (max-width: 680px) {
        :root { --page: min(100% - 30px, 650px); }
        .site-header { padding: 10px 0; }
        .modern-nav { min-height: 60px; padding: 5px 6px 5px 17px; }
        .menu-toggle { width: 46px; height: 46px; }
        .hero { min-height: auto; padding: 126px 0 72px; }
        .hero-title { font-size: clamp(3rem,14vw,4.25rem); line-height: .98; letter-spacing: -.052em; }
        .hero-subheadline { font-size: 1.2rem; }
        .hero-actions { display: grid; grid-template-columns: 1fr; }
        .hero-actions .btn { width: 100%; }
        .enterprise-art { min-height: 540px; margin-top: 20px; }
        .enterprise-core { width: 142px; height: 142px; }
        .enterprise-core__copy strong { font-size: 1.02rem; }
        .enterprise-node { min-width: 148px; grid-template-columns: 38px 1fr; padding: 10px; }
        .enterprise-node__icon { width: 38px; height: 38px; }
        .enterprise-node__copy strong { font-size: .64rem; }
        .enterprise-node__copy small { font-size: .5rem; }
        .enterprise-node--stores { left: -2%; top: 14%; }
        .enterprise-node--punchout { right: -2%; top: 15%; }
        .enterprise-node--orders { left: -2%; bottom: 15%; }
        .enterprise-node--accounting { right: -2%; bottom: 15%; }
        .enterprise-node--suppliers { top: 1%; }
        .enterprise-node--approvals { bottom: 0; }
        .enterprise-mini-dashboard { display: none; }
        .flow-line { opacity: .28; }
        .proof-panel { grid-template-columns: 1fr; }
        .proof-intro { grid-column: auto; }
        .proof-item { border-left: 0 !important; }
        .page-nav-shell { border-radius: 22px; }
        .page-nav-toggle { width: 100%; min-height: 54px; display: flex; align-items: center; justify-content: space-between; gap: 16px; padding: 0 17px; border: 0; background: transparent; text-align: left; }
        .page-nav-toggle small,
        .page-nav-toggle strong { display: block; }
        .page-nav-toggle small { color: var(--muted); font-size: .55rem; font-weight: 800; text-transform: uppercase; letter-spacing: .1em; }
        .page-nav-toggle strong { margin-top: 2px; font-size: .75rem; }
        .page-nav-toggle__icon { font-size: 1rem; transition: transform .2s ease; }
        .page-nav-shell.is-open .page-nav-toggle__icon { transform: rotate(45deg); }
        .page-nav { display: grid; grid-template-rows: 0fr; padding: 0 7px; overflow: hidden; transition: grid-template-rows .25s var(--ease), padding .25s ease; }
        .page-nav::before { content: ""; overflow: hidden; }
        .page-nav-shell.is-open .page-nav { grid-template-rows: none; display: grid; padding: 0 7px 7px; }
        .page-nav-shell:not(.is-open) .page-nav a { display: none; }
        .page-nav a { width: 100%; min-height: 40px; border-radius: 12px; }
        .page-nav__cta { margin-left: 0 !important; margin-top: 3px; justify-content: center; }
        .problem-closer { margin-top: 52px; }
        .architecture-panel { padding: 18px; border-radius: 28px; }
        .capability-grid { display: grid; grid-template-columns: 1fr; }
        .capability-card--wide,
        .capability-card--medium,
        .capability-card--half { grid-column: auto; }
        .systems-grid { grid-template-columns: 1fr; }
        .workflow-track { grid-template-columns: 1fr; }
        .workflow-step { min-height: 240px; }
        .workflow-step:not(:last-child)::after { content: "↓"; right: 50%; top: auto; bottom: -17px; transform: translateX(50%); }
        .use-case-grid { grid-template-columns: 1fr; }
        .case-panel { padding: 28px; border-radius: 30px; }
        .case-stats { grid-template-columns: 1fr; }
        .case-note { grid-column: auto; }
        .process-grid { grid-template-columns: 1fr; }
        .process-card { min-height: 260px; }
        .final-cta { padding: 42px 24px; border-radius: 30px; }
        .final-cta__actions { display: grid; grid-template-columns: 1fr; }
        .final-cta__actions .btn { width: 100%; }
        .footer-grid { grid-template-columns: 1fr 1fr; gap: 28px 20px; }
        .footer-bottom { flex-direction: column; align-items: flex-start; }
        .mobile-sticky-cta {
          position: fixed;
          z-index: 270;
          left: 12px;
          right: 12px;
          bottom: max(12px, env(safe-area-inset-bottom));
          display: block;
          padding: 8px;
          border: 1px solid rgba(255,255,255,.88);
          border-radius: 999px;
          background: rgba(255,255,255,.90);
          box-shadow: 0 18px 45px rgba(32,33,36,.17);
          -webkit-backdrop-filter: blur(14px);
          backdrop-filter: blur(14px);
          transform: translateY(120%);
          transition: transform .35s var(--ease);
        }
        .mobile-sticky-cta.is-visible { transform: translateY(0); }
        .mobile-sticky-cta .btn { width: 100%; min-height: 50px; }
        .site-footer { padding-bottom: 110px; }
      }
  
      @media (max-width: 440px) {
        .enterprise-art { min-height: 500px; }
        .enterprise-node { min-width: 133px; }
        .enterprise-node__copy small { display: none; }
        .enterprise-node--stores { left: -3%; }
        .enterprise-node--punchout { right: -3%; }
        .enterprise-node--orders { left: -3%; }
        .enterprise-node--accounting { right: -3%; }
        .footer-grid { grid-template-columns: 1fr; }
      }
  
      @media (prefers-reduced-motion: reduce) {
        *, *::before, *::after { scroll-behavior: auto !important; animation-duration: .01ms !important; animation-iteration-count: 1 !important; transition-duration: .01ms !important; }
        [data-reveal] { opacity: 1; transform: none; }
      }
  
      @media print {
        .site-header,
        .page-nav-wrap,
        .mobile-sticky-cta,
        .mobile-menu { display: none !important; }
        .hero { min-height: auto; padding-top: 40px; }
        [data-reveal] { opacity: 1; transform: none; }
      }
    
  
  /* ==========================================================
     V4: Base platform + optional add-ons
     ========================================================== */
  .solution-package-section {
    background:
      radial-gradient(circle at 92% 8%, rgba(217,214,253,.36), transparent 24%),
      radial-gradient(circle at 8% 78%, rgba(153,254,237,.26), transparent 25%),
      linear-gradient(180deg,#fff 0%,#fbfbf8 100%);
  }
  .solution-base-card {
    margin-top: 52px;
    padding: 38px;
    border: 1px solid rgba(32,33,36,.12);
    border-radius: 34px;
    background:
      radial-gradient(circle at 94% 10%, rgba(194,255,2,.28), transparent 24%),
      linear-gradient(145deg,#fff,#f8faf3);
    box-shadow: 0 24px 70px rgba(32,33,36,.075);
    overflow: hidden;
    position: relative;
  }
  .solution-base-card::after {
    content:"";
    position:absolute;
    right:-70px;
    bottom:-110px;
    width:260px;
    height:260px;
    border:1px solid rgba(32,33,36,.07);
    border-radius:50%;
  }
  .solution-base-card__top,
  .solution-addon-card__top {
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:14px;
  }
  .solution-base-card__top > div { display:flex; align-items:center; flex-wrap:wrap; gap:10px; }
  .solution-pill {
    display:inline-flex;
    align-items:center;
    min-height:30px;
    padding:0 10px;
    border:1px solid rgba(32,33,36,.10);
    border-radius:999px;
    background:rgba(255,255,255,.72);
    color:var(--ink);
    font-size:.57rem;
    font-weight:800;
    letter-spacing:.08em;
    text-transform:uppercase;
  }
  .solution-pill--included { background:var(--lime); border-color:transparent; }
  .solution-pill--dark { color:#fff; background:var(--ink); border-color:var(--ink); }
  .solution-card-kicker,
  .solution-base-card__status {
    color:var(--muted);
    font-size:.62rem;
    font-weight:800;
    letter-spacing:.09em;
    text-transform:uppercase;
  }
  .solution-base-card__body {
    position:relative;
    z-index:2;
    display:grid;
    grid-template-columns:1.05fr .95fr;
    gap:54px;
    align-items:end;
    margin-top:38px;
  }
  .solution-base-card h3 {
    max-width:650px;
    margin:0;
    font-size:clamp(2.6rem,4.5vw,5rem);
    line-height:.96;
    letter-spacing:-.06em;
  }
  .solution-base-card p {
    max-width:660px;
    margin:20px 0 0;
    color:var(--muted);
    font-size:.94rem;
    line-height:1.7;
  }
  .solution-base-features {
    display:grid;
    grid-template-columns:1fr 1fr;
    gap:10px;
  }
  .solution-base-features > div {
    min-height:112px;
    padding:19px;
    border:1px solid rgba(32,33,36,.08);
    border-radius:20px;
    background:rgba(255,255,255,.72);
  }
  .solution-base-features strong,
  .solution-base-features span { display:block; }
  .solution-base-features strong { font-size:.92rem; }
  .solution-base-features span { margin-top:7px; color:var(--muted); font-size:.72rem; line-height:1.45; }
  .optional-addons-intro {
    display:flex;
    align-items:center;
    gap:18px;
    margin:30px 0 18px;
    padding:22px 24px;
    border:1px solid rgba(32,33,36,.09);
    border-radius:22px;
    background:var(--ink);
    color:#fff;
  }
  .optional-addons-intro__icon {
    width:48px;
    height:48px;
    flex:0 0 48px;
    display:grid;
    place-items:center;
    border-radius:50%;
    background:var(--lime);
    color:var(--ink);
    font-size:1.4rem;
  }
  .optional-addons-intro small,
  .optional-addons-intro strong { display:block; }
  .optional-addons-intro small { color:rgba(255,255,255,.55); font-size:.58rem; font-weight:800; letter-spacing:.1em; text-transform:uppercase; }
  .optional-addons-intro strong { margin-top:4px; font-size:1.02rem; }
  .optional-addons-intro p { margin:5px 0 0; color:rgba(255,255,255,.65); font-size:.74rem; line-height:1.5; }
  .addon-card-grid {
    display:grid;
    grid-template-columns:repeat(3,minmax(0,1fr));
    gap:16px;
  }
  .solution-addon-card {
    min-height:355px;
    display:flex;
    flex-direction:column;
    padding:26px;
    border:1px solid rgba(32,33,36,.085);
    border-radius:26px;
    background:#fff;
    box-shadow:0 14px 38px rgba(32,33,36,.045);
    transition:transform .34s var(--ease), box-shadow .34s var(--ease), border-color .34s var(--ease);
  }
  .solution-addon-card:hover {
    transform:translateY(-7px);
    border-color:rgba(32,33,36,.15);
    box-shadow:0 24px 54px rgba(32,33,36,.09);
  }
  .solution-addon-card--lavender { background:linear-gradient(145deg,rgba(217,214,253,.62),#fff 68%); }
  .solution-addon-card--mint { background:linear-gradient(145deg,rgba(153,254,237,.52),#fff 68%); }
  .solution-addon-card--lime { background:linear-gradient(145deg,rgba(194,255,2,.34),#fff 68%); }
  .solution-addon-card--peach { background:linear-gradient(145deg,rgba(255,189,155,.48),#fff 68%); }
  .solution-addon-card--finance { background:linear-gradient(145deg,rgba(255,235,222,.92),rgba(217,214,253,.28) 48%,#fff 76%); }
  .solution-addon-card--ai { color:#fff; background:radial-gradient(circle at 84% 12%,rgba(194,255,2,.18),transparent 25%),linear-gradient(145deg,#202124,#2a2c31); }
  .solution-addon-card--ai p,
  .solution-addon-card--ai .solution-addon-card__best small { color:rgba(255,255,255,.62); }
  .solution-addon-card--ai .solution-addon-card__best strong { color:#fff; }
  .solution-addon-card--ai .solution-addon-tags span { color:#fff; border-color:rgba(255,255,255,.12); background:rgba(255,255,255,.08); }
  .solution-addon-card--ai .solution-addon-card__top > span:last-child { color:rgba(255,255,255,.48); }
  .solution-addon-card--custom { grid-column:span 2; background:linear-gradient(135deg,rgba(217,214,253,.74),rgba(153,254,237,.48),rgba(255,189,155,.34)); }
  .solution-addon-card__top > span:last-child { color:var(--muted); font-size:.62rem; font-weight:800; }
  .solution-addon-card h3 {
    margin:28px 0 0;
    font-size:clamp(1.65rem,2.35vw,2.55rem);
    line-height:1.02;
    letter-spacing:-.045em;
  }
  .solution-addon-card p {
    margin:16px 0 0;
    color:var(--muted);
    font-size:.82rem;
    line-height:1.65;
  }
  .solution-addon-tags { display:flex; flex-wrap:wrap; gap:7px; margin-top:19px; }
  .solution-addon-tags span { padding:7px 9px; border:1px solid rgba(32,33,36,.08); border-radius:999px; background:rgba(255,255,255,.62); font-size:.57rem; font-weight:800; }
  .solution-addon-card__best { margin-top:auto; padding-top:22px; }
  .solution-addon-card__best small,
  .solution-addon-card__best strong { display:block; }
  .solution-addon-card__best small { color:var(--muted); font-size:.55rem; font-weight:800; letter-spacing:.09em; text-transform:uppercase; }
  .solution-addon-card__best strong { margin-top:6px; font-size:.73rem; line-height:1.5; }
  .solution-package-section .architecture-note { margin-top:20px; }
  
  /* Landing-page Club Colors customer-story component */
  .customer-story-section {
    padding-top:54px;
    padding-bottom:112px;
    background:
      radial-gradient(circle at 9% 12%, rgba(153,254,237,.34), transparent 25%),
      radial-gradient(circle at 91% 82%, rgba(217,214,253,.48), transparent 29%),
      linear-gradient(180deg,#fff 0%,#f8f8f5 100%);
  }
  .customer-story {
    display:grid;
    grid-template-columns:1.14fr .86fr;
    min-height:760px;
    overflow:hidden;
    border-radius:46px;
    color:var(--graphite);
    background:rgba(255,255,255,.90);
    border:1px solid rgba(32,33,36,.08);
    box-shadow:0 34px 100px rgba(32,33,36,.10),inset 0 1px 0 rgba(255,255,255,.92);
  }
  .customer-story__main {
    padding:72px 66px;
    display:flex;
    flex-direction:column;
    justify-content:space-between;
    position:relative;
    isolation:isolate;
    background:
      radial-gradient(circle at 88% 7%, rgba(194,255,2,.16), transparent 23%),
      radial-gradient(circle at 8% 92%, rgba(153,254,237,.22), transparent 27%),
      linear-gradient(145deg,#fff 0%,#fbfbf8 100%);
  }
  .customer-story__main::before {
    content:"“";
    position:absolute;
    z-index:-1;
    right:5%;
    top:-7%;
    color:rgba(32,33,36,.045);
    font-size:clamp(18rem,28vw,32rem);
    line-height:1;
    font-weight:800;
  }
  .customer-story__header .eyebrow { color:var(--muted); }
  .customer-story__header h2 { margin:0; max-width:850px; font-size:clamp(3.2rem,6.2vw,6.8rem); line-height:.93; letter-spacing:-.07em; }
  .customer-story__header > p { margin:28px 0 0; max-width:710px; color:rgba(32,33,36,.66); font-size:1.02rem; line-height:1.72; }
  .customer-story__quote {
    position:relative;
    margin:58px 0 0;
    padding:30px 32px;
    max-width:900px;
    border:1px solid rgba(32,33,36,.08);
    border-radius:26px;
    background:rgba(255,255,255,.74);
    box-shadow:0 18px 46px rgba(32,33,36,.055);
  }
  .customer-story__quote::before { content:""; position:absolute; left:0; top:28px; bottom:28px; width:4px; border-radius:999px; background:linear-gradient(180deg,var(--lime),var(--mint),var(--lavender)); }
  .customer-story__quote p { margin:0 0 19px; color:rgba(32,33,36,.80); font-size:clamp(1.08rem,1.5vw,1.35rem); line-height:1.62; letter-spacing:-.018em; }
  .customer-story__quote p:last-child { margin-bottom:0; }
  .customer-story__attribution { display:flex; align-items:center; gap:14px; margin-top:42px; }
  .customer-story__mark { width:52px; height:52px; flex:0 0 52px; display:grid; place-items:center; border-radius:16px; background:#fff; padding:4px; box-shadow:0 10px 28px rgba(32,33,36,.07); }
  .customer-story__mark svg { width:100%; height:100%; }
  .customer-story__attribution strong,
  .customer-story__attribution span { display:block; }
  .customer-story__attribution strong { font-size:1rem; }
  .customer-story__attribution span { margin-top:5px; color:var(--muted); font-size:.78rem; }
  .customer-story__proof {
    padding:62px 48px;
    display:flex;
    flex-direction:column;
    color:var(--graphite);
    background:
      radial-gradient(circle at 84% 8%,rgba(255,255,255,.92),transparent 24%),
      radial-gradient(circle at 14% 88%,rgba(194,255,2,.20),transparent 26%),
      linear-gradient(155deg,var(--mint),var(--lavender) 52%,var(--peach));
    border-left:1px solid rgba(32,33,36,.07);
  }
  .customer-story__label { display:block; font-size:.68rem; font-weight:800; letter-spacing:.12em; text-transform:uppercase; }
  .customer-story__proof-top h3 { margin:18px 0 0; font-size:clamp(2rem,3.4vw,3.7rem); line-height:1; letter-spacing:-.055em; }
  .customer-story__metrics { display:grid; gap:0; margin-top:42px; border-top:1px solid rgba(32,33,36,.20); }
  .customer-story__metric { padding:26px 0; border-bottom:1px solid rgba(32,33,36,.20); }
  .customer-story__metric strong { display:block; font-size:clamp(2.7rem,5vw,5.5rem); line-height:.92; letter-spacing:-.07em; }
  .customer-story__metric span { display:block; margin-top:11px; max-width:390px; color:rgba(32,33,36,.68); font-size:.82rem; line-height:1.5; }
  .customer-story__scope { margin-top:36px; }
  .customer-story__scope ul { margin:18px 0 0; padding:0; display:grid; gap:11px; list-style:none; }
  .customer-story__scope li { position:relative; padding-left:24px; font-size:.84rem; line-height:1.45; font-weight:650; }
  .customer-story__scope li::before { content:"↗"; position:absolute; left:0; top:0; font-weight:800; }
  .customer-story__link { margin-top:auto; padding-top:40px; color:var(--graphite); }
  
  /* Process hover motion */
  .process-card {
    position:relative;
    overflow:hidden;
    transition:transform .35s var(--ease), box-shadow .35s var(--ease), border-color .35s var(--ease), background .35s var(--ease);
  }
  .process-card::before {
    content:"";
    position:absolute;
    inset:0;
    opacity:0;
    background:radial-gradient(circle at 82% 12%,rgba(194,255,2,.18),transparent 30%);
    transition:opacity .35s ease;
    pointer-events:none;
  }
  .process-card:hover {
    transform:translateY(-10px) rotate(-.35deg);
    border-color:rgba(32,33,36,.15);
    box-shadow:0 22px 52px rgba(32,33,36,.09);
  }
  .process-card:hover::before { opacity:1; }
  .process-card__index { position:relative; z-index:1; transition:transform .35s var(--ease), box-shadow .35s var(--ease); }
  .process-card:hover .process-card__index { transform:translateY(-3px) rotate(7deg) scale(1.06); box-shadow:0 10px 22px rgba(32,33,36,.08); }
  .process-card h3,
  .process-card p { position:relative; z-index:1; transition:transform .35s var(--ease); }
  .process-card:hover h3 { transform:translateX(4px); }
  .process-card:hover p { transform:translateX(2px); }
  
  /* Lighter final CTA */
  .final-cta-section {
    background:linear-gradient(180deg,#fbfbf9 0%,#f3f7f6 100%);
  }
  .final-cta {
    border:1px solid rgba(32,33,36,.075);
    background:
      radial-gradient(circle at 86% 8%,rgba(255,255,255,.94),transparent 25%),
      radial-gradient(circle at 16% 92%,rgba(194,255,2,.16),transparent 24%),
      linear-gradient(140deg,#f7fff9 0%,#f2f0ff 52%,#fff6f0 100%);
    box-shadow:0 28px 74px rgba(32,33,36,.08);
  }
  .final-visual__orbit { border-color:rgba(32,33,36,.11); }
  .final-visual__orbit--two { border-color:rgba(32,33,36,.12); }
  .final-visual__core {
    background:rgba(255,255,255,.90);
    color:var(--ink);
    border:1px solid rgba(32,33,36,.09);
    box-shadow:0 22px 48px rgba(32,33,36,.10);
  }
  .final-visual__core small { color:var(--muted); }
  .final-visual__chip { border-color:rgba(32,33,36,.07); box-shadow:0 10px 26px rgba(32,33,36,.065); }
  .final-visual__chip--one { background:rgba(217,214,253,.88); }
  .final-visual__chip--two { background:rgba(194,255,2,.80); }
  .final-visual__chip--three { background:rgba(255,189,155,.82); }
  .final-visual__chip--four { background:rgba(153,254,237,.86); }
  .final-visual__chip--five { background:rgba(255,255,255,.92); }
  
  @media (max-width:1100px) {
    .solution-base-card__body { grid-template-columns:1fr; }
    .addon-card-grid { grid-template-columns:repeat(2,minmax(0,1fr)); }
    .solution-addon-card--custom { grid-column:span 2; }
    .customer-story { grid-template-columns:1fr; }
    .customer-story__proof { min-height:680px; border-left:0; border-top:1px solid rgba(32,33,36,.07); }
  }
  @media (max-width:720px) {
    .solution-base-card { padding:27px; border-radius:28px; }
    .solution-base-card__top { align-items:flex-start; flex-direction:column; }
    .solution-base-features { grid-template-columns:1fr; }
    .optional-addons-intro { align-items:flex-start; }
    .addon-card-grid { grid-template-columns:1fr; }
    .solution-addon-card--custom { grid-column:auto; }
    .solution-addon-card { min-height:320px; }
    .customer-story-section { padding-bottom:92px; }
    .customer-story { border-radius:32px; }
    .customer-story__main,
    .customer-story__proof { padding:46px 28px; }
    .customer-story__header h2 { font-size:clamp(3rem,13vw,4.7rem); }
    .customer-story__quote { margin-top:42px; padding:26px 24px; }
    .customer-story__quote p { font-size:1.02rem; line-height:1.65; }
    .customer-story__proof { min-height:auto; }
    .customer-story__metric strong { font-size:clamp(3.2rem,15vw,5rem); }
    .customer-story__link { margin-top:0; }
  }
  @media (prefers-reduced-motion: reduce) {
    .process-card,
    .process-card__index,
    .process-card h3,
    .process-card p,
    .solution-addon-card { transition:none !important; transform:none !important; }
  }
  
  
  /* ==========================================================
     V6 refinements: pain-to-relief typography + distinct add-ons
     ========================================================== */
  .better-approach-type { max-width: 1160px; }
  .better-approach-type__interactive {
    --gradient-x: 50%;
    --gradient-y: 50%;
    position: relative;
    cursor: default;
    background:
      radial-gradient(circle 220px at var(--gradient-x) var(--gradient-y),
        var(--lime) 0%,
        #8b79ea 28%,
        var(--ink) 62%,
        #536d66 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    transition: filter .22s ease, transform .22s ease;
  }
  .better-approach-type__interactive span,
  .better-approach-type__interactive .better-approach-type__soft,
  .better-approach-type__interactive .better-approach-type__accent {
    color: transparent !important;
    background: none !important;
    -webkit-background-clip: initial;
    background-clip: initial;
  }
  .better-approach-type__interactive:hover {
    filter: saturate(1.08);
    transform: translateY(-2px);
  }
  .better-approach-type__interactive .better-approach-type__soft {
    font-weight: 600;
    font-size: .58em;
    letter-spacing: -.038em;
  }
  
  /* Add-on layout: two rows of three, then one slim full-width custom card. */
  .addon-card-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
  }
  .solution-addon-card {
    min-height: 390px;
    z-index: 0;
    color: var(--ink);
    border-color: rgba(32,33,36,.08);
    box-shadow: 0 18px 42px rgba(32,33,36,.06);
  }
  .solution-addon-card > *:not(.solution-addon-card__bg-icon) {
    position: relative;
    z-index: 2;
  }
  .solution-addon-card::before {
    width: 230px;
    height: 230px;
    right: -120px;
    top: -120px;
    background: rgba(255,255,255,.20);
    opacity: .72;
  }
  .solution-addon-card:hover::before { opacity: 1; }
  .solution-addon-card:hover::after { background: rgba(255,255,255,.92); }
  
  /* Distinct Swishtag tiles */
  .solution-addon-card--lavender {
    background: linear-gradient(145deg, #d9d6fd 0%, #cbc6ff 100%);
  }
  .solution-addon-card--mint {
    background: linear-gradient(145deg, #99feed 0%, #77ead6 100%);
  }
  .solution-addon-card--lime {
    background: linear-gradient(145deg, #dfff73 0%, #c2ff02 100%);
  }
  .solution-addon-card--peach {
    background: linear-gradient(145deg, #ffd0b8 0%, #ffad83 100%);
  }
  .solution-addon-card--finance {
    background: linear-gradient(145deg, #ffe0bd 0%, #f2d6fd 100%);
  }
  .solution-addon-card--ai {
    color: #fff;
    background:
      radial-gradient(circle at 86% 10%, rgba(255,255,255,.08), transparent 26%),
      linear-gradient(145deg, #202124 0%, #292b30 58%, #17181b 100%);
  }
  .solution-addon-card--ai p,
  .solution-addon-card--ai .solution-addon-card__best small,
  .solution-addon-card--ai .solution-addon-card__top > span:last-child { color: rgba(255,255,255,.66); }
  .solution-addon-card--ai .solution-addon-card__best strong { color: #fff; }
  .solution-addon-card--ai .solution-addon-tags span {
    color: #fff;
    border-color: rgba(255,255,255,.14);
    background: rgba(255,255,255,.09);
  }
  .solution-addon-card--ai .solution-pill--dark {
    color: #fff;
    background: rgba(255,255,255,.10);
    border-color: rgba(255,255,255,.15);
  }
  .solution-addon-card--ai .solution-addon-card__icon {
    color: #fff;
    background: rgba(255,255,255,.10);
    border-color: rgba(255,255,255,.12);
  }
  .solution-addon-card--ai .solution-addon-card__number { color: rgba(255,255,255,.58); }
  
  /* Large background icon, white only. */
  .solution-addon-card__bg-icon {
    position: absolute;
    z-index: 1;
    right: -18px;
    bottom: -26px;
    width: 182px;
    height: 182px;
    display: grid;
    place-items: center;
    color: rgba(255,255,255,.38);
    opacity: .72;
    transform: rotate(-8deg) scale(.94);
    transform-origin: center;
    transition: color .35s ease, opacity .35s ease, transform .48s var(--ease);
    pointer-events: none;
  }
  .solution-addon-card__bg-icon svg {
    width: 100%;
    height: 100%;
    fill: none;
    stroke: currentColor;
    stroke-width: 1.15;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  .solution-addon-card:hover .solution-addon-card__bg-icon {
    color: #fff;
    opacity: .96;
    transform: rotate(-2deg) scale(1.06) translate(-8px,-8px);
  }
  .solution-addon-card--ai .solution-addon-card__bg-icon { color: rgba(255,255,255,.16); opacity: .82; }
  .solution-addon-card--ai:hover .solution-addon-card__bg-icon { color: #fff; opacity: .88; }
  .solution-addon-card:hover .solution-addon-card__icon {
    background: rgba(255,255,255,.96);
    color: var(--ink);
  }
  .solution-addon-card--ai:hover .solution-addon-card__icon {
    background: #fff;
    color: var(--ink);
  }
  
  /* Full-width Custom Automation card, intentionally slimmer than Base Platform. */
  .solution-addon-card--custom {
    grid-column: 1 / -1;
    min-height: 245px;
    display: grid;
    grid-template-columns: minmax(220px,.82fr) minmax(320px,1.25fr) minmax(220px,.72fr);
    grid-template-rows: auto 1fr;
    column-gap: 34px;
    row-gap: 12px;
    align-items: start;
    padding: 30px 34px;
    background:
      radial-gradient(circle at 84% 20%, rgba(255,255,255,.50), transparent 28%),
      linear-gradient(125deg, #ece9ff 0%, #d8fff6 55%, #fff0e5 100%);
  }
  .solution-addon-card--custom .solution-addon-card__top { grid-column: 1 / -1; }
  .solution-addon-card--custom h3 {
    grid-column: 1;
    margin-top: 14px;
    align-self: start;
  }
  .solution-addon-card--custom > p {
    grid-column: 2;
    margin-top: 14px;
    max-width: 620px;
  }
  .solution-addon-card--custom .solution-addon-tags {
    grid-column: 2;
    margin-top: 2px;
  }
  .solution-addon-card--custom .solution-addon-card__best {
    grid-column: 3;
    grid-row: 2 / span 2;
    margin-top: 12px;
    padding: 18px 20px;
    border: 1px solid rgba(32,33,36,.08);
    border-radius: 18px;
    background: rgba(255,255,255,.52);
  }
  .solution-addon-card--custom .solution-addon-card__bg-icon {
    width: 215px;
    height: 215px;
    right: 2%;
    bottom: -70px;
    color: rgba(255,255,255,.44);
  }
  
  @media (max-width: 980px) {
    .addon-card-grid { grid-template-columns: repeat(2, minmax(0,1fr)); }
    .solution-addon-card--custom {
      grid-column: 1 / -1;
      grid-template-columns: 1fr 1fr;
      min-height: 285px;
    }
    .solution-addon-card--custom h3 { grid-column: 1; }
    .solution-addon-card--custom > p,
    .solution-addon-card--custom .solution-addon-tags { grid-column: 1; }
    .solution-addon-card--custom .solution-addon-card__best { grid-column: 2; grid-row: 2 / span 3; }
  }
  @media (max-width: 680px) {
    .better-approach-type { margin-top: 72px; padding-inline: 0; }
    .better-approach-type__interactive { font-size: clamp(2.45rem, 11vw, 4.1rem); }
    .addon-card-grid { grid-template-columns: 1fr; }
    .solution-addon-card { min-height: 350px; }
    .solution-addon-card--custom {
      grid-column: auto;
      display: flex;
      min-height: 0;
      padding: 28px;
    }
    .solution-addon-card--custom h3 { margin-top: 26px; }
    .solution-addon-card--custom .solution-addon-card__best { margin-top: 24px; }
    .solution-addon-card__bg-icon { width: 155px; height: 155px; right: -15px; bottom: -18px; }
  }
  @media (prefers-reduced-motion: reduce) {
    .better-approach-type__interactive,
    .solution-addon-card__bg-icon { transition: none !important; transform: none !important; }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V3 refinements
     ========================================================== */
  :root { --page-nav-top: 96px; --navigation-width: min(1240px, calc(100vw - 48px)); }
  
  /* Above-the-fold hero */
  .hero {
    min-height: min(100svh, 860px);
    padding: 124px 0 44px;
  }
  .hero-grid { grid-template-columns: minmax(0, .98fr) minmax(470px, 1.02fr); gap: 40px; }
  .hero-title { max-width: 690px; font-size: clamp(3.35rem, 5.15vw, 5.8rem); line-height: .95; }
  .hero-subheadline { max-width: 620px; margin-top: 16px; font-size: clamp(1.12rem, 1.4vw, 1.42rem); }
  .hero-copy { max-width: 620px; margin-top: 11px; font-size: .98rem; line-height: 1.6; }
  .hero-actions { margin-top: 23px; }
  .hero-meta { margin-top: 18px; }
  .enterprise-art { min-height: 560px; transform: scale(.94); transform-origin: center center; }
  
  /* Trust strip */
  .proof-strip { padding: 0 0 38px; background: linear-gradient(180deg,#fbfbf8 0%,#fff 100%); }
  .proof-panel { grid-template-columns: minmax(260px,1.2fr) repeat(3,1fr); }
  .proof-intro, .proof-item { padding: 22px 24px; }
  .proof-intro { background: linear-gradient(145deg, var(--mint), rgba(217,214,253,.78)); }
  
  /* Connected primary + secondary navigation, matched to SAGE page */
  .site-header,
  .site-header.is-scrolled,
  .site-header.is-nav-connected { z-index: 320 !important; }
  .modern-nav { width: var(--navigation-width) !important; max-width: none !important; transition: border-radius .28s var(--ease), min-height .28s var(--ease), padding .28s var(--ease), box-shadow .22s ease; }
  .mega-menu { z-index: 360 !important; }
  .page-nav-wrap,
  .page-nav-wrap.is-stuck {
    top: var(--page-nav-top);
    z-index: 210 !important;
    padding: 7px 0 12px;
    background: transparent !important;
    border: 0 !important;
    -webkit-backdrop-filter: none !important;
    backdrop-filter: none !important;
  }
  .page-nav-wrap > .page-shell { width: var(--navigation-width) !important; max-width: none !important; }
  .page-nav-shell { width: 100%; min-height: 52px; padding: 6px 8px 6px 17px; border-radius: 999px; transition: border-radius .28s var(--ease), box-shadow .25s ease, background .25s ease; }
  .site-header.is-nav-connected { padding-bottom: 0; }
  .site-header.is-nav-connected .modern-nav {
    min-height: 60px;
    border-color: rgba(32,33,36,.085);
    border-bottom-color: transparent;
    border-radius: 24px 24px 0 0;
    background: rgba(255,255,255,.97);
    box-shadow: none;
  }
  .page-nav-wrap.is-stuck { padding-top: 0; }
  .page-nav-wrap.is-stuck .page-nav-shell {
    position: relative;
    margin-top: -2px;
    border-color: rgba(32,33,36,.085);
    border-top-color: transparent;
    border-radius: 0 0 24px 24px;
    background: rgba(255,255,255,.97);
    box-shadow: 0 20px 48px rgba(32,33,36,.105);
  }
  .page-nav-wrap.is-stuck .page-nav-shell::before {
    content:""; position:absolute; z-index:0; left:-1px; right:-1px; top:-5px; height:7px; background:rgba(255,255,255,.97); pointer-events:none;
  }
  .page-nav-shell > * { position: relative; z-index: 1; }
  
  /* Problem closer */
  .problem-closer-card {
    max-width: 1040px;
    margin: 58px auto 0;
    display: grid;
    grid-template-columns: 7px minmax(0,1fr) auto;
    align-items: center;
    gap: 28px;
    padding: 28px 30px 28px 0;
    overflow: hidden;
    border: 1px solid var(--line);
    border-radius: 28px;
    background: linear-gradient(135deg, rgba(255,255,255,.96), rgba(245,245,243,.95));
    box-shadow: var(--shadow-sm);
  }
  .problem-closer-card__accent { align-self: stretch; width: 7px; min-height: 120px; background: var(--lime); border-radius: 0 999px 999px 0; }
  .problem-closer-card__label { display:block; color:var(--muted); font-size:.62rem; font-weight:800; letter-spacing:.11em; text-transform:uppercase; }
  .problem-closer-card p { margin:8px 0 0; font-size:.9rem; font-weight:750; }
  .problem-closer-card h3 { max-width:760px; margin:7px 0 0; font-size:clamp(1.55rem,2.3vw,2.45rem); line-height:1.12; letter-spacing:-.04em; }
  .problem-closer-card__tags { display:grid; gap:7px; }
  .problem-closer-card__tags span { min-width:105px; padding:9px 12px; border-radius:999px; background:#fff; border:1px solid var(--line); font-size:.62rem; font-weight:800; text-align:center; }
  
  /* Base + add-on package explanation */
  .package-explainer { margin-top: 52px; display:grid; grid-template-columns:minmax(0,1fr) 80px minmax(0,1fr); align-items:stretch; gap:18px; }
  .package-card { min-height: 430px; padding:32px; border:1px solid var(--line); border-radius:32px; background:#fff; box-shadow:var(--shadow-sm); }
  .package-card--base { background: linear-gradient(145deg, rgba(194,255,2,.22), #fff 48%); border-color:rgba(32,33,36,.14); }
  .package-card--addons { background: linear-gradient(145deg, rgba(217,214,253,.48), rgba(153,254,237,.24)); }
  .package-card__top { display:flex; align-items:center; justify-content:space-between; gap:12px; color:var(--muted); font-size:.64rem; font-weight:800; letter-spacing:.08em; text-transform:uppercase; }
  .package-badge { display:inline-flex; align-items:center; min-height:29px; padding:0 10px; border-radius:999px; font-size:.58rem; }
  .package-badge--included { background:var(--lime); color:var(--ink); }
  .package-badge--optional { background:#fff; color:var(--ink); border:1px solid var(--line); }
  .package-card h3 { max-width:500px; margin:34px 0 0; font-size:clamp(2rem,3vw,3.25rem); line-height:1; letter-spacing:-.055em; }
  .package-card p { max-width:550px; margin:17px 0 0; color:var(--muted); font-size:.9rem; line-height:1.65; }
  .package-card ul { display:grid; gap:0; margin:28px 0 0; padding:0; list-style:none; border-top:1px solid rgba(32,33,36,.11); }
  .package-card li { position:relative; padding:15px 0 15px 25px; border-bottom:1px solid rgba(32,33,36,.11); font-size:.78rem; font-weight:750; }
  .package-card li::before { content:""; position:absolute; left:0; top:50%; width:8px; height:8px; transform:translateY(-50%); border-radius:50%; background:var(--lime); }
  .package-card--addons li::before { background:var(--ink); }
  .package-plus { display:grid; place-items:center; align-content:center; gap:8px; }
  .package-plus span { width:54px; height:54px; display:grid; place-items:center; border-radius:50%; background:var(--ink); color:#fff; font-size:1.4rem; font-weight:500; box-shadow:var(--shadow-sm); }
  .package-plus small { color:var(--muted); font-size:.56rem; font-weight:800; text-transform:uppercase; letter-spacing:.08em; text-align:center; }
  
  /* Add-on section UX */
  .addon-heading { display:grid; grid-template-columns:1fr auto; align-items:end; gap:36px; }
  .addon-heading .section-title { max-width:900px; }
  .addon-heading__note { max-width:310px; padding:18px 20px; border:1px solid rgba(32,33,36,.1); border-radius:18px; background:#fff; }
  .addon-heading__note span, .addon-heading__note strong { display:block; }
  .addon-heading__note span { color:var(--muted); font-size:.62rem; font-weight:800; text-transform:uppercase; letter-spacing:.08em; }
  .addon-heading__note strong { margin-top:7px; font-size:.86rem; line-height:1.4; }
  .addon-legend { display:flex; flex-wrap:wrap; gap:8px; margin-top:30px; }
  .addon-legend > span { display:inline-flex; align-items:center; gap:8px; min-height:34px; padding:0 11px; border:1px solid var(--line); border-radius:999px; background:#fff; color:var(--muted); font-size:.62rem; font-weight:750; }
  .addon-dot { width:8px; height:8px; border-radius:50%; background:var(--ink); }
  .addon-dot--ops { background:var(--lavender); } .addon-dot--data{background:var(--mint);} .addon-dot--finance{background:var(--peach);} .addon-dot--procurement{background:var(--lime);} .addon-dot--ai{background:var(--ink);}
  .capability-grid--addons { margin-top:20px; }
  .addon-card { display:flex; flex-direction:column; }
  .addon-card__top { display:flex; align-items:center; justify-content:space-between; gap:15px; }
  .addon-type { font-size:.61rem; font-weight:800; letter-spacing:.09em; text-transform:uppercase; }
  .addon-number { width:36px; height:36px; display:grid; place-items:center; flex:0 0 36px; border-radius:12px; background:rgba(255,255,255,.6); border:1px solid rgba(32,33,36,.09); font-size:.6rem; font-weight:800; }
  .addon-card h3 { margin-top:34px; }
  .addon-outcome { margin-top:auto; padding-top:22px; }
  .addon-outcome small { display:block; color:var(--muted); font-size:.58rem; font-weight:800; text-transform:uppercase; letter-spacing:.09em; }
  .addon-outcome strong { display:block; max-width:530px; margin-top:7px; font-size:.8rem; line-height:1.5; }
  .addon-card--custom { background:linear-gradient(135deg,var(--lavender),rgba(153,254,237,.5),rgba(255,189,155,.35)); }
  .addon-principle { margin-top:18px; display:flex; align-items:center; gap:12px; padding:18px 20px; border-radius:18px; background:var(--ink); color:#fff; }
  .addon-principle strong { color:var(--lime); font-size:.8rem; }
  .addon-principle span { color:rgba(255,255,255,.72); font-size:.76rem; line-height:1.5; }
  
  /* Integration groups */
  .systems-grid--five { grid-template-columns:repeat(5,1fr); }
  .system-group { min-height:285px; }
  .system-group__label { display:inline-flex; margin-bottom:14px; padding:6px 9px; border-radius:999px; background:var(--gray); color:var(--muted); font-size:.55rem; font-weight:800; letter-spacing:.08em; text-transform:uppercase; }
  .system-group:nth-child(2) .system-group__label { background:rgba(255,189,155,.4); }
  .system-group:nth-child(3) .system-group__label { background:rgba(194,255,2,.45); }
  .system-group:nth-child(4) .system-group__label { background:rgba(153,254,237,.5); }
  .system-group--ai { background:var(--ink); color:#fff; }
  .system-group--ai .system-group__label { background:rgba(255,255,255,.12); color:rgba(255,255,255,.68); }
  .system-group--ai li { background:rgba(255,255,255,.08); color:rgba(255,255,255,.82); }
  .integration-callout { margin-top:24px; display:grid; grid-template-columns:52px 1fr auto; align-items:center; gap:18px; padding:20px 22px; border:1px solid var(--line); border-radius:22px; background:linear-gradient(135deg,rgba(217,214,253,.3),rgba(153,254,237,.2)); }
  .integration-callout__mark { width:48px; height:48px; display:grid; place-items:center; border-radius:15px; background:var(--lime); font-size:1.1rem; font-weight:800; }
  .integration-callout strong { font-size:.9rem; }
  .integration-callout p { margin:5px 0 0; color:var(--muted); font-size:.72rem; line-height:1.55; }
  .integration-callout .text-link { margin:0; white-space:nowrap; }
  
  /* Order flow arrow visibility */
  .workflow-track { gap:18px; }
  .workflow-step { overflow:visible; }
  .workflow-step:not(:last-child)::after {
    right:-27px; width:36px; height:36px; z-index:20; border:1px solid rgba(32,33,36,.16); background:#fff; box-shadow:0 7px 18px rgba(32,33,36,.12); font-size:.92rem;
  }
  .workflow-board { overflow:visible; }
  
  /* Club Colors success story */
  .case-panel--club { grid-template-columns:1.02fr .98fr; min-height:560px; background:linear-gradient(145deg,#202124,#292b30 68%,#22252a); }
  .case-panel--club::before { content:""; position:absolute; width:220px; height:220px; left:-70px; bottom:-90px; border-radius:50%; background:rgba(217,214,253,.12); }
  .case-panel__copy { position:relative; z-index:2; display:flex; flex-direction:column; align-items:flex-start; }
  .case-panel__copy .btn { margin-top:28px; }
  .case-panel__bridge { margin-top:15px !important; color:rgba(255,255,255,.88) !important; font-weight:700; }
  .case-stat strong { font-size:clamp(2rem,3.4vw,3.5rem); }
  
  /* SAGE-family final CTA */
  .final-cta-section { padding:82px 0 96px; background:#fff; }
  .final-cta { min-height:570px; display:grid; grid-template-columns:1.05fr .95fr; align-items:center; gap:40px; padding:62px; border-radius:44px; background:radial-gradient(circle at 88% 10%,rgba(255,255,255,.86),transparent 26%),linear-gradient(140deg,var(--mint),var(--lavender) 54%,var(--peach)); box-shadow:var(--shadow-lg); }
  .final-cta::before { display:none; }
  .final-cta__content { max-width:none; }
  .final-cta h2 { max-width:760px; font-size:clamp(3.1rem,5.6vw,5.9rem); }
  .final-cta p { max-width:660px; }
  .final-cta__benefits { display:flex; flex-wrap:wrap; gap:8px; margin-top:25px; }
  .final-cta__benefits span { display:inline-flex; min-height:34px; align-items:center; padding:0 11px; border:1px solid rgba(32,33,36,.1); border-radius:999px; background:rgba(255,255,255,.55); font-size:.61rem; font-weight:800; }
  .final-visual { min-height:390px; position:relative; }
  .final-visual__orbit { position:absolute; border:1px solid rgba(32,33,36,.16); border-radius:50%; }
  .final-visual__orbit--one { inset:5% 7% 7% 5%; animation:finalOrbit 8s ease-in-out infinite; }
  .final-visual__orbit--two { inset:20% 23%; border-style:dashed; animation:finalOrbit 8s ease-in-out infinite reverse; }
  .final-visual__core { position:absolute; left:50%; top:50%; width:160px; height:160px; display:grid; place-items:center; align-content:center; padding:22px; transform:translate(-50%,-50%); border-radius:50%; background:var(--ink); color:#fff; text-align:center; box-shadow:0 24px 55px rgba(32,33,36,.2); }
  .final-visual__core small { color:rgba(255,255,255,.55); font-size:.55rem; font-weight:800; letter-spacing:.1em; }
  .final-visual__core strong { margin-top:7px; font-size:1rem; line-height:1.15; }
  .final-visual__chip { position:absolute; min-width:84px; display:grid; place-items:center; min-height:42px; padding:0 13px; border-radius:999px; border:1px solid rgba(255,255,255,.8); background:rgba(255,255,255,.82); box-shadow:0 12px 30px rgba(32,33,36,.1); font-size:.65rem; font-weight:800; backdrop-filter:blur(9px); animation:chipFloat 5s ease-in-out infinite; }
  .final-visual__chip--one { left:3%; top:24%; } .final-visual__chip--two { right:6%; top:16%; animation-delay:-1s; } .final-visual__chip--three { right:0; bottom:23%; animation-delay:-2s; } .final-visual__chip--four { left:8%; bottom:15%; animation-delay:-3s; } .final-visual__chip--five { left:42%; top:2%; animation-delay:-4s; }
  @keyframes finalOrbit { 50% { transform:rotate(3deg) scale(1.02); } }
  @keyframes chipFloat { 50% { transform:translateY(-7px); } }
  
  @media (max-width:1180px) {
    .hero { min-height:auto; padding-top:132px; }
    .hero-grid { grid-template-columns:1fr; }
    .enterprise-art { min-height:520px; transform:scale(.9); }
    .systems-grid--five { grid-template-columns:repeat(3,1fr); }
    .system-group--ai { grid-column:span 2; }
  }
  @media (max-width:900px) {
    :root { --navigation-width:min(100% - 32px, 1060px); }
    .site-header.is-nav-connected .modern-nav { border-radius:22px 22px 0 0; }
    .page-nav-wrap.is-stuck .page-nav-shell { border-radius:0 0 22px 22px; }
    .package-explainer { grid-template-columns:1fr; }
    .package-plus { grid-auto-flow:column; justify-content:center; }
    .package-card { min-height:0; }
    .addon-heading { grid-template-columns:1fr; align-items:start; }
    .addon-heading__note { max-width:520px; }
    .systems-grid--five { grid-template-columns:repeat(2,1fr); }
    .system-group--ai { grid-column:auto; }
    .integration-callout { grid-template-columns:52px 1fr; }
    .integration-callout .text-link { grid-column:2; }
    .case-panel--club { grid-template-columns:1fr; }
    .final-cta { grid-template-columns:1fr; padding:48px; }
    .final-visual { min-height:320px; }
  }
  @media (max-width:680px) {
    :root { --navigation-width:calc(100vw - 24px); }
    .hero { min-height:auto; padding:108px 0 42px; }
    .hero-title { font-size:clamp(2.85rem,13.5vw,4rem); }
    .hero-subheadline { margin-top:13px; font-size:1.08rem; }
    .hero-copy { font-size:.9rem; }
    .hero-actions { margin-top:20px; }
    .hero-meta { display:none; }
    .enterprise-art { min-height:440px; margin-top:-12px; transform:scale(.82); }
    .proof-strip { margin-top:-14px; padding-bottom:28px; }
    .proof-panel { grid-template-columns:1fr; }
    .proof-item { border-left:0; border-top:1px solid var(--line); }
    .page-nav-wrap { padding:0; }
    .site-header.is-nav-connected .modern-nav { border-radius:20px 20px 0 0; }
    .page-nav-wrap.is-stuck .page-nav-shell { border-radius:0 0 20px 20px; }
    .problem-closer-card { grid-template-columns:6px 1fr; gap:18px; padding:22px 20px 22px 0; }
    .problem-closer-card__tags { grid-column:2; grid-template-columns:repeat(3,1fr); }
    .problem-closer-card__tags span { min-width:0; }
    .package-card { padding:24px; border-radius:25px; }
    .package-card h3 { font-size:2rem; }
    .package-plus span { width:46px; height:46px; }
    .addon-heading__note { padding:15px 16px; }
    .addon-legend { overflow-x:auto; flex-wrap:nowrap; padding-bottom:4px; }
    .addon-legend > span { flex:0 0 auto; }
    .addon-principle { align-items:flex-start; flex-direction:column; }
    .systems-grid--five { grid-template-columns:1fr; }
    .integration-callout { grid-template-columns:44px 1fr; padding:17px; }
    .integration-callout__mark { width:42px; height:42px; }
    .integration-callout .text-link { grid-column:1/-1; }
    .workflow-track { gap:18px; }
    .workflow-step:not(:last-child)::after { content:"↓"; right:50%; top:auto; bottom:-27px; transform:translateX(50%); }
    .case-panel--club { padding:28px; }
    .final-cta { padding:38px 24px 22px; border-radius:30px; }
    .final-cta h2 { font-size:clamp(2.55rem,11.5vw,4rem); }
    .final-visual { min-height:300px; }
    .final-visual__core { width:140px; height:140px; }
    .final-visual__chip { min-width:72px; min-height:38px; font-size:.58rem; }
  }
  @media (prefers-reduced-motion:reduce) {
    .final-visual__orbit, .final-visual__chip { animation:none !important; }
  }
  
  
  /* ==========================================================
     V5 refinements: typography, interactive add-ons, success proof,
     onboarding journey, final CTA color system
     ========================================================== */
  .better-approach-type {
    max-width: 1080px;
    margin: 96px auto 10px;
    padding: 16px 24px 0;
    text-align: center;
  }
  .better-approach-type .eyebrow { margin-bottom: 22px; }
  .better-approach-type h3 {
    margin: 0 auto;
    display: grid;
    justify-items: center;
    gap: 7px;
    font-size: clamp(2.7rem, 5.6vw, 6.4rem);
    line-height: .98;
    letter-spacing: -.065em;
    font-weight: 800;
  }
  .better-approach-type h3 span { display: block; }
  .better-approach-type__soft { color: var(--muted); font-weight: 500; font-size: .64em; letter-spacing: -.045em; }
  .better-approach-type__accent {
    background: linear-gradient(90deg, #7a65d5 0%, #202124 42%, #527b71 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  .better-approach-type__steps {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    margin-top: 34px;
    padding: 11px 16px;
    border: 1px solid var(--line);
    border-radius: 999px;
    background: rgba(255,255,255,.72);
    color: var(--muted);
    font-size: .65rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: .08em;
  }
  .better-approach-type__steps i { width: 5px; height: 5px; border-radius: 50%; background: var(--lime); box-shadow: 0 0 0 5px rgba(194,255,2,.13); }
  
  /* Add-on cards */
  .addon-card-grid { gap: 18px; }
  .solution-addon-card {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    min-height: 380px;
    border-color: rgba(32,33,36,.07);
    transition: transform .38s var(--ease), box-shadow .38s var(--ease), border-color .38s var(--ease);
  }
  .solution-addon-card::before {
    content: "";
    position: absolute;
    z-index: -1;
    width: 190px;
    height: 190px;
    right: -84px;
    top: -92px;
    border-radius: 50%;
    background: rgba(255,255,255,.48);
    transition: transform .55s var(--ease), opacity .4s ease;
  }
  .solution-addon-card::after {
    content: "";
    position: absolute;
    z-index: -1;
    left: 24px;
    right: 24px;
    bottom: 0;
    height: 3px;
    border-radius: 999px 999px 0 0;
    background: rgba(32,33,36,.14);
    transform: scaleX(.22);
    transform-origin: left;
    transition: transform .4s var(--ease), background .4s ease;
  }
  .solution-addon-card:hover { transform: translateY(-9px) rotate(-.25deg); box-shadow: 0 28px 62px rgba(32,33,36,.105); }
  .solution-addon-card:hover::before { transform: translate(-18px, 18px) scale(1.13); }
  .solution-addon-card:hover::after { transform: scaleX(1); background: var(--lime); }
  .solution-addon-card--lavender { background: linear-gradient(145deg,#f1efff 0%,#faf9ff 62%,#fff 100%); }
  .solution-addon-card--mint { background: linear-gradient(145deg,#eafff9 0%,#f7fffd 65%,#fff 100%); }
  .solution-addon-card--lime { background: linear-gradient(145deg,#f3ffd1 0%,#fbfff0 66%,#fff 100%); }
  .solution-addon-card--peach { background: linear-gradient(145deg,#fff0e7 0%,#fff9f5 65%,#fff 100%); }
  .solution-addon-card--finance { background: linear-gradient(145deg,#fff5df 0%,#f3efff 52%,#fff 100%); }
  .solution-addon-card--ai { color: var(--ink); background: linear-gradient(145deg,#e9f4ff 0%,#eeeaff 48%,#f7fff3 100%); }
  .solution-addon-card--ai p,
  .solution-addon-card--ai .solution-addon-card__best small { color: var(--muted); }
  .solution-addon-card--ai .solution-addon-card__best strong { color: var(--ink); }
  .solution-addon-card--ai .solution-addon-tags span { color: var(--ink); border-color: rgba(32,33,36,.08); background: rgba(255,255,255,.66); }
  .solution-addon-card--ai .solution-addon-card__top > span:last-child { color: var(--muted); }
  .solution-addon-card--custom { background: linear-gradient(135deg,#eeeaff 0%,#e8fff8 48%,#fff1e8 100%); }
  .solution-addon-card__identity { display:flex; align-items:center; gap:10px; }
  .solution-addon-card__icon {
    width: 48px;
    height: 48px;
    display: grid;
    place-items: center;
    flex: 0 0 48px;
    border-radius: 16px;
    color: var(--ink);
    background: rgba(255,255,255,.78);
    border: 1px solid rgba(32,33,36,.07);
    box-shadow: 0 10px 24px rgba(32,33,36,.055);
    transition: transform .38s var(--ease), box-shadow .38s var(--ease), background .38s ease;
  }
  .solution-addon-card__icon svg { width: 24px; height: 24px; fill:none; stroke:currentColor; stroke-width:1.7; stroke-linecap:round; stroke-linejoin:round; }
  .solution-addon-card__number { color: var(--muted); font-size: .62rem; font-weight: 800; }
  .solution-addon-card:hover .solution-addon-card__icon { transform: translateY(-3px) rotate(5deg) scale(1.06); background: var(--lime); box-shadow: 0 13px 28px rgba(32,33,36,.09); }
  .solution-addon-card h3 { margin-top: 32px; }
  .solution-addon-tags span { transition: transform .25s ease, background .25s ease; }
  .solution-addon-card:hover .solution-addon-tags span:nth-child(odd) { transform: translateY(-2px); background: rgba(255,255,255,.9); }
  .solution-addon-card:hover .solution-addon-tags span:nth-child(even) { transform: translateY(2px); }
  
  /* Customer story V5 */
  .customer-story-section--v5 {
    padding-top: 108px;
    padding-bottom: 118px;
    background:
      radial-gradient(circle at 8% 12%, rgba(217,214,253,.28), transparent 25%),
      radial-gradient(circle at 90% 78%, rgba(153,254,237,.25), transparent 27%),
      linear-gradient(180deg,#fff 0%,#fbfbf8 100%);
  }
  .customer-story-v5__heading { max-width: 900px; margin: 0 auto 48px; text-align: center; }
  .customer-story-v5__heading .section-copy { margin-inline: auto; }
  .customer-story-v5 {
    display: grid;
    grid-template-columns: 1.05fr .95fr;
    gap: 18px;
    align-items: stretch;
  }
  .customer-story-v5__quote-card,
  .customer-story-v5__proof {
    min-height: 640px;
    border: 1px solid rgba(32,33,36,.075);
    border-radius: 34px;
    overflow: hidden;
    box-shadow: 0 24px 68px rgba(32,33,36,.07);
  }
  .customer-story-v5__quote-card {
    position: relative;
    padding: 52px;
    background:
      radial-gradient(circle at 88% 7%, rgba(194,255,2,.20), transparent 23%),
      radial-gradient(circle at 8% 94%, rgba(217,214,253,.34), transparent 30%),
      #fff;
  }
  .customer-story-v5__quote-mark { position:absolute; right:36px; top:0; color:rgba(32,33,36,.055); font-size:13rem; line-height:1; font-weight:800; }
  .customer-story-v5__quote-card blockquote { position:relative; z-index:1; margin:0; padding-top:46px; }
  .customer-story-v5__quote-card blockquote p { margin:0 0 22px; color:rgba(32,33,36,.78); font-size:clamp(1.08rem,1.55vw,1.36rem); line-height:1.68; letter-spacing:-.02em; }
  .customer-story-v5__quote-card blockquote p:first-child { font-size:clamp(1.25rem,1.8vw,1.55rem); color:var(--ink); }
  .customer-story-v5__attribution { position:relative; z-index:1; display:flex; align-items:center; gap:14px; margin-top:34px; padding-top:28px; border-top:1px solid var(--line); }
  .customer-story-v5__mark { width:58px; height:58px; flex:0 0 58px; padding:5px; border-radius:18px; background:#fff; box-shadow:0 10px 25px rgba(32,33,36,.07); }
  .customer-story-v5__mark svg { width:100%; height:100%; }
  .customer-story-v5__attribution strong,.customer-story-v5__attribution span { display:block; }
  .customer-story-v5__attribution span { margin-top:4px; color:var(--muted); font-size:.75rem; }
  .customer-story-v5__proof { display:flex; flex-direction:column; padding:36px; background:linear-gradient(145deg,#f2f0ff 0%,#effbf8 52%,#fff7f2 100%); }
  .customer-story-v5__proof-head span { display:block; color:var(--muted); font-size:.62rem; font-weight:800; text-transform:uppercase; letter-spacing:.1em; }
  .customer-story-v5__proof-head strong { display:block; max-width:520px; margin-top:12px; font-size:clamp(1.65rem,2.7vw,2.9rem); line-height:1.04; letter-spacing:-.045em; }
  .customer-story-v5__metric-grid { display:grid; gap:10px; margin-top:30px; }
  .customer-story-v5__metric { padding:22px; border:1px solid rgba(32,33,36,.07); border-radius:22px; background:rgba(255,255,255,.67); transition:transform .3s var(--ease), box-shadow .3s var(--ease); }
  .customer-story-v5__metric:hover { transform:translateX(6px); box-shadow:0 12px 30px rgba(32,33,36,.06); }
  .customer-story-v5__metric strong { display:block; font-size:clamp(2rem,3.7vw,3.8rem); line-height:.95; letter-spacing:-.06em; }
  .customer-story-v5__metric span { display:block; margin-top:8px; color:var(--muted); font-size:.75rem; line-height:1.5; }
  .customer-story-v5__metric--mint { background:rgba(153,254,237,.38); }
  .customer-story-v5__metric--lavender { background:rgba(217,214,253,.56); }
  .customer-story-v5__metric--peach { background:rgba(255,189,155,.35); }
  .customer-story-v5__scope { margin-top:24px; padding-top:22px; border-top:1px solid rgba(32,33,36,.09); }
  .customer-story-v5__scope > span { color:var(--muted); font-size:.61rem; font-weight:800; letter-spacing:.09em; text-transform:uppercase; }
  .customer-story-v5__scope > div { display:flex; flex-wrap:wrap; gap:8px; margin-top:12px; }
  .customer-story-v5__scope b { padding:8px 10px; border:1px solid rgba(32,33,36,.08); border-radius:999px; background:rgba(255,255,255,.62); font-size:.61rem; }
  .customer-story-v5__link { margin-top:auto; padding-top:28px; }
  
  /* Onboarding */
  .onboarding-section {
    background:
      radial-gradient(circle at 92% 8%, rgba(217,214,253,.30), transparent 25%),
      radial-gradient(circle at 7% 85%, rgba(153,254,237,.24), transparent 24%),
      linear-gradient(180deg,#f7f7f4 0%,#fff 100%);
    overflow:hidden;
  }
  .onboarding-heading { max-width: 980px; margin:0 auto; text-align:center; }
  .onboarding-heading .section-copy { margin-inline:auto; max-width:780px; }
  .onboarding-start {
    display:grid;
    grid-template-columns:.34fr .66fr;
    gap:28px;
    align-items:center;
    margin-top:48px;
    padding:26px 30px;
    border:1px solid rgba(32,33,36,.08);
    border-radius:28px;
    background:var(--ink);
    color:#fff;
    box-shadow:0 22px 60px rgba(32,33,36,.10);
  }
  .onboarding-start__label span,.onboarding-start__label strong { display:block; }
  .onboarding-start__label span { color:var(--lime); font-size:.62rem; font-weight:800; text-transform:uppercase; letter-spacing:.1em; }
  .onboarding-start__label strong { margin-top:7px; font-size:1.45rem; letter-spacing:-.03em; }
  .onboarding-start__items { display:grid; grid-template-columns:repeat(3,1fr); gap:8px; }
  .onboarding-start__items > div { min-height:120px; padding:16px; border:1px solid rgba(255,255,255,.10); border-radius:18px; background:rgba(255,255,255,.06); }
  .onboarding-start__items span,.onboarding-start__items strong,.onboarding-start__items small { display:block; }
  .onboarding-start__items span { color:var(--lime); font-size:.58rem; font-weight:800; }
  .onboarding-start__items strong { margin-top:12px; font-size:.82rem; line-height:1.35; }
  .onboarding-start__items small { margin-top:6px; color:rgba(255,255,255,.58); font-size:.66rem; line-height:1.45; }
  .onboarding-journey { display:grid; grid-template-columns:repeat(5,1fr); gap:12px; margin-top:22px; position:relative; }
  .onboarding-journey::before { content:""; position:absolute; left:8%; right:8%; top:31px; height:1px; background:linear-gradient(90deg,transparent,rgba(32,33,36,.15) 10%,rgba(32,33,36,.15) 90%,transparent); }
  .onboarding-step {
    position:relative;
    min-height:430px;
    display:flex;
    flex-direction:column;
    padding:22px;
    border:1px solid rgba(32,33,36,.08);
    border-radius:24px;
    background:rgba(255,255,255,.90);
    box-shadow:0 12px 32px rgba(32,33,36,.04);
    transition:transform .35s var(--ease), box-shadow .35s var(--ease), background .35s ease;
  }
  .onboarding-step:nth-child(2) { background:#f3f0ff; }
  .onboarding-step:nth-child(3) { background:#edfff9; }
  .onboarding-step:nth-child(4) { background:#fff2e9; }
  .onboarding-step:nth-child(5) { background:#f5ffd8; }
  .onboarding-step:hover { transform:translateY(-8px); box-shadow:0 24px 52px rgba(32,33,36,.08); background:#fff; }
  .onboarding-step__index { position:relative; z-index:2; width:44px; height:44px; display:grid; place-items:center; border-radius:14px; background:var(--ink); color:#fff; font-size:.62rem; font-weight:800; transition:transform .35s var(--ease), background .35s ease; }
  .onboarding-step:hover .onboarding-step__index { transform:rotate(7deg) scale(1.08); background:var(--lime); color:var(--ink); }
  .onboarding-step__icon { width:50px; height:50px; display:grid; place-items:center; margin-top:34px; border-radius:16px; background:rgba(255,255,255,.72); border:1px solid rgba(32,33,36,.07); }
  .onboarding-step__icon svg { width:24px; height:24px; fill:none; stroke:currentColor; stroke-width:1.7; stroke-linecap:round; stroke-linejoin:round; }
  .onboarding-step > small { margin-top:20px; color:var(--muted); font-size:.58rem; font-weight:800; text-transform:uppercase; letter-spacing:.1em; }
  .onboarding-step h3 { margin:8px 0 0; font-size:1.35rem; line-height:1.08; letter-spacing:-.04em; }
  .onboarding-step p { margin:14px 0 0; color:var(--muted); font-size:.75rem; line-height:1.6; }
  .onboarding-step__role { margin-top:auto; padding-top:18px; border-top:1px solid rgba(32,33,36,.08); }
  .onboarding-step__role b,.onboarding-step__role span { display:block; }
  .onboarding-step__role b { font-size:.58rem; text-transform:uppercase; letter-spacing:.08em; }
  .onboarding-step__role span { margin-top:6px; color:var(--muted); font-size:.68rem; line-height:1.45; }
  .automation-outcomes { margin-top:24px; padding:34px; border:1px solid rgba(32,33,36,.08); border-radius:30px; background:#fff; box-shadow:0 18px 50px rgba(32,33,36,.055); }
  .automation-outcomes__intro { display:flex; align-items:end; justify-content:space-between; gap:30px; padding-bottom:24px; border-bottom:1px solid var(--line); }
  .automation-outcomes__intro span { color:var(--muted); font-size:.62rem; font-weight:800; text-transform:uppercase; letter-spacing:.1em; }
  .automation-outcomes__intro strong { max-width:650px; font-size:1.65rem; line-height:1.15; letter-spacing:-.035em; text-align:right; }
  .automation-outcomes__grid { display:grid; grid-template-columns:repeat(4,1fr); gap:10px; margin-top:20px; }
  .automation-outcome { min-height:205px; padding:20px; border-radius:20px; border:1px solid rgba(32,33,36,.065); }
  .automation-outcome--lime { background:rgba(194,255,2,.25); }
  .automation-outcome--mint { background:rgba(153,254,237,.30); }
  .automation-outcome--peach { background:rgba(255,189,155,.28); }
  .automation-outcome--lavender { background:rgba(217,214,253,.42); }
  .automation-outcome__icon { width:42px; height:42px; display:grid; place-items:center; border-radius:14px; background:rgba(255,255,255,.70); font-size:1rem; font-weight:800; }
  .automation-outcome strong { display:block; margin-top:24px; font-size:1rem; }
  .automation-outcome p { margin:9px 0 0; color:var(--muted); font-size:.72rem; line-height:1.5; }
  
  /* Final CTA card color refinement */
  .final-cta { background:radial-gradient(circle at 88% 10%,rgba(255,255,255,.92),transparent 24%),linear-gradient(140deg,#f3f0ff 0%,#ebfff9 54%,#fff3ea 100%); }
  .final-cta__benefits span:nth-child(1) { background:rgba(217,214,253,.72); border-color:transparent; }
  .final-cta__benefits span:nth-child(2) { background:rgba(153,254,237,.62); border-color:transparent; }
  .final-cta__benefits span:nth-child(3) { background:rgba(255,189,155,.48); border-color:transparent; }
  .final-visual__chip--one { background:#f0edff; }
  .final-visual__chip--two { background:#eeffd0; }
  .final-visual__chip--three { background:#fff0e7; }
  .final-visual__chip--four { background:#e9fff9; }
  .final-visual__chip--five { background:#f2efff; }
  
  @media (max-width: 1120px) {
    .customer-story-v5 { grid-template-columns:1fr; }
    .customer-story-v5__quote-card,.customer-story-v5__proof { min-height:auto; }
    .onboarding-start { grid-template-columns:1fr; }
    .onboarding-journey { grid-template-columns:repeat(2,1fr); }
    .onboarding-journey::before { display:none; }
    .onboarding-step:last-child { grid-column:1 / -1; min-height:330px; }
    .automation-outcomes__grid { grid-template-columns:repeat(2,1fr); }
  }
  @media (max-width: 760px) {
    .better-approach-type { margin-top:70px; padding-inline:0; }
    .better-approach-type h3 { font-size:clamp(2.5rem,12vw,4rem); }
    .better-approach-type__soft { font-size:.58em; }
    .better-approach-type__steps { gap:8px; padding:10px 12px; font-size:.56rem; }
    .solution-addon-card__identity { align-items:flex-start; flex-direction:column; }
    .solution-addon-card { min-height:350px; }
    .customer-story-section--v5 { padding-top:88px; padding-bottom:92px; }
    .customer-story-v5__heading { margin-bottom:32px; }
    .customer-story-v5__quote-card,.customer-story-v5__proof { padding:30px 24px; border-radius:28px; }
    .customer-story-v5__quote-mark { right:18px; font-size:9rem; }
    .onboarding-start { padding:24px 20px; border-radius:24px; }
    .onboarding-start__items { grid-template-columns:1fr; }
    .onboarding-start__items > div { min-height:auto; }
    .onboarding-journey { grid-template-columns:1fr; }
    .onboarding-step,.onboarding-step:last-child { min-height:340px; grid-column:auto; }
    .automation-outcomes { padding:24px 18px; }
    .automation-outcomes__intro { align-items:flex-start; flex-direction:column; }
    .automation-outcomes__intro strong { text-align:left; font-size:1.35rem; }
    .automation-outcomes__grid { grid-template-columns:1fr; }
    .automation-outcome { min-height:180px; }
  }
  @media (prefers-reduced-motion: reduce) {
    .solution-addon-card,.solution-addon-card::before,.solution-addon-card::after,.solution-addon-card__icon,.solution-addon-tags span,.customer-story-v5__metric,.onboarding-step,.onboarding-step__index { transition:none !important; transform:none !important; }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V7 hero dashboard + trust bar
     ========================================================== */
  .enterprise-art--dashboard {
    min-height: 560px;
    transform: none;
    display: grid;
    place-items: center;
    padding: 8px 0 0;
  }
  .enterprise-art--dashboard::before {
    content: "";
    position: absolute;
    inset: 8% 7% 10% 7%;
    border-radius: 42px;
    background:
      radial-gradient(circle at 18% 18%, rgba(194,255,2,.18), transparent 28%),
      radial-gradient(circle at 85% 14%, rgba(217,214,253,.42), transparent 28%),
      radial-gradient(circle at 82% 84%, rgba(153,254,237,.38), transparent 30%),
      radial-gradient(circle at 16% 84%, rgba(255,189,155,.24), transparent 24%);
    filter: blur(24px);
    pointer-events: none;
  }
  .dashboard-hover-scene {
    --dashboard-w: min(100%, 690px);
    position: relative;
    width: var(--dashboard-w);
    aspect-ratio: 1600 / 1050;
    cursor: grab;
    isolation: isolate;
  }
  .dashboard-hover-scene:where(:hover, :focus-visible) { cursor: grabbing; outline: none; }
  .dashboard-ghost {
    position: absolute;
    inset: 0;
    border-radius: 30px;
    border: 1px solid rgba(32,33,36,.08);
    background: linear-gradient(180deg, rgba(255,255,255,.86), rgba(251,251,248,.9));
    box-shadow: 0 38px 70px rgba(32,33,36,.10), inset 0 1px 0 rgba(255,255,255,.55);
    transition: transform .6s cubic-bezier(.2,.8,.2,1), box-shadow .6s ease;
  }
  .dashboard-hover-scene:where(:hover,:focus-visible) .dashboard-ghost {
    transform: scale(.986);
    box-shadow: 0 24px 52px rgba(32,33,36,.10), inset 0 1px 0 rgba(255,255,255,.42);
  }
  .dashboard-fragment {
    position: absolute;
    background-image: url('swishtag-enterprise-ceo-dashboard.png');
    background-repeat: no-repeat;
    background-size: 100% 100%;
    border-radius: 22px;
    box-shadow: 0 12px 28px rgba(32,33,36,.08), 0 2px 8px rgba(32,33,36,.04);
    transition: transform .65s cubic-bezier(.2,.8,.2,1), box-shadow .65s ease, filter .45s ease;
    will-change: transform;
  }
  .dashboard-fragment::after {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: inherit;
    box-shadow: inset 0 0 0 1px rgba(255,255,255,.22);
    pointer-events: none;
  }
  .dashboard-hover-scene:where(:hover,:focus-visible) .dashboard-fragment {
    box-shadow: 0 22px 46px rgba(32,33,36,.12), 0 3px 12px rgba(32,33,36,.06);
    filter: saturate(1.02);
  }
  .frag--sidebar { left: 4%; top: 5.7%; width: 15.7%; height: 88.6%; background-position: -4% -5.7%; }
  .frag--header { left: 21.9%; top: 7.4%; width: 57.3%; height: 7.9%; background-position: -21.9% -7.4%; }
  .frag--kpi-1 { left: 21.9%; top: 16.7%; width: 17.4%; height: 14.8%; background-position: -21.9% -16.7%; }
  .frag--kpi-2 { left: 39.8%; top: 16.7%; width: 17.4%; height: 14.8%; background-position: -39.8% -16.7%; }
  .frag--kpi-3 { left: 57.6%; top: 16.7%; width: 17.4%; height: 14.8%; background-position: -57.6% -16.7%; }
  .frag--kpi-4 { left: 75.4%; top: 16.7%; width: 17.4%; height: 14.8%; background-position: -75.4% -16.7%; }
  .frag--sales-chart { left: 21.9%; top: 33.3%; width: 47.2%; height: 31.4%; background-position: -21.9% -33.3%; }
  .frag--top-stores { left: 70.3%; top: 33.3%; width: 25.6%; height: 31.4%; background-position: -70.3% -33.3%; }
  .frag--order-health { left: 21.9%; top: 67.1%; width: 26.9%; height: 24.8%; background-position: -21.9% -67.1%; }
  .frag--insights { left: 50%; top: 67.1%; width: 19.1%; height: 24.8%; background-position: -50% -67.1%; }
  .frag--business-pulse { left: 70.3%; top: 67.1%; width: 25.6%; height: 24.8%; background-position: -70.3% -67.1%; }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--sidebar { transform: translate(-22px, 8px) rotate(-2.2deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--header { transform: translate(0, -18px) rotate(-.6deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--kpi-1 { transform: translate(-16px, -14px) rotate(-1.9deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--kpi-2 { transform: translate(-6px, -22px) rotate(-1.1deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--kpi-3 { transform: translate(8px, -22px) rotate(1.1deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--kpi-4 { transform: translate(18px, -14px) rotate(2deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--sales-chart { transform: translate(-24px, -4px) rotate(-1.15deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--top-stores { transform: translate(24px, -6px) rotate(1.25deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--order-health { transform: translate(-20px, 20px) rotate(-1.45deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--insights { transform: translate(0, 28px) rotate(.95deg); }
  .dashboard-hover-scene:where(:hover,:focus-visible) .frag--business-pulse { transform: translate(24px, 18px) rotate(1.35deg); }
  .dashboard-fallback-image { display: none; width: 100%; height: auto; border-radius: 30px; box-shadow: 0 24px 52px rgba(32,33,36,.12); }
  
  .enterprise-trust-bar-section {
    padding: 0 0 30px;
    background: linear-gradient(180deg, rgba(255,255,255,0) 0%, #fff 100%);
  }
  .hero-trust-card-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 16px;
  }
  .hero-trust-card {
    display: flex;
    align-items: center;
    gap: 16px;
    min-height: 116px;
    padding: 18px 20px;
    border: 1px solid rgba(32,33,36,.08);
    border-radius: 22px;
    background: linear-gradient(180deg, #fff 0%, #fbfbf8 100%);
    box-shadow: 0 16px 34px rgba(32,33,36,.05);
  }
  .hero-trust-card__logo {
    flex: 0 0 auto;
    display: grid;
    place-items: center;
    min-width: 82px;
  }
  .hero-trust-card__copy { display: grid; gap: 5px; }
  .hero-trust-card__copy small {
    color: var(--muted);
    font-size: .72rem;
    font-weight: 800;
    letter-spacing: .14em;
  }
  .hero-trust-card__copy strong {
    color: var(--ink);
    font-size: 1.02rem;
    line-height: 1.32;
  }
  .hero-trust-card__logo--delivery {
    width: 58px;
    height: 58px;
    border-radius: 18px;
    background: linear-gradient(180deg, #f4ffe2 0%, #dfff94 100%);
    color: var(--ink);
    font-size: 1.8rem;
    font-weight: 900;
  }
  .hero-trustpilot-wordmark {
    display: flex;
    align-items: center;
    gap: 8px;
    color: var(--ink);
    font-weight: 800;
  }
  .hero-trustpilot-wordmark svg { width: 22px; height: 22px; }
  .hero-trustpilot-stars {
    display: flex;
    gap: 2px;
    margin-top: 8px;
    color: #00b67a;
    font-size: 1rem;
    line-height: 1;
  }
  .hero-trustpilot-stars .partial { opacity: .55; }
  
  @media (max-width: 1180px) {
    .hero-trust-card-grid { grid-template-columns: repeat(2, minmax(0,1fr)); }
  }
  @media (max-width: 980px) {
    .enterprise-art--dashboard { min-height: auto; }
    .dashboard-hover-scene {
      width: min(100%, 720px);
      cursor: default;
    }
    .dashboard-fallback-image { display: block; }
    .dashboard-ghost,
    .dashboard-fragment { display: none; }
  }
  @media (max-width: 680px) {
    .enterprise-trust-bar-section { padding: 6px 0 22px; }
    .hero-trust-card-grid { grid-template-columns: 1fr; gap: 12px; }
    .hero-trust-card { min-height: 102px; padding: 16px 18px; border-radius: 18px; }
  }
  @media (prefers-reduced-motion: reduce) {
    .dashboard-ghost,
    .dashboard-fragment { transition: none !important; transform: none !important; }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V8 refinements
     ========================================================== */
  .solution-addon-card--ai .solution-pill--dark {
    color: var(--ink);
    background: rgba(255,255,255,.92);
    border-color: rgba(255,255,255,.56);
  }
  
  .better-approach-type__interactive {
    background:
      radial-gradient(circle 240px at var(--gradient-x) var(--gradient-y),
        var(--lime) 0%,
        var(--mint) 24%,
        var(--lavender) 48%,
        var(--peach) 72%,
        var(--ink) 100%);
  }
  .better-approach-type__interactive:hover {
    filter: saturate(1.12) brightness(1.01);
  }
  
  .enterprise-art--dashboard {
    position: relative;
    min-height: 590px;
    display: grid;
    place-items: center;
    padding-top: 6px;
    overflow: visible;
  }
  .enterprise-art--dashboard::before {
    inset: 16% 9% 12% 9%;
    border-radius: 54px;
    background:
      radial-gradient(circle at 14% 22%, rgba(194,255,2,.16), transparent 24%),
      radial-gradient(circle at 84% 20%, rgba(217,214,253,.45), transparent 26%),
      radial-gradient(circle at 84% 84%, rgba(153,254,237,.38), transparent 28%),
      radial-gradient(circle at 16% 84%, rgba(255,189,155,.25), transparent 22%);
  }
  .dashboard-browser-mockup {
    position: relative;
    width: min(100%, 620px);
    margin: 0 auto;
    padding: 16px 16px 20px;
    border-radius: 30px;
    background: linear-gradient(180deg, rgba(255,255,255,.96), rgba(250,250,248,.98));
    border: 1px solid rgba(32,33,36,.08);
    box-shadow: 0 34px 72px rgba(32,33,36,.12), inset 0 1px 0 rgba(255,255,255,.75);
    z-index: 2;
  }
  .dashboard-browser-mockup__bar {
    display:flex;
    align-items:center;
    gap:8px;
    height: 42px;
    padding: 0 14px;
    border-radius: 18px 18px 12px 12px;
    background: #f4f4f1;
    border: 1px solid rgba(32,33,36,.06);
  }
  .traffic {
    width: 10px;
    height: 10px;
    border-radius: 999px;
    background: #d8d8d3;
  }
  .traffic--red { background:#ffbd9b; }
  .traffic--yellow { background:#d9d6fd; }
  .traffic--green { background:#c2ff02; }
  .dashboard-browser-mockup__url {
    margin-left: 10px;
    padding: 8px 16px;
    border-radius: 999px;
    background: #fff;
    border: 1px solid rgba(32,33,36,.06);
    color: var(--muted);
    font-size: .78rem;
    font-weight: 700;
    letter-spacing: -.02em;
  }
  .dashboard-browser-mockup__viewport {
    position: relative;
    margin-top: 12px;
    border-radius: 24px;
    overflow: hidden;
    background: linear-gradient(180deg, #fbfbf8 0%, #f5f6f2 100%);
    box-shadow: inset 0 0 0 1px rgba(32,33,36,.05);
  }
  .dashboard-browser-mockup__viewport .dashboard-hover-scene {
    width: 100%;
    aspect-ratio: 1600 / 1050;
    isolation: isolate;
  }
  .dashboard-browser-mockup__viewport .dashboard-ghost { border-radius: 0; box-shadow: none; border: 0; background: transparent; }
  .dashboard-browser-mockup__viewport .dashboard-fallback-image { border-radius: 0; box-shadow: none; }
  .hero-floating-pill {
    position: absolute;
    z-index: 3;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 42px;
    padding: 0 16px;
    border-radius: 999px;
    border: 1px solid rgba(32,33,36,.08);
    background: rgba(255,255,255,.9);
    box-shadow: 0 14px 28px rgba(32,33,36,.07);
    color: var(--ink);
    font-size: .82rem;
    font-weight: 800;
    letter-spacing: -.02em;
    backdrop-filter: blur(10px);
    animation: heroFloat 8s ease-in-out infinite;
  }
  .hero-floating-pill--top { top: 3%; left: 50%; transform: translateX(-50%); background: rgba(153,254,237,.92); }
  .hero-floating-pill--right-top { top: 18%; right: -1%; background: rgba(217,214,253,.94); animation-delay: -1.3s; }
  .hero-floating-pill--left-mid { top: 33%; left: -2%; background: rgba(194,255,2,.92); animation-delay: -2.4s; }
  .hero-floating-pill--right-mid { top: 48%; right: -3%; background: rgba(255,189,155,.94); animation-delay: -3.1s; }
  .hero-floating-pill--left-bottom { bottom: 18%; left: -3%; background: rgba(255,255,255,.95); animation-delay: -4.5s; }
  .hero-floating-pill--bottom { bottom: 4%; left: 50%; transform: translateX(-50%); background: rgba(32,33,36,.92); color:#fff; animation-delay: -5.2s; }
  @keyframes heroFloat {
    0%,100% { transform: translate3d(0,0,0); }
    50% { transform: translate3d(0,-8px,0); }
  }
  .hero-floating-pill--top, .hero-floating-pill--bottom { animation-name: heroFloatCenter; }
  @keyframes heroFloatCenter {
    0%,100% { transform: translateX(-50%) translateY(0); }
    50% { transform: translateX(-50%) translateY(-8px); }
  }
  
  .enterprise-trust-bar-section {
    margin-top: -8px;
    padding: 0 0 22px;
    background: linear-gradient(180deg, rgba(251,251,248,0) 0%, #fff 100%);
  }
  .hero-trust-card-grid--enterprise {
    gap: 0;
    padding: 10px 12px;
    border: 1px solid rgba(32,33,36,.08);
    border-radius: 28px;
    background: rgba(255,255,255,.94);
    box-shadow: 0 24px 48px rgba(32,33,36,.06);
    overflow: hidden;
  }
  .hero-trust-card-grid--enterprise .hero-trust-card {
    min-height: 96px;
    padding: 16px 18px;
    border-radius: 0;
    border: 0;
    border-right: 1px solid rgba(32,33,36,.08);
    background: transparent;
    box-shadow: none;
  }
  .hero-trust-card-grid--enterprise .hero-trust-card:last-child { border-right: 0; }
  .hero-trust-card-grid--enterprise .hero-trust-card__copy strong { font-size: .98rem; }
  .hero-trust-card-grid--enterprise .hero-trust-card__copy small { font-size: .68rem; }
  
  @media (max-width: 1180px) {
    .hero-floating-pill--right-top { right: 2%; }
    .hero-floating-pill--right-mid { right: 0; }
    .hero-floating-pill--left-mid, .hero-floating-pill--left-bottom { left: 0; }
    .hero-trust-card-grid--enterprise .hero-trust-card:nth-child(2) { border-right: 0; }
    .hero-trust-card-grid--enterprise .hero-trust-card:nth-child(-n+2) { border-bottom: 1px solid rgba(32,33,36,.08); }
  }
  @media (max-width: 980px) {
    .enterprise-art--dashboard { min-height: auto; padding-top: 16px; }
    .dashboard-browser-mockup { width: min(100%, 700px); }
    .hero-floating-pill {
      position: static;
      animation: none;
      font-size: .78rem;
      min-height: 38px;
    }
    .enterprise-art--dashboard {
      display: flex;
      flex-direction: column;
      gap: 10px;
      align-items: center;
    }
    .hero-floating-pill--top, .hero-floating-pill--right-top, .hero-floating-pill--left-mid,
    .hero-floating-pill--right-mid, .hero-floating-pill--left-bottom, .hero-floating-pill--bottom {
      transform: none !important;
    }
    .hero-trust-card-grid--enterprise { gap: 0; }
  }
  @media (max-width: 680px) {
    .hero-floating-pill { width: auto; max-width: 100%; }
    .dashboard-browser-mockup { padding: 12px 12px 16px; border-radius: 24px; }
    .dashboard-browser-mockup__bar { height: 38px; padding: 0 10px; }
    .dashboard-browser-mockup__url { font-size: .68rem; padding: 7px 12px; }
    .hero-trust-card-grid--enterprise {
      display: grid;
      grid-template-columns: 1fr;
      padding: 0;
      border: none;
      background: transparent;
      box-shadow: none;
      gap: 12px;
    }
    .hero-trust-card-grid--enterprise .hero-trust-card {
      min-height: 102px;
      border: 1px solid rgba(32,33,36,.08);
      border-right: 0;
      border-radius: 18px;
      background: #fff;
      box-shadow: 0 14px 28px rgba(32,33,36,.05);
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-floating-pill { animation: none !important; }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V9 hero simplification
     ========================================================== */
  .enterprise-art--dashboard {
    min-height: 520px;
    padding-top: 0;
  }
  .dashboard-browser-mockup--simple {
    position: relative;
    width: min(100%, 560px);
    margin: 0 auto;
    padding: 12px 12px 14px;
    border-radius: 26px;
    background: linear-gradient(180deg, rgba(255,255,255,.98), rgba(249,249,246,.98));
    border: 1px solid rgba(32,33,36,.08);
    box-shadow: 0 30px 60px rgba(32,33,36,.12), inset 0 1px 0 rgba(255,255,255,.85);
    z-index: 2;
  }
  .dashboard-browser-mockup__bar--minimal {
    justify-content: flex-start;
    height: 30px;
    padding: 0 10px;
    border-radius: 14px 14px 10px 10px;
    background: #f2f2ee;
  }
  .dashboard-browser-mockup__viewport--simple {
    margin-top: 10px;
    border-radius: 18px;
    overflow: hidden;
    background: #f7f8f5;
    box-shadow: inset 0 0 0 1px rgba(32,33,36,.05);
  }
  .simple-dashboard-image {
    display: block;
    width: 100%;
    height: auto;
    border-radius: 0;
  }
  .dashboard-browser-mockup__url,
  .dashboard-hover-scene,
  .dashboard-ghost,
  .dashboard-fragment,
  .dashboard-fallback-image { display: none !important; }
  .hero-floating-pill--top { top: 2%; }
  .hero-floating-pill--right-top { top: 16%; right: -3%; }
  .hero-floating-pill--left-mid { top: 35%; left: -4%; }
  .hero-floating-pill--right-mid { top: 51%; right: -4%; }
  .hero-floating-pill--left-bottom { bottom: 16%; left: -4%; }
  .hero-floating-pill--bottom { bottom: 1%; }
  @media (max-width: 1180px) {
    .dashboard-browser-mockup--simple { width: min(100%, 540px); }
    .hero-floating-pill--right-top { right: 0; }
    .hero-floating-pill--right-mid { right: 0; }
    .hero-floating-pill--left-mid,
    .hero-floating-pill--left-bottom { left: 0; }
  }
  @media (max-width: 980px) {
    .enterprise-art--dashboard {
      min-height: auto;
      gap: 10px;
    }
    .dashboard-browser-mockup--simple { width: min(100%, 640px); }
  }
  @media (max-width: 680px) {
    .enterprise-art--dashboard { padding-top: 8px; }
    .dashboard-browser-mockup--simple {
      width: 100%;
      padding: 10px 10px 12px;
      border-radius: 22px;
    }
    .dashboard-browser-mockup__bar--minimal { height: 28px; }
    .dashboard-browser-mockup__viewport--simple { border-radius: 16px; }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V10 hero refinements
     ========================================================== */
  .enterprise-art--dashboard {
    min-height: 560px;
    padding-top: 4px;
  }
  .dashboard-browser-mockup--simple {
    width: min(100%, 490px);
    padding: 11px 11px 13px;
    border-radius: 24px;
  }
  .dashboard-browser-mockup__bar--minimal {
    height: 28px;
    padding: 0 10px;
  }
  .dashboard-browser-mockup__viewport--simple {
    margin-top: 9px;
    border-radius: 16px;
  }
  .enterprise-art--dashboard .enterprise-node {
    z-index: 4;
  }
  .enterprise-art--dashboard .enterprise-node--stores { left: -1%; top: 19%; }
  .enterprise-art--dashboard .enterprise-node--punchout { right: -1%; top: 16%; }
  .enterprise-art--dashboard .enterprise-node--orders { left: 0; bottom: 17%; }
  .enterprise-art--dashboard .enterprise-node--accounting { right: -2%; bottom: 17%; }
  .enterprise-art--dashboard .enterprise-node--suppliers { top: 0; }
  .enterprise-art--dashboard .enterprise-node--approvals { bottom: 1%; }
  .enterprise-art--dashboard .flow-line {
    opacity: .55;
    z-index: 1;
    background: repeating-linear-gradient(90deg, rgba(32,33,36,.28) 0 5px, transparent 5px 10px);
  }
  .enterprise-art--dashboard .flow-line--1 { width: 148px; left: 18.5%; top: 29%; transform: rotate(20deg); }
  .enterprise-art--dashboard .flow-line--2 { width: 150px; right: 19%; left: auto; top: 29%; transform: rotate(-20deg); }
  .enterprise-art--dashboard .flow-line--3 { width: 150px; left: 18%; top: 67%; transform: rotate(-20deg); }
  .enterprise-art--dashboard .flow-line--4 { width: 150px; right: 19%; left: auto; top: 67%; transform: rotate(20deg); }
  .enterprise-art--dashboard .flow-line--5 { width: 78px; left: 50%; top: 13.5%; transform: translateX(-50%) rotate(90deg); }
  .enterprise-art--dashboard .flow-line--6 { width: 78px; left: 50%; top: 71.8%; transform: translateX(-50%) rotate(90deg); }
  .enterprise-art--dashboard::before {
    inset: 14% 11% 13% 11%;
  }
  @media (max-width: 1180px) {
    .dashboard-browser-mockup--simple { width: min(100%, 470px); }
    .enterprise-art--dashboard .enterprise-node { min-width: 166px; }
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--3 { left: 19%; }
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--4 { right: 19%; }
  }
  @media (max-width: 980px) {
    .enterprise-art--dashboard {
      min-height: auto;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
    }
    .dashboard-browser-mockup--simple { width: min(100%, 560px); order: 1; }
    .enterprise-art--dashboard .flow-line { display:none; }
    .enterprise-art--dashboard .enterprise-node {
      position: static;
      width: 100%;
      max-width: 320px;
      animation: nodeFloat 6.4s ease-in-out infinite;
    }
    .enterprise-art--dashboard .enterprise-node--suppliers,
    .enterprise-art--dashboard .enterprise-node--stores,
    .enterprise-art--dashboard .enterprise-node--punchout,
    .enterprise-art--dashboard .enterprise-node--orders,
    .enterprise-art--dashboard .enterprise-node--accounting,
    .enterprise-art--dashboard .enterprise-node--approvals {
      left: auto; right: auto; top: auto; bottom: auto; transform: none;
    }
  }
  @media (max-width: 680px) {
    .dashboard-browser-mockup--simple {
      width: 100%;
      padding: 10px 10px 12px;
      border-radius: 22px;
    }
  }
  
  /* ==========================================================
     V11: smaller hero hub + fully floating V6 cards + readable
     Better Approach with branded hover-only gradient
     ========================================================== */
  
  /* Hero: small dashboard hub in the middle, all V6-style cards around it */
  .enterprise-art--dashboard {
    min-height: 570px;
    position: relative;
    display: block;
    overflow: visible;
    padding: 0;
  }
  .enterprise-art--dashboard::before {
    content: "";
    position: absolute;
    left: 50%;
    top: 50%;
    width: 330px;
    height: 330px;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    background:
      radial-gradient(circle at 50% 50%, rgba(255,255,255,.94) 0 30%, rgba(153,254,237,.23) 46%, rgba(217,214,253,.20) 62%, rgba(255,189,155,.11) 74%, transparent 76%);
    filter: blur(2px);
    pointer-events: none;
  }
  .enterprise-art--dashboard::after {
    content: "";
    position: absolute;
    left: 50%;
    top: 50%;
    width: 390px;
    height: 390px;
    transform: translate(-50%, -50%);
    border: 1px dashed rgba(32,33,36,.11);
    border-radius: 50%;
    pointer-events: none;
  }
  
  .dashboard-browser-mockup--simple {
    position: absolute;
    z-index: 5;
    left: 50%;
    top: 50%;
    width: 245px; /* roughly 50% of V10 */
    margin: 0;
    padding: 7px 7px 9px;
    border-radius: 17px;
    transform: translate(-50%, -50%);
    background: linear-gradient(180deg, rgba(255,255,255,.99), rgba(249,249,246,.99));
    border: 1px solid rgba(32,33,36,.09);
    box-shadow: 0 26px 52px rgba(32,33,36,.13), inset 0 1px 0 rgba(255,255,255,.88);
  }
  .dashboard-browser-mockup__bar--minimal {
    height: 19px;
    padding: 0 7px;
    gap: 4px;
    border-radius: 9px 9px 6px 6px;
  }
  .dashboard-browser-mockup__bar--minimal .traffic {
    width: 6px;
    height: 6px;
  }
  .dashboard-browser-mockup__viewport--simple {
    margin-top: 6px;
    border-radius: 11px;
  }
  .simple-dashboard-image { width: 100%; height: auto; }
  
  /* Floating V6-style cards */
  .enterprise-art--dashboard .enterprise-node {
    z-index: 7;
    min-width: 168px;
    max-width: 190px;
    grid-template-columns: 40px 1fr;
    gap: 10px;
    padding: 11px 12px;
    border-radius: 17px;
    background: rgba(255,255,255,.91);
    border: 1px solid rgba(255,255,255,.95);
    box-shadow: 0 18px 42px rgba(32,33,36,.11);
    -webkit-backdrop-filter: blur(12px);
    backdrop-filter: blur(12px);
    animation: nodeFloat 6.4s ease-in-out infinite;
  }
  .enterprise-art--dashboard .enterprise-node__icon {
    width: 40px;
    height: 40px;
    border-radius: 13px;
  }
  .enterprise-art--dashboard .enterprise-node__icon svg { width: 20px; }
  .enterprise-art--dashboard .enterprise-node__copy strong {
    font-size: .70rem;
    line-height: 1.2;
  }
  .enterprise-art--dashboard .enterprise-node__copy small {
    margin-top: 3px;
    font-size: .54rem;
    line-height: 1.28;
  }
  
  .enterprise-art--dashboard .enterprise-node--suppliers {
    left: 50%; top: 1%; bottom: auto; right: auto;
    transform: translateX(-50%);
    animation: nodeFloatCenter 6.8s ease-in-out infinite -4.1s;
  }
  .enterprise-art--dashboard .enterprise-node--stores {
    left: -1%; top: 23%; right: auto; bottom: auto;
  }
  .enterprise-art--dashboard .enterprise-node--punchout {
    right: -1%; top: 20%; left: auto; bottom: auto;
  }
  .enterprise-art--dashboard .enterprise-node--orders {
    left: 0; bottom: 19%; right: auto; top: auto;
  }
  .enterprise-art--dashboard .enterprise-node--accounting {
    right: -1%; bottom: 18%; left: auto; top: auto;
  }
  .enterprise-art--dashboard .enterprise-node--approvals {
    left: 50%; bottom: 0; top: auto; right: auto;
    transform: translateX(-50%);
    animation: nodeFloatCenter 6.7s ease-in-out infinite -5s;
  }
  @keyframes nodeFloatCenter {
    0%,100% { transform: translateX(-50%) translateY(0); }
    50% { transform: translateX(-50%) translateY(-9px); }
  }
  
  /* Dotted connectors from each card toward the smaller center hub */
  .enterprise-art--dashboard .flow-line {
    z-index: 2;
    height: 2px;
    opacity: .52;
    transform-origin: left center;
    background: repeating-linear-gradient(90deg, rgba(32,33,36,.31) 0 4px, transparent 4px 9px);
  }
  .enterprise-art--dashboard .flow-line::after {
    width: 6px;
    height: 6px;
    top: -2px;
  }
  .enterprise-art--dashboard .flow-line--1 {
    width: 212px;
    left: 19%;
    top: 34%;
    transform: rotate(17deg);
  }
  .enterprise-art--dashboard .flow-line--2 {
    width: 212px;
    left: 50.5%;
    top: 34%;
    transform: rotate(-17deg);
  }
  .enterprise-art--dashboard .flow-line--3 {
    width: 212px;
    left: 19%;
    top: 65%;
    transform: rotate(-17deg);
  }
  .enterprise-art--dashboard .flow-line--4 {
    width: 212px;
    left: 50.5%;
    top: 65%;
    transform: rotate(17deg);
  }
  .enterprise-art--dashboard .flow-line--5 {
    width: 135px;
    left: 50%;
    top: 19%;
    transform: rotate(90deg);
  }
  .enterprise-art--dashboard .flow-line--6 {
    width: 135px;
    left: 50%;
    top: 58%;
    transform: rotate(90deg);
  }
  
  /* Better approach: restore V6 readability, hover-only branded color */
  .better-approach-type {
    max-width: 1080px;
    margin: 96px auto 10px;
    padding: 16px 24px 0;
    text-align: center;
  }
  .better-approach-type .eyebrow { margin-bottom: 22px; }
  .better-approach-type__interactive {
    --gradient-x: 50%;
    --gradient-y: 50%;
    margin: 0 auto;
    display: grid;
    justify-items: center;
    gap: 7px;
    font-size: clamp(2.7rem, 5.6vw, 6.4rem);
    line-height: .98;
    letter-spacing: -.065em;
    font-weight: 800;
    color: var(--ink) !important;
    background: none !important;
    -webkit-background-clip: initial !important;
    background-clip: initial !important;
    filter: none;
    transform: none;
    transition: transform .22s ease;
  }
  .better-approach-type__interactive span {
    display: block;
    color: var(--ink) !important;
    background: none !important;
    -webkit-background-clip: initial !important;
    background-clip: initial !important;
    transition: color .18s ease, transform .22s ease;
  }
  .better-approach-type__interactive .better-approach-type__soft {
    color: var(--muted) !important;
    font-weight: 600;
    font-size: .58em;
    letter-spacing: -.038em;
  }
  .better-approach-type__interactive .better-approach-type__accent {
    color: var(--ink) !important;
  }
  .better-approach-type__interactive:hover {
    filter: none;
    transform: translateY(-2px);
  }
  .better-approach-type__interactive:hover span,
  .better-approach-type__interactive:hover .better-approach-type__soft,
  .better-approach-type__interactive:hover .better-approach-type__accent {
    color: transparent !important;
    background:
      radial-gradient(circle 230px at var(--gradient-x) var(--gradient-y),
        var(--lime) 0%,
        var(--mint) 25%,
        var(--lavender) 49%,
        var(--peach) 73%,
        var(--ink) 100%) !important;
    -webkit-background-clip: text !important;
    background-clip: text !important;
  }
  
  @media (max-width: 1180px) {
    .dashboard-browser-mockup--simple { width: 235px; }
    .enterprise-art--dashboard .enterprise-node { min-width: 158px; max-width: 178px; }
    .enterprise-art--dashboard .enterprise-node--stores { left: 0; }
    .enterprise-art--dashboard .enterprise-node--punchout { right: 0; }
    .enterprise-art--dashboard .enterprise-node--orders { left: 0; }
    .enterprise-art--dashboard .enterprise-node--accounting { right: 0; }
  }
  @media (max-width: 980px) {
    .enterprise-art--dashboard {
      min-height: 630px;
      display: block;
    }
    .dashboard-browser-mockup--simple { width: 260px; }
    .enterprise-art--dashboard .enterprise-node { position: absolute; width: auto; }
    .enterprise-art--dashboard .enterprise-node--stores { left: 0; top: 25%; }
    .enterprise-art--dashboard .enterprise-node--punchout { right: 0; top: 23%; }
    .enterprise-art--dashboard .enterprise-node--orders { left: 0; bottom: 20%; }
    .enterprise-art--dashboard .enterprise-node--accounting { right: 0; bottom: 20%; }
    .enterprise-art--dashboard .enterprise-node--suppliers { top: 2%; }
    .enterprise-art--dashboard .enterprise-node--approvals { bottom: 1%; }
    .enterprise-art--dashboard .flow-line { display: block; }
  }
  @media (max-width: 680px) {
    .enterprise-art--dashboard {
      min-height: 690px;
    }
    .dashboard-browser-mockup--simple { width: 220px; }
    .enterprise-art--dashboard .enterprise-node {
      min-width: 138px;
      max-width: 145px;
      grid-template-columns: 32px 1fr;
      padding: 8px 9px;
      gap: 7px;
    }
    .enterprise-art--dashboard .enterprise-node__icon { width: 32px; height: 32px; border-radius: 10px; }
    .enterprise-art--dashboard .enterprise-node__copy strong { font-size: .58rem; }
    .enterprise-art--dashboard .enterprise-node__copy small { display: none; }
    .enterprise-art--dashboard .enterprise-node--stores { left: -2%; top: 27%; }
    .enterprise-art--dashboard .enterprise-node--punchout { right: -2%; top: 25%; }
    .enterprise-art--dashboard .enterprise-node--orders { left: -2%; bottom: 21%; }
    .enterprise-art--dashboard .enterprise-node--accounting { right: -2%; bottom: 21%; }
    .enterprise-art--dashboard .flow-line { opacity: .4; }
    .better-approach-type { margin-top: 72px; padding-inline: 0; }
    .better-approach-type__interactive { font-size: clamp(2.45rem, 11vw, 4.1rem); }
  }
  @media (prefers-reduced-motion: reduce) {
    .enterprise-art--dashboard .enterprise-node,
    .better-approach-type__interactive { animation: none !important; transition: none !important; }
    .better-approach-type__interactive:hover { transform: none; }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V12 refinements
     ========================================================== */
  /* Better approach, keep V6-style cursor-follow effect but switch colors closer to PromoStandards card */
  .better-approach-type__interactive {
    cursor: default;
    transition: filter .22s ease, transform .22s ease;
  }
  .better-approach-type__interactive:hover {
    filter: saturate(1.06);
    transform: translateY(-2px);
  }
  .better-approach-type__interactive:hover span,
  .better-approach-type__interactive:hover .better-approach-type__soft,
  .better-approach-type__interactive:hover .better-approach-type__accent {
    color: transparent !important;
    background: radial-gradient(circle 230px at var(--gradient-x) var(--gradient-y),
      #f5ffcc 0%,
      #e4ff8a 22%,
      #d6ff55 42%,
      #c2ff02 62%,
      #9ed300 82%,
      rgba(32,33,36,.98) 100%) !important;
    -webkit-background-clip: text !important;
    background-clip: text !important;
  }
  
  /* Why automate? redesign */
  .automation-impact {
    margin-top: 24px;
    padding: 34px;
    border: 1px solid rgba(32,33,36,.08);
    border-radius: 30px;
    background: linear-gradient(180deg, #fff 0%, #fbfbf8 100%);
    box-shadow: 0 18px 50px rgba(32,33,36,.055);
  }
  .automation-impact__header {
    display: grid;
    gap: 10px;
    padding-bottom: 22px;
    border-bottom: 1px solid var(--line);
  }
  .automation-impact__header span {
    color: var(--muted);
    font-size: .62rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: .1em;
  }
  .automation-impact__header strong {
    max-width: 780px;
    font-size: 1.65rem;
    line-height: 1.12;
    letter-spacing: -.035em;
  }
  .automation-impact__header p {
    max-width: 760px;
    margin: 0;
    color: var(--muted);
    font-size: .98rem;
    line-height: 1.65;
  }
  .automation-impact__list {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 14px;
    margin-top: 22px;
  }
  .impact-row {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(0, 1.2fr);
    gap: 18px;
    align-items: center;
    padding: 22px;
    border: 1px solid rgba(32,33,36,.06);
    border-radius: 22px;
  }
  .impact-row--lime { background: linear-gradient(145deg, rgba(223,255,115,.34), rgba(194,255,2,.20)); }
  .impact-row--mint { background: linear-gradient(145deg, rgba(153,254,237,.34), rgba(119,234,214,.18)); }
  .impact-row--peach { background: linear-gradient(145deg, rgba(255,208,184,.36), rgba(255,173,131,.20)); }
  .impact-row--lavender { background: linear-gradient(145deg, rgba(217,214,253,.48), rgba(203,198,255,.22)); }
  .impact-row__lead {
    display: grid;
    align-content: start;
    gap: 10px;
  }
  .impact-row__lead span {
    width: 42px;
    height: 42px;
    display: grid;
    place-items: center;
    border-radius: 14px;
    background: rgba(255,255,255,.74);
    color: var(--ink);
    font-size: .84rem;
    font-weight: 800;
  }
  .impact-row__lead strong {
    font-size: 1rem;
    line-height: 1.25;
  }
  .impact-row p {
    margin: 0;
    color: var(--muted);
    font-size: .82rem;
    line-height: 1.6;
  }
  .impact-row__result {
    grid-column: 1 / -1;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    width: fit-content;
    padding: 10px 14px;
    border-radius: 999px;
    background: rgba(255,255,255,.78);
    color: var(--ink);
    font-size: .73rem;
    font-weight: 700;
  }
  .impact-row__result::before {
    content: "";
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--ink);
    opacity: .72;
  }
  
  /* Hero connectors should visibly connect cards to center hub */
  .enterprise-art--dashboard .flow-line {
    opacity: .58;
    height: 2px;
    background: repeating-linear-gradient(90deg, rgba(32,33,36,.30) 0 4px, transparent 4px 9px);
  }
  .enterprise-art--dashboard .flow-line--1 {
    width: 176px;
    left: calc(50% - 106px);
    top: 31.5%;
    transform: rotate(198deg);
  }
  .enterprise-art--dashboard .flow-line--2 {
    width: 176px;
    left: 50%;
    top: 31.5%;
    transform: rotate(-18deg);
  }
  .enterprise-art--dashboard .flow-line--3 {
    width: 176px;
    left: calc(50% - 106px);
    top: 63.5%;
    transform: rotate(162deg);
  }
  .enterprise-art--dashboard .flow-line--4 {
    width: 176px;
    left: 50%;
    top: 63.5%;
    transform: rotate(18deg);
  }
  .enterprise-art--dashboard .flow-line--5 {
    width: 112px;
    left: 50%;
    top: 17.5%;
    transform: translateX(-50%) rotate(90deg);
  }
  .enterprise-art--dashboard .flow-line--6 {
    width: 112px;
    left: 50%;
    top: 60.7%;
    transform: translateX(-50%) rotate(90deg);
  }
  
  @media (max-width: 1180px) {
    .automation-impact__list { grid-template-columns: 1fr; }
    .impact-row { grid-template-columns: minmax(0,.92fr) minmax(0,1.2fr); }
  }
  @media (max-width: 980px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 { width: 162px; }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 { width: 102px; }
  }
  @media (max-width: 680px) {
    .automation-impact { padding: 24px 18px; }
    .automation-impact__header strong { font-size: 1.35rem; }
    .automation-impact__list { grid-template-columns: 1fr; }
    .impact-row { grid-template-columns: 1fr; gap: 12px; padding: 18px; }
    .impact-row__result { width: 100%; justify-content: center; }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V13 final fixes
     ========================================================== */
  /* Restore The better approach exactly like V6 */
  .better-approach-type { max-width: 1160px; }
  .better-approach-type__interactive {
    --gradient-x: 50%;
    --gradient-y: 50%;
    position: relative;
    cursor: default;
    background:
      radial-gradient(circle 220px at var(--gradient-x) var(--gradient-y),
        var(--lime) 0%,
        #8b79ea 28%,
        var(--ink) 62%,
        #536d66 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent !important;
    transition: filter .22s ease, transform .22s ease;
  }
  .better-approach-type__interactive span,
  .better-approach-type__interactive .better-approach-type__soft,
  .better-approach-type__interactive .better-approach-type__accent {
    color: transparent !important;
    background: none !important;
    -webkit-background-clip: initial !important;
    background-clip: initial !important;
  }
  .better-approach-type__interactive:hover {
    filter: saturate(1.08);
    transform: translateY(-2px);
  }
  .better-approach-type__interactive .better-approach-type__soft {
    font-weight: 600;
    font-size: .58em;
    letter-spacing: -.038em;
  }
  
  /* Hero connectors, visually connected on both ends and terminating behind the dashboard */
  .enterprise-art--dashboard .flow-line {
    z-index: 3;
    opacity: .55;
    height: 2px;
    background: repeating-linear-gradient(90deg, rgba(32,33,36,.28) 0 4px, transparent 4px 9px);
  }
  .enterprise-art--dashboard .flow-line::after {
    content: "";
    position: absolute;
    top: -2px;
    left: 0;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--lime);
    box-shadow: 0 0 0 4px rgba(194,255,2,.10);
    animation: packet 4s linear infinite;
  }
  .enterprise-art--dashboard .flow-line--1 {
    width: 188px;
    left: 23.5%;
    top: 34.2%;
    transform: rotate(17deg);
  }
  .enterprise-art--dashboard .flow-line--2 {
    width: 188px;
    left: 50.2%;
    top: 34.2%;
    transform: rotate(-17deg);
  }
  .enterprise-art--dashboard .flow-line--3 {
    width: 188px;
    left: 23.5%;
    top: 64.2%;
    transform: rotate(-17deg);
  }
  .enterprise-art--dashboard .flow-line--4 {
    width: 188px;
    left: 50.2%;
    top: 64.2%;
    transform: rotate(17deg);
  }
  .enterprise-art--dashboard .flow-line--5 {
    width: 132px;
    left: 50%;
    top: 16.5%;
    transform: translateX(-50%) rotate(90deg);
  }
  .enterprise-art--dashboard .flow-line--6 {
    width: 132px;
    left: 50%;
    top: 60.6%;
    transform: translateX(-50%) rotate(90deg);
  }
  .enterprise-art--dashboard .flow-line--2::after { animation-delay: -1.3s; background: var(--mint); box-shadow: 0 0 0 4px rgba(153,254,237,.10); }
  .enterprise-art--dashboard .flow-line--3::after { animation-delay: -2.2s; background: var(--peach); box-shadow: 0 0 0 4px rgba(255,189,155,.10); }
  .enterprise-art--dashboard .flow-line--4::after { animation-delay: -3s; background: var(--lavender); box-shadow: 0 0 0 4px rgba(217,214,253,.12); }
  .enterprise-art--dashboard .flow-line--5::after { animation-delay: -1.8s; }
  .enterprise-art--dashboard .flow-line--6::after { animation-delay: -2.6s; background: var(--mint); box-shadow: 0 0 0 4px rgba(153,254,237,.10); }
  
  @media (max-width: 1180px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--3 { left: 22.8%; }
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--4 { left: 50.2%; }
  }
  @media (max-width: 980px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 { width: 176px; }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 { width: 124px; }
  }
  @media (max-width: 680px) {
    .better-approach-type__interactive { font-size: clamp(2.45rem, 11vw, 4.1rem); }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V14
     Restore Better Approach to V6, star-style hero connectors
     ========================================================== */
  
  /* Restore section exactly in V6 spirit */
  .better-approach-type { max-width: 1160px; }
  .better-approach-type__interactive {
    --gradient-x: 50%;
    --gradient-y: 50%;
    position: relative;
    cursor: default;
    background:
      radial-gradient(circle 220px at var(--gradient-x) var(--gradient-y),
        var(--lime) 0%,
        #8b79ea 28%,
        var(--ink) 62%,
        #536d66 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent !important;
    transition: filter .22s ease, transform .22s ease;
  }
  .better-approach-type__interactive span,
  .better-approach-type__interactive .better-approach-type__soft,
  .better-approach-type__interactive .better-approach-type__accent {
    color: transparent !important;
    background: none !important;
    -webkit-background-clip: initial !important;
    background-clip: initial !important;
  }
  .better-approach-type__interactive:hover {
    filter: saturate(1.08);
    transform: translateY(-2px);
  }
  .better-approach-type__interactive .better-approach-type__soft {
    font-weight: 600;
    font-size: .58em;
    letter-spacing: -.038em;
  }
  
  /* Make all dotted lines meet in the center like an asterisk, behind the dashboard */
  .enterprise-art--dashboard .flow-line {
    z-index: 2;
    left: 50% !important;
    top: 50% !important;
    height: 2px;
    opacity: .54;
    transform-origin: 0 50%;
    background: repeating-linear-gradient(90deg, rgba(32,33,36,.28) 0 4px, transparent 4px 9px);
  }
  .enterprise-art--dashboard .flow-line::after {
    content: "";
    position: absolute;
    top: -2px;
    left: 0;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--lime);
    box-shadow: 0 0 0 4px rgba(194,255,2,.10);
    animation: packet 4s linear infinite;
  }
  .enterprise-art--dashboard .flow-line--1 {
    width: 205px;
    transform: translate(-2px,-1px) rotate(199deg) !important;
  }
  .enterprise-art--dashboard .flow-line--2 {
    width: 205px;
    transform: translate(2px,-1px) rotate(-19deg) !important;
  }
  .enterprise-art--dashboard .flow-line--3 {
    width: 205px;
    transform: translate(-2px,1px) rotate(161deg) !important;
  }
  .enterprise-art--dashboard .flow-line--4 {
    width: 205px;
    transform: translate(2px,1px) rotate(19deg) !important;
  }
  .enterprise-art--dashboard .flow-line--5 {
    width: 148px;
    transform: translateX(-50%) rotate(-90deg) !important;
  }
  .enterprise-art--dashboard .flow-line--6 {
    width: 148px;
    transform: translateX(-50%) rotate(90deg) !important;
  }
  .enterprise-art--dashboard .flow-line--2::after { animation-delay: -1.3s; background: var(--mint); box-shadow: 0 0 0 4px rgba(153,254,237,.10); }
  .enterprise-art--dashboard .flow-line--3::after { animation-delay: -2.2s; background: var(--peach); box-shadow: 0 0 0 4px rgba(255,189,155,.10); }
  .enterprise-art--dashboard .flow-line--4::after { animation-delay: -3s; background: var(--lavender); box-shadow: 0 0 0 4px rgba(217,214,253,.12); }
  .enterprise-art--dashboard .flow-line--5::after { animation-delay: -1.8s; }
  .enterprise-art--dashboard .flow-line--6::after { animation-delay: -2.6s; background: var(--mint); box-shadow: 0 0 0 4px rgba(153,254,237,.10); }
  
  @media (max-width: 1180px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 {
      width: 192px;
    }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 {
      width: 138px;
    }
  }
  @media (max-width: 980px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 {
      width: 182px;
    }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 {
      width: 130px;
    }
  }
  @media (max-width: 680px) {
    .better-approach-type__interactive { font-size: clamp(2.45rem, 11vw, 4.1rem); }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V15
     Simple text version for The better approach section
     ========================================================== */
  .better-approach-type__interactive {
    background: none !important;
    -webkit-background-clip: initial !important;
    background-clip: initial !important;
    color: var(--ink) !important;
    cursor: default !important;
    filter: none !important;
    transform: none !important;
    transition: none !important;
  }
  .better-approach-type__interactive span,
  .better-approach-type__interactive .better-approach-type__soft,
  .better-approach-type__interactive .better-approach-type__accent {
    color: var(--ink) !important;
    background: none !important;
    -webkit-background-clip: initial !important;
    background-clip: initial !important;
    transition: none !important;
  }
  .better-approach-type__interactive .better-approach-type__soft {
    color: var(--muted) !important;
  }
  .better-approach-type__interactive:hover,
  .better-approach-type__interactive:hover span,
  .better-approach-type__interactive:hover .better-approach-type__soft,
  .better-approach-type__interactive:hover .better-approach-type__accent {
    color: inherit !important;
    background: none !important;
    -webkit-background-clip: initial !important;
    background-clip: initial !important;
    filter: none !important;
    transform: none !important;
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V16
     Center-align all dotted lines to the browser hub
     ========================================================== */
  
  /* Use the exact center of the browser mockup as the shared anchor point. */
  .enterprise-art--dashboard .flow-line {
    position: absolute;
    left: 50% !important;
    top: 50% !important;
    z-index: 2;
    height: 2px;
    opacity: .56;
    transform-origin: 0 50%;
    background: repeating-linear-gradient(90deg, rgba(32,33,36,.29) 0 4px, transparent 4px 9px);
  }
  .enterprise-art--dashboard .flow-line::after {
    content: "";
    position: absolute;
    top: -2px;
    left: 0;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--lime);
    box-shadow: 0 0 0 4px rgba(194,255,2,.10);
    animation: packet 4s linear infinite;
  }
  
  /* Star-style routing from the exact center behind the dashboard. */
  .enterprise-art--dashboard .flow-line--1 {
    width: 210px;
    transform: translate(0, 0) rotate(202deg) !important;
  }
  .enterprise-art--dashboard .flow-line--2 {
    width: 210px;
    transform: translate(0, 0) rotate(-22deg) !important;
  }
  .enterprise-art--dashboard .flow-line--3 {
    width: 210px;
    transform: translate(0, 0) rotate(158deg) !important;
  }
  .enterprise-art--dashboard .flow-line--4 {
    width: 210px;
    transform: translate(0, 0) rotate(22deg) !important;
  }
  .enterprise-art--dashboard .flow-line--5 {
    width: 152px;
    transform: translate(0, 0) rotate(-90deg) !important;
  }
  .enterprise-art--dashboard .flow-line--6 {
    width: 152px;
    transform: translate(0, 0) rotate(90deg) !important;
  }
  
  /* Keep the moving dots color-coded like the surrounding pills. */
  .enterprise-art--dashboard .flow-line--2::after {
    animation-delay: -1.3s;
    background: var(--lavender);
    box-shadow: 0 0 0 4px rgba(217,214,253,.12);
  }
  .enterprise-art--dashboard .flow-line--3::after {
    animation-delay: -2.2s;
    background: var(--peach);
    box-shadow: 0 0 0 4px rgba(255,189,155,.10);
  }
  .enterprise-art--dashboard .flow-line--4::after {
    animation-delay: -3s;
    background: var(--mint);
    box-shadow: 0 0 0 4px rgba(153,254,237,.10);
  }
  .enterprise-art--dashboard .flow-line--5::after {
    animation-delay: -1.8s;
  }
  .enterprise-art--dashboard .flow-line--6::after {
    animation-delay: -2.6s;
    background: var(--lavender);
    box-shadow: 0 0 0 4px rgba(217,214,253,.12);
  }
  
  /* Slight position tuning so every line disappears under its target card edge. */
  .enterprise-art--dashboard .enterprise-node--suppliers {
    left: 50%;
    top: 1%;
    transform: translateX(-50%);
  }
  .enterprise-art--dashboard .enterprise-node--stores {
    left: 0%;
    top: 22%;
  }
  .enterprise-art--dashboard .enterprise-node--punchout {
    right: 0%;
    top: 20%;
  }
  .enterprise-art--dashboard .enterprise-node--orders {
    left: 1%;
    bottom: 18%;
  }
  .enterprise-art--dashboard .enterprise-node--accounting {
    right: 0%;
    bottom: 17%;
  }
  .enterprise-art--dashboard .enterprise-node--approvals {
    left: 50%;
    bottom: 1%;
    transform: translateX(-50%);
  }
  
  @media (max-width: 1180px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 {
      width: 198px;
    }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 {
      width: 144px;
    }
  }
  
  @media (max-width: 980px) {
    .enterprise-art--dashboard {
      min-height: 630px;
    }
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 {
      width: 184px;
    }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 {
      width: 132px;
    }
  }
  
  @media (max-width: 680px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 {
      width: 164px;
    }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 {
      width: 118px;
    }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V17
     Extend hero connector lines so they connect into the floating cards
     on desktop and phone views.
     ========================================================== */
  
  /* Desktop / large screens */
  .enterprise-art--dashboard .flow-line--1,
  .enterprise-art--dashboard .flow-line--2,
  .enterprise-art--dashboard .flow-line--3,
  .enterprise-art--dashboard .flow-line--4 {
    width: 252px;
  }
  .enterprise-art--dashboard .flow-line--5,
  .enterprise-art--dashboard .flow-line--6 {
    width: 188px;
  }
  
  /* Tablet / medium screens */
  @media (max-width: 1180px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 {
      width: 238px;
    }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 {
      width: 176px;
    }
  }
  
  /* Phone / small tablets */
  @media (max-width: 980px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 {
      width: 228px;
    }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 {
      width: 168px;
    }
  }
  
  /* Mobile */
  @media (max-width: 680px) {
    .enterprise-art--dashboard .flow-line--1,
    .enterprise-art--dashboard .flow-line--2,
    .enterprise-art--dashboard .flow-line--3,
    .enterprise-art--dashboard .flow-line--4 {
      width: 214px;
    }
    .enterprise-art--dashboard .flow-line--5,
    .enterprise-art--dashboard .flow-line--6 {
      width: 158px;
    }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V19
     Click the dashboard to pack/unpack the connected ecosystem.
     ========================================================== */
  .enterprise-art--dashboard {
    --pack-duration: 780ms;
  }
  .dashboard-browser-mockup--simple {
    cursor: pointer;
    user-select: none;
    -webkit-user-select: none;
    transition:
      box-shadow .42s var(--ease),
      border-color .42s ease,
      background .42s ease,
      transform .42s var(--ease);
  }
  .dashboard-browser-mockup--simple:hover {
    box-shadow: 0 30px 62px rgba(32,33,36,.16), inset 0 1px 0 rgba(255,255,255,.9);
  }
  .dashboard-browser-mockup--simple:focus-visible {
    outline: 3px solid rgba(194,255,2,.78);
    outline-offset: 7px;
  }
  .dashboard-browser-mockup__viewport--simple {
    position: relative;
    min-height: 0;
    background: #fff;
    transition: background .38s ease;
  }
  .simple-dashboard-image {
    position: relative;
    z-index: 1;
    opacity: 1;
    transform: scale(1);
    transition: opacity .34s ease, transform .58s var(--ease), filter .34s ease;
    will-change: opacity, transform;
  }
  .control-center-grid {
    position: absolute;
    z-index: 8;
    inset: 8px;
    display: grid;
    grid-template-columns: repeat(2, minmax(0,1fr));
    grid-template-rows: repeat(3, minmax(0,1fr));
    gap: 5px;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
  }
  .control-center-item {
    min-width: 0;
    display: grid;
    grid-template-columns: 22px minmax(0,1fr);
    align-items: center;
    gap: 5px;
    padding: 5px 6px;
    border: 1px solid rgba(32,33,36,.07);
    border-radius: 8px;
    background: #f8f8f5;
    box-shadow: 0 4px 10px rgba(32,33,36,.04);
  }
  .control-center-item:nth-child(1) { background: rgba(153,254,237,.34); }
  .control-center-item:nth-child(2) { background: rgba(217,214,253,.42); }
  .control-center-item:nth-child(3) { background: rgba(194,255,2,.27); }
  .control-center-item:nth-child(4) { background: rgba(153,254,237,.26); }
  .control-center-item:nth-child(5) { background: rgba(255,189,155,.32); }
  .control-center-item:nth-child(6) { background: rgba(217,214,253,.34); }
  .control-center-item__icon {
    width: 22px;
    height: 22px;
    display: grid;
    place-items: center;
    border-radius: 7px;
    background: rgba(255,255,255,.86);
    color: var(--ink);
    font-size: .36rem;
    font-weight: 800;
    overflow: hidden;
  }
  .control-center-item__icon svg {
    width: 13px;
    height: 13px;
    fill: none;
    stroke: currentColor;
    stroke-width: 1.8;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  .control-center-item strong {
    min-width: 0;
    display: block;
    color: var(--ink);
    font-size: .36rem;
    line-height: 1.15;
    letter-spacing: -.025em;
    overflow-wrap: anywhere;
  }
  
  /* Lines retract into the shared center point while cards enter the Control Center. */
  .enterprise-art--dashboard .flow-line {
    transition: width .56s var(--ease), opacity .28s ease;
  }
  .enterprise-art--dashboard.is-packing .flow-line,
  .enterprise-art--dashboard.is-packed .flow-line {
    width: 0 !important;
    opacity: 0;
  }
  
  .enterprise-art--dashboard.is-transitioning .enterprise-node,
  .enterprise-art--dashboard.is-packed .enterprise-node {
    animation-play-state: paused !important;
  }
  .enterprise-art--dashboard.is-packed .enterprise-node {
    opacity: 0;
    pointer-events: none;
  }
  .enterprise-art--dashboard.is-packed .simple-dashboard-image,
  .enterprise-art--dashboard.is-packing .simple-dashboard-image {
    opacity: 0;
    transform: scale(.96);
    filter: blur(2px);
  }
  .enterprise-art--dashboard.is-packed .dashboard-browser-mockup__viewport--simple,
  .enterprise-art--dashboard.is-packing .dashboard-browser-mockup__viewport--simple {
    background: #fff;
  }
  .enterprise-art--dashboard.is-packed .control-center-grid {
    opacity: 1;
    visibility: visible;
  }
  .enterprise-art--dashboard.is-packed .dashboard-browser-mockup--simple {
    border-color: rgba(32,33,36,.13);
    box-shadow: 0 30px 64px rgba(32,33,36,.15), 0 0 0 8px rgba(194,255,2,.07);
  }
  
  /* Small tactile cue when state changes. */
  .enterprise-art--dashboard.is-packing .dashboard-browser-mockup--simple {
    animation: controlCenterPress .78s var(--ease) both;
  }
  .enterprise-art--dashboard.is-unpacking .dashboard-browser-mockup--simple {
    animation: controlCenterRelease .78s var(--ease) both;
  }
  @keyframes controlCenterPress {
    0% { transform: translate(-50%,-50%) scale(1); }
    42% { transform: translate(-50%,-50%) scale(1.035); }
    100% { transform: translate(-50%,-50%) scale(1); }
  }
  @keyframes controlCenterRelease {
    0% { transform: translate(-50%,-50%) scale(1); }
    45% { transform: translate(-50%,-50%) scale(.97); }
    100% { transform: translate(-50%,-50%) scale(1); }
  }
  
  @media (max-width: 680px) {
    .control-center-grid { inset: 6px; gap: 4px; }
    .control-center-item { grid-template-columns: 18px minmax(0,1fr); gap: 4px; padding: 4px; border-radius: 7px; }
    .control-center-item__icon { width: 18px; height: 18px; border-radius: 6px; }
    .control-center-item__icon svg { width: 11px; height: 11px; }
    .control-center-item strong { font-size: .32rem; }
  }
  @media (prefers-reduced-motion: reduce) {
    .dashboard-browser-mockup--simple,
    .simple-dashboard-image,
    .enterprise-art--dashboard .flow-line { transition: none !important; animation: none !important; }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V20
     Keep packed cards visually consistent with floating cards,
     then gently nudge the packed browser to invite a second click.
     ========================================================== */
  
  /* Packed cards should look like mini versions of the floating pills. */
  .control-center-item,
  .control-center-item:nth-child(1),
  .control-center-item:nth-child(2),
  .control-center-item:nth-child(3),
  .control-center-item:nth-child(4),
  .control-center-item:nth-child(5),
  .control-center-item:nth-child(6) {
    background: rgba(255,255,255,.96);
    border-color: rgba(32,33,36,.075);
    box-shadow: 0 4px 11px rgba(32,33,36,.05);
  }
  
  /* Match the original floating-card icon tile colors. */
  .control-center-item:nth-child(1) .control-center-item__icon { background: var(--lime); }
  .control-center-item:nth-child(2) .control-center-item__icon { background: var(--lavender); }
  .control-center-item:nth-child(3) .control-center-item__icon { background: var(--peach); }
  .control-center-item:nth-child(4) .control-center-item__icon { background: var(--mint); }
  .control-center-item:nth-child(5) .control-center-item__icon { background: var(--mint); }
  .control-center-item:nth-child(6) .control-center-item__icon { background: var(--lavender); }
  
  .control-center-item__icon {
    border: 1px solid rgba(32,33,36,.045);
    box-shadow: inset 0 1px 0 rgba(255,255,255,.45);
  }
  
  /* Once packed, give the browser a restrained periodic nudge. */
  .enterprise-art--dashboard.is-packed:not(.is-transitioning) .dashboard-browser-mockup--simple {
    animation: packedControlCenterNudge 3.8s ease-in-out 3;
    transform-origin: center center;
  }
  
  @keyframes packedControlCenterNudge {
    0%, 72%, 100% {
      transform: translate(-50%,-50%) translateX(0) rotate(0deg) scale(1);
    }
    76% {
      transform: translate(-50%,-50%) translateX(-2px) rotate(-.45deg) scale(1.006);
    }
    79% {
      transform: translate(-50%,-50%) translateX(2px) rotate(.45deg) scale(1.006);
    }
    82% {
      transform: translate(-50%,-50%) translateX(-1px) rotate(-.22deg) scale(1.003);
    }
    85% {
      transform: translate(-50%,-50%) translateX(1px) rotate(.22deg) scale(1.003);
    }
    89% {
      transform: translate(-50%,-50%) translateX(0) rotate(0deg) scale(1);
    }
  }
  
  /* Hover/focus pauses the loop slightly so the click target feels stable. */
  .enterprise-art--dashboard.is-packed .dashboard-browser-mockup--simple:hover,
  .enterprise-art--dashboard.is-packed .dashboard-browser-mockup--simple:focus-within {
    animation-play-state: paused;
  }
  
  @media (prefers-reduced-motion: reduce) {
    .enterprise-art--dashboard.is-packed:not(.is-transitioning) .dashboard-browser-mockup--simple {
      animation: none !important;
    }
  }
  
  /* ==========================================================
     Enterprise Company Store Automation V21
     SAGE-style hero + trust + proof composition
     ========================================================== */
  .hero-trust-composition {
    position: relative;
    overflow: hidden;
    background:
      radial-gradient(circle at 82% 9%, rgba(217,214,253,.62), transparent 27%),
      radial-gradient(circle at 74% 72%, rgba(153,254,237,.44), transparent 31%),
      radial-gradient(circle at 7% 91%, rgba(255,189,155,.22), transparent 24%),
      linear-gradient(180deg, #fff 0%, #fbfbf8 100%);
  }
  .hero-trust-composition::after {
    content: "";
    position: absolute;
    z-index: 0;
    left: 50%;
    bottom: 2%;
    width: 1050px;
    height: 340px;
    transform: translateX(-50%);
    border-radius: 50%;
    background: radial-gradient(circle, rgba(255,255,255,.42) 0%, rgba(255,255,255,.10) 48%, transparent 72%);
    pointer-events: none;
  }
  .hero-trust-composition > * { position: relative; z-index: 1; }
  
  /* Hero now uses the shared wrapper background instead of ending visually before trust. */
  .hero {
    background: transparent !important;
    padding-bottom: 28px;
  }
  
  /* SAGE-style trust row: four compact, individual cards sitting inside the hero field. */
  .enterprise-trust-bar-section {
    margin: 0;
    padding: 0 0 24px;
    background: transparent !important;
  }
  .hero-trust-card-grid--enterprise {
    display: grid;
    grid-template-columns: repeat(4, minmax(0,1fr));
    gap: 10px;
    padding: 0;
    border: 0;
    border-radius: 0;
    background: transparent;
    box-shadow: none;
    overflow: visible;
  }
  .hero-trust-card-grid--enterprise .hero-trust-card {
    min-height: 82px;
    display: grid;
    grid-template-columns: minmax(74px,.72fr) minmax(0,1fr);
    align-items: center;
    gap: 14px;
    padding: 13px 16px;
    border: 1px solid rgba(32,33,36,.10);
    border-right: 1px solid rgba(32,33,36,.10);
    border-radius: 16px;
    background: rgba(255,255,255,.88);
    box-shadow: 0 10px 24px rgba(32,33,36,.035);
    -webkit-backdrop-filter: blur(8px);
    backdrop-filter: blur(8px);
  }
  .hero-trust-card-grid--enterprise .hero-trust-card:last-child {
    border-right: 1px solid rgba(32,33,36,.10);
  }
  .hero-trust-card-grid--enterprise .hero-trust-card__logo {
    min-width: 0;
    width: 100%;
    justify-content: start;
  }
  .hero-trust-card-grid--enterprise .hero-trust-card__logo svg {
    max-width: 112px;
    max-height: 46px;
  }
  .hero-trust-card-grid--enterprise .hero-trust-card__copy {
    gap: 4px;
    min-width: 0;
  }
  .hero-trust-card-grid--enterprise .hero-trust-card__copy small {
    font-size: .53rem;
    line-height: 1.2;
    letter-spacing: .11em;
    white-space: nowrap;
  }
  .hero-trust-card-grid--enterprise .hero-trust-card__copy strong {
    font-size: .72rem;
    line-height: 1.28;
  }
  .hero-trust-card__logo--delivery {
    width: 46px;
    height: 46px;
    min-width: 46px !important;
    border-radius: 50%;
    background: var(--lime);
    box-shadow: 0 0 0 7px rgba(194,255,2,.18);
    font-size: 1.45rem;
  }
  .hero-trustpilot-wordmark {
    gap: 6px;
    font-size: .76rem;
  }
  .hero-trustpilot-wordmark svg { width: 18px; height: 18px; }
  .hero-trustpilot-stars {
    gap: 2px;
    margin-top: 5px;
    font-size: .76rem;
  }
  
  /* Second row blends directly below trust cards, as on SAGE page. */
  .proof-strip {
    margin: 0;
    padding: 0 0 36px;
    background: transparent !important;
  }
  .proof-panel {
    display: grid;
    grid-template-columns: 1.45fr repeat(3,1fr);
    overflow: hidden;
    border: 1px solid rgba(32,33,36,.08);
    border-radius: 24px;
    background: rgba(255,255,255,.82);
    box-shadow: 0 12px 28px rgba(32,33,36,.035);
    -webkit-backdrop-filter: blur(8px);
    backdrop-filter: blur(8px);
  }
  .proof-intro,
  .proof-item {
    min-height: 104px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 22px 26px;
  }
  .proof-intro {
    background: linear-gradient(135deg, rgba(153,254,237,.76), rgba(231,255,249,.86));
  }
  .proof-intro strong {
    max-width: 480px;
    font-size: 1.05rem;
    line-height: 1.35;
    letter-spacing: -.028em;
  }
  .proof-item {
    border-left: 1px solid rgba(32,33,36,.08);
    background: rgba(255,255,255,.68);
  }
  .proof-item small {
    font-size: .55rem;
    letter-spacing: .10em;
  }
  .proof-item strong {
    margin-top: 8px;
    font-size: .76rem;
    line-height: 1.38;
  }
  
  @media (max-width: 1180px) {
    .hero-trust-card-grid--enterprise {
      grid-template-columns: repeat(2, minmax(0,1fr));
    }
    .hero-trust-card-grid--enterprise .hero-trust-card:nth-child(2),
    .hero-trust-card-grid--enterprise .hero-trust-card:nth-child(-n+2) {
      border-right: 1px solid rgba(32,33,36,.10);
      border-bottom: 1px solid rgba(32,33,36,.10);
    }
    .proof-panel { grid-template-columns: 1.25fr 1fr 1fr; }
    .proof-item:last-child { grid-column: 1 / -1; border-left: 0; border-top: 1px solid rgba(32,33,36,.08); }
  }
  
  @media (max-width: 900px) {
    .hero { padding-bottom: 20px; }
    .proof-panel { grid-template-columns: 1fr 1fr; }
    .proof-intro { grid-column: 1 / -1; }
    .proof-item { border-top: 1px solid rgba(32,33,36,.08); }
    .proof-item:last-child { grid-column: auto; border-left: 1px solid rgba(32,33,36,.08); }
  }
  
  @media (max-width: 680px) {
    .hero-trust-composition {
      background:
        radial-gradient(circle at 82% 14%, rgba(217,214,253,.52), transparent 28%),
        radial-gradient(circle at 72% 66%, rgba(153,254,237,.34), transparent 34%),
        linear-gradient(180deg, #fff 0%, #fbfbf8 100%);
    }
    .enterprise-trust-bar-section { padding: 0 0 16px; }
    .hero-trust-card-grid--enterprise {
      grid-template-columns: 1fr;
      gap: 8px;
    }
    .hero-trust-card-grid--enterprise .hero-trust-card {
      min-height: 74px;
      grid-template-columns: 92px 1fr;
      padding: 11px 14px;
      border-radius: 14px;
      box-shadow: none;
    }
    .hero-trust-card-grid--enterprise .hero-trust-card__copy small { white-space: normal; }
    .proof-strip { padding-bottom: 24px; }
    .proof-panel {
      grid-template-columns: 1fr;
      border-radius: 20px;
    }
    .proof-intro,
    .proof-item {
      min-height: 88px;
      padding: 18px 20px;
    }
    .proof-intro,
    .proof-item,
    .proof-item:last-child {
      grid-column: auto;
      border-left: 0;
    }
    .proof-item { border-top: 1px solid rgba(32,33,36,.08); }
  }
  
  /* ==========================================================
     V22: trust marks, continuous section transition, mobile + speed
     ========================================================== */
  
  /* Reliable trust marks. Plain HTML/SVG avoids SVG text rendering issues. */
  .hero-trust-card-grid--enterprise .hero-trust-card {
    grid-template-columns: 104px minmax(0, 1fr);
  }
  .hero-trust-card-grid--enterprise .hero-trust-card__logo {
    width: auto;
    min-width: 0;
    justify-content: flex-start;
    overflow: visible;
  }
  .trust-brand {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    min-width: 0;
    color: var(--ink);
    line-height: 1;
  }
  .trust-brand--shopify svg {
    width: 28px !important;
    height: 34px !important;
    flex: 0 0 28px;
  }
  .trust-brand--shopify span,
  .trust-brand--shopify b,
  .trust-brand--shopify small {
    display: block;
  }
  .trust-brand--shopify b {
    font-size: .77rem;
    font-weight: 800;
    letter-spacing: -.035em;
  }
  .trust-brand--shopify small {
    margin-top: 4px;
    font-size: .47rem;
    font-weight: 800;
    letter-spacing: .17em;
  }
  .trust-brand--ppai {
    display: grid;
    gap: 4px;
    color: #3242a8;
  }
  .trust-brand--ppai b {
    font-size: 1rem;
    font-weight: 800;
    letter-spacing: -.045em;
  }
  .trust-brand--ppai sup {
    position: relative;
    top: -.28em;
    margin-left: 2px;
    font-size: .4em;
  }
  .trust-brand--ppai small {
    font-size: .48rem;
    font-weight: 800;
    letter-spacing: .18em;
  }
  .trust-delivery-mark {
    width: 48px;
    height: 48px;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: var(--lime);
    box-shadow: 0 0 0 7px rgba(194,255,2,.19);
  }
  .trust-delivery-mark span {
    color: #111;
    font-size: 1.45rem;
    font-weight: 900;
    line-height: 1;
  }
  
  /* Continue the hero composition behind the secondary nav into the problem section. */
  .page-nav-wrap {
    margin-bottom: -54px;
  }
  .page-nav-wrap:not(.is-stuck) {
    background: transparent !important;
  }
  #problem {
    margin-top: 0;
    padding-top: 176px;
    background:
      radial-gradient(circle at 10% 4%, rgba(217,214,253,.27), transparent 25%),
      radial-gradient(circle at 90% 22%, rgba(153,254,237,.23), transparent 28%),
      linear-gradient(180deg, #fbfbf8 0%, #f8fbfa 22%, #ffffff 72%) !important;
  }
  
  /* Initial-render optimization for heavy below-the-fold sections. */
  #platform,
  #architecture,
  #workflow,
  #systems,
  #success-story,
  #process,
  #faq,
  .final-cta-section,
  .site-footer {
    content-visibility: auto;
    contain-intrinsic-size: 900px;
  }
  
  /* The picture element must behave like the original image. */
  .dashboard-browser-mockup__viewport picture {
    display: block;
    width: 100%;
  }
  .dashboard-browser-mockup__viewport picture .simple-dashboard-image {
    width: 100%;
    height: auto;
  }
  
  @media (max-width: 900px) {
    .hero-trust-card-grid--enterprise .hero-trust-card {
      grid-template-columns: 94px minmax(0,1fr);
    }
    #problem {
      padding-top: 154px;
    }
  }
  
  @media (max-width: 680px) {
    /* 2x2 is faster to scan and avoids an unnecessarily tall trust stack. */
    .hero-trust-card-grid--enterprise {
      grid-template-columns: repeat(2, minmax(0,1fr));
      gap: 8px;
    }
    .hero-trust-card-grid--enterprise .hero-trust-card {
      min-height: 88px;
      grid-template-columns: 68px minmax(0,1fr);
      gap: 8px;
      padding: 11px 12px;
      -webkit-backdrop-filter: none;
      backdrop-filter: none;
    }
    .hero-trust-card-grid--enterprise .hero-trust-card__copy small {
      font-size: .48rem;
      line-height: 1.25;
    }
    .hero-trust-card-grid--enterprise .hero-trust-card__copy strong {
      font-size: .67rem;
    }
    .trust-brand--shopify {
      gap: 5px;
    }
    .trust-brand--shopify svg {
      width: 22px !important;
      height: 28px !important;
      flex-basis: 22px;
    }
    .trust-brand--shopify b { font-size: .63rem; }
    .trust-brand--shopify small { font-size: .39rem; letter-spacing: .12em; }
    .trust-brand--ppai b { font-size: .85rem; }
    .trust-brand--ppai small { font-size: .4rem; letter-spacing: .13em; }
    .trust-delivery-mark {
      width: 42px;
      height: 42px;
      box-shadow: 0 0 0 5px rgba(194,255,2,.17);
    }
    .trust-delivery-mark span { font-size: 1.2rem; }
  
    .proof-panel {
      -webkit-backdrop-filter: none;
      backdrop-filter: none;
    }
    .page-nav-wrap {
      margin-bottom: -42px;
    }
    #problem {
      padding-top: 128px;
    }
  
    /* Avoid expensive blur effects on small screens while preserving the look. */
    .enterprise-node,
    .modern-nav,
    .page-nav-shell {
      -webkit-backdrop-filter: none;
      backdrop-filter: none;
    }
  }
  
  @media (max-width: 420px) {
    .hero-trust-card-grid--enterprise {
      grid-template-columns: 1fr;
    }
    .hero-trust-card-grid--enterprise .hero-trust-card {
      min-height: 76px;
      grid-template-columns: 82px minmax(0,1fr);
    }
  }
  
  /* Accessibility + production interaction polish */
  .sr-only {
    position: absolute !important;
    width: 1px !important;
    height: 1px !important;
    padding: 0 !important;
    margin: -1px !important;
    overflow: hidden !important;
    clip: rect(0, 0, 0, 0) !important;
    white-space: nowrap !important;
    border: 0 !important;
  }
  
  .dashboard-browser-mockup--simple {
    position: absolute;
  }
  
  .dashboard-browser-trigger {
    position: absolute;
    z-index: 30;
    inset: 0;
    width: 100%;
    height: 100%;
    padding: 0;
    border: 0;
    border-radius: inherit;
    appearance: none;
    background: transparent;
    cursor: pointer;
  }
  
  .dashboard-browser-trigger:focus-visible {
    outline: 3px solid rgba(194,255,2,.88);
    outline-offset: 5px;
  }
  </style>
  <main>
  <div class="hero-trust-composition">
  <section class="hero" id="top">
  <div class="page-shell hero-grid">
  <div class="hero-content" data-reveal="">
  <nav aria-label="Breadcrumb" class="breadcrumb"><a href="../">Home</a><span aria-hidden="true">/</span><a href="../shopify-solutions/">Shopify Solutions</a><span aria-hidden="true">/</span><strong>Enterprise Company Store Automation</strong></nav>
  <p class="eyebrow">Enterprise company store management + automation</p>
  <h1 class="hero-title">Stop managing company stores <span class="accent">one by one.</span></h1>
  <p class="hero-subheadline">One dashboard for every company store. Add automation only when it solves a real operational problem.</p>
  <p class="hero-copy">Built for large promotional distributors. Centralize orders and reporting first, then add the workflows your operation needs.</p>
  <div class="hero-actions">
  <a class="btn btn--primary" data-analytics-event="hero_automation_cta" href="../book-demo/?service=enterprise-company-store-automation&amp;intent=automation"><span>Automate My Company</span><span aria-hidden="true" class="btn__arrow">↗</span></a>
  <a class="btn btn--secondary" data-analytics-event="hero_order_flow_click" href="#workflow">See an Automated Order Flow</a>
  </div>
  <div aria-label="Service highlights" class="hero-meta"><span class="meta-pill">Centralized multi-store control</span><span class="meta-pill">Base platform + paid add-ons</span><span class="meta-pill">Built for promo distributors</span></div>
  </div>
  <div aria-label="Swishtag CEO dashboard preview inside a browser mockup" class="enterprise-art enterprise-art--dashboard" data-reveal="">
  <div aria-hidden="true" class="flow-line flow-line--1"></div>
  <div aria-hidden="true" class="flow-line flow-line--2"></div>
  <div aria-hidden="true" class="flow-line flow-line--3"></div>
  <div aria-hidden="true" class="flow-line flow-line--4"></div>
  <div aria-hidden="true" class="flow-line flow-line--5"></div>
  <div aria-hidden="true" class="flow-line flow-line--6"></div>
  <div class="dashboard-browser-mockup dashboard-browser-mockup--simple">
  <button aria-controls="controlCenterGrid" aria-label="Open the Control Center. Click to bring the connected company-store tools inside the dashboard." aria-pressed="false" class="dashboard-browser-trigger" type="button"><span class="sr-only">Toggle Control Center</span></button>
  <div aria-hidden="true" class="dashboard-browser-mockup__bar dashboard-browser-mockup__bar--minimal"><span class="traffic traffic--red"></span><span class="traffic traffic--yellow"></span><span class="traffic traffic--green"></span></div>
  <div class="dashboard-browser-mockup__viewport dashboard-browser-mockup__viewport--simple">
  <picture>
    <source srcset="/assets/swishtag-enterprise-ceo-dashboard-640.webp" type="image/webp"/>
    <img alt="Swishtag enterprise CEO dashboard showing one dashboard view across multiple company stores" class="simple-dashboard-image" decoding="async" fetchpriority="high" height="420" loading="eager" src="../assets/swishtag-enterprise-ceo-dashboard-640.webp" width="640"/>
  </picture>
  <div aria-hidden="true" class="control-center-grid" id="controlCenterGrid"></div>
  </div>
  </div>
  <div class="enterprise-node enterprise-node--stores"><span class="enterprise-node__icon">SHOP</span><div class="enterprise-node__copy"><strong>Company Stores</strong><small>One portfolio view</small></div></div>
  <div class="enterprise-node enterprise-node--punchout"><span class="enterprise-node__icon"><svg viewBox="0 0 24 24"><path d="M4 6h16v12H4zM8 10h8M8 14h5"></path></svg></span><div class="enterprise-node__copy"><strong>PunchOut</strong><small>Optional add-on</small></div></div>
  <div class="enterprise-node enterprise-node--orders"><span class="enterprise-node__icon"><svg viewBox="0 0 24 24"><path d="M5 4h14v16H5zM8 8h8M8 12h8M8 16h5"></path></svg></span><div class="enterprise-node__copy"><strong>Order Automation</strong><small>Split, PO, route</small></div></div>
  <div class="enterprise-node enterprise-node--accounting"><span class="enterprise-node__icon"><svg viewBox="0 0 24 24"><path d="M4 7h16M6 4h12v16H6zM9 11h6M9 15h4"></path></svg></span><div class="enterprise-node__copy"><strong>Supplier APIs</strong><small>Custom connections</small></div></div>
  <div class="enterprise-node enterprise-node--suppliers"><span class="enterprise-node__icon"><svg viewBox="0 0 24 24"><path d="M4 9h16v10H4zM8 9V5h8v4"></path></svg></span><div class="enterprise-node__copy"><strong>SAGE + PromoStandards</strong><small>Promo data connections</small></div></div>
  <div class="enterprise-node enterprise-node--approvals"><span class="enterprise-node__icon"><svg viewBox="0 0 24 24"><path d="M5 5h14v14H5z"></path><path d="m8 12 3 3 5-6"></path></svg></span><div class="enterprise-node__copy"><strong>Decorators</strong><small>Configured routing paths</small></div></div>
  </div>
  </div>
  </section>
  <section aria-label="Trust indicators" class="enterprise-trust-bar-section">
  <div class="page-shell">
  <div class="hero-trust-card-grid hero-trust-card-grid--enterprise">
  <div aria-label="Shopify Partner" class="hero-trust-card hero-trust-card--shopify">
    <div class="hero-trust-card__logo">
      <div aria-hidden="true" class="trust-brand trust-brand--shopify">
        <svg viewBox="0 0 34 40"><path d="M8 10h20l2.2 25H5.8L8 10Z" fill="#95BF47"></path><path d="M11 12c.3-6 2.8-9 6.1-9 3.4 0 5.7 3 6 9" fill="none" stroke="#95BF47" stroke-width="3" stroke-linecap="round"></path><path d="M20.8 16.5c-1-.6-2.3-.9-3.5-.9-1.1 0-1.8.4-1.8 1.2 0 .7.6 1 1.9 1.5 2.4.9 3.4 2.2 3 4.3-.4 2.6-2.7 4.3-5.5 4.3-1.9 0-3.7-.5-4.9-1.4l1.2-3.2c1.2.9 2.5 1.3 3.9 1.3 1.1 0 1.9-.4 2-1.1.1-.7-.4-1.1-1.8-1.5-2.2-.8-3.3-2-2.9-4 .4-2.5 2.5-4.2 5.5-4.2 1.7 0 3.1.4 4.1 1l-1.2 2.7Z" fill="#fff"></path></svg>
        <span><b>shopify</b><small>PARTNER</small></span>
      </div>
    </div>
    <div class="hero-trust-card__copy"><small>OFFICIAL PARTNER</small><strong>Shopify Partner</strong></div>
  </div>
  <div aria-label="PPAI Member" class="hero-trust-card hero-trust-card--ppai">
    <div class="hero-trust-card__logo">
      <div aria-hidden="true" class="trust-brand trust-brand--ppai"><b>PPAI<sup>®</sup></b><small>MEMBER</small></div>
    </div>
    <div class="hero-trust-card__copy"><small>INDUSTRY MEMBERSHIP</small><strong>PPAI Member</strong></div>
  </div>
  <div aria-label="Trustpilot 4.6 rating" class="hero-trust-card hero-trust-card--trustpilot">
    <div class="hero-trust-card__logo hero-trust-card__logo--trustpilot"><div class="hero-trustpilot-wordmark"><svg aria-hidden="true" viewBox="0 0 24 24"><path d="M12 1.8l3.15 6.37 7.03 1.02-5.09 4.96 1.2 7.01L12 17.86 5.71 21.16l1.2-7.01L1.82 9.19l7.03-1.02L12 1.8z" fill="#00B67A"></path></svg><strong>Trustpilot</strong></div><div aria-hidden="true" class="hero-trustpilot-stars"><span>★</span><span>★</span><span>★</span><span>★</span><span class="partial">★</span></div></div>
    <div class="hero-trust-card__copy"><small>CUSTOMER REVIEWS</small><strong>4.6 rating</strong></div>
  </div>
  <div aria-label="100 percent project delivery" class="hero-trust-card hero-trust-card--delivery">
    <div class="hero-trust-card__logo"><div aria-hidden="true" class="trust-delivery-mark"><span>✓</span></div></div>
    <div class="hero-trust-card__copy"><small>DELIVERY RECORD</small><strong>100% Project Delivery</strong></div>
  </div>
  </div>
  </div>
  </section>
  <section aria-label="Enterprise company store platform trust points" class="proof-strip">
  <div class="page-shell proof-panel">
  <div class="proof-intro"><strong>Built for distributors running complex company-store programs.</strong></div>
  <div class="proof-item"><small>Base platform</small><strong>Stores, orders &amp; reporting in one place</strong></div>
  <div class="proof-item"><small>Automation add-ons</small><strong>Supplier routing, POs &amp; PunchOut</strong></div>
  <div class="proof-item"><small>Custom connections</small><strong>Accounting, ERP, AI &amp; your systems</strong></div>
  </div>
  </section>
  </div>
  <div class="page-nav-wrap" id="pageNavWrap">
  <div class="page-shell">
  <nav aria-label="Page navigation" class="page-nav-shell" id="pageNavShell">
  <button aria-controls="pageNavLinks" aria-expanded="false" class="page-nav-toggle" id="pageNavToggle" type="button"><span><small>On this page</small><strong id="pageNavCurrent">Problem</strong></span><span aria-hidden="true" class="page-nav-toggle__icon">+</span></button>
  <div class="page-nav" id="pageNavLinks"><a href="#problem">Problem</a><a href="#platform">Base Platform</a><a href="#architecture">Platform + Add-ons</a><a href="#workflow">Order Flow</a><a href="#systems">Integrations</a><a href="#success-story">Success Story</a><a href="#process">Onboarding</a><a href="#faq">FAQs</a><a class="page-nav__cta" data-analytics-event="secondary_nav_automation_cta" href="../book-demo/?service=enterprise-company-store-automation&amp;intent=automation">Automate My Company</a></div>
  </nav>
  </div>
  </div>
  <section class="section section--soft" data-nav-label="Problem" id="problem">
  <div class="page-shell">
  <div class="section-heading-split">
  <div data-reveal=""><p class="eyebrow">The multi-store problem</p><h2 class="section-title">More company stores should not mean more tabs, more spreadsheets, and more people.</h2></div>
  <p class="section-copy" data-reveal="">Large distributors rarely struggle to launch the next Shopify store. The pain comes later, when the operations team has to keep opening store after store to find orders, build reports, split supplier work, create POs, and answer enterprise client requests.</p>
  </div>
  <div class="problem-grid">
  <article class="problem-card" data-reveal=""><span class="problem-card__number">01</span><h3>You cannot see the whole business at once.</h3><p>Orders and performance are spread across separate Shopify admins, so getting a clear portfolio view means logging into stores individually or rebuilding the same report outside Shopify.</p></article>
  <article class="problem-card" data-reveal=""><span class="problem-card__number">02</span><h3>Your team is doing work the order already told you to do.</h3><p>People still split orders by supplier, create purchase orders, enter supplier orders, decide the decorator path, and chase the next step manually.</p></article>
  <article class="problem-card" data-reveal=""><span class="problem-card__number">03</span><h3>Every large client asks for something different.</h3><p>One needs PunchOut. Another needs a supplier connection. Another has a unique routing or approval rule. Off-the-shelf apps stop fitting once the workflow gets specific.</p></article>
  </div>
  <div class="better-approach-type" data-reveal="">
  <p class="eyebrow">The better approach</p>
  <h3 class="better-approach-type__interactive"><span class="better-approach-type__soft">More stores. More tabs. More manual work.</span><span>Bring every company store into one place.</span><span class="better-approach-type__accent">Automate the repetitive work eating your time and margins.</span></h3>
  <div aria-label="Recommended approach" class="better-approach-type__steps"><span>Centralize</span><i></i><span>Connect</span><i></i><span>Automate</span></div>
  </div>
  </div>
  </section>
  <section class="section section--gray" data-nav-label="Base Platform" id="platform">
  <div class="page-shell platform-layout">
  <div class="platform-sticky" data-reveal=""><p class="eyebrow">Start with the base platform</p><h2 class="section-title section-title--small">One dashboard for every company store.</h2><p class="section-copy">If your biggest problem is simply managing many stores, start here. Bring your Shopify company stores into one control layer so your team can see the portfolio without living inside separate Shopify admins.</p><a class="text-link" href="#architecture">Then add only what you need <span>→</span></a></div>
  <div class="platform-list">
  <article class="platform-item" data-reveal=""><span class="platform-icon"><svg viewBox="0 0 24 24"><path d="M4 5h16v14H4zM8 9h8M8 13h5"></path></svg></span><div><h3>See orders from every store in one place</h3><p>Give operations one central order view across multiple client programs instead of making people jump between Shopify stores just to see what came in.</p></div></article>
  <article class="platform-item" data-reveal=""><span class="platform-icon"><svg viewBox="0 0 24 24"><path d="M4 18V9M10 18V5M16 18v-7M22 18V7"></path></svg></span><div><h3>Get portfolio-level reporting</h3><p>See reporting across the company store portfolio, then drill into individual stores when a client, account manager, or operations team needs the detail.</p></div></article>
  <article class="platform-item" data-reveal=""><span class="platform-icon"><svg viewBox="0 0 24 24"><circle cx="6" cy="12" r="2"></circle><circle cx="18" cy="6" r="2"></circle><circle cx="18" cy="18" r="2"></circle><path d="m8 11 8-4M8 13l8 4"></path></svg></span><div><h3>Manage different store types together</h3><p>Your portfolio can include simple Shopify company stores and SAGE-powered Shopify stores. The platform gives you a common operational view while each store keeps the setup it needs.</p></div></article>
  <article class="platform-item" data-reveal=""><span class="platform-icon"><svg viewBox="0 0 24 24"><path d="M5 6h14v12H5z"></path><path d="m8 12 3 3 5-6"></path></svg></span><div><h3>Stop at the platform if that solves the problem</h3><p>You do not have to buy a full automation project. Use the base platform for centralized management, then add workflow automation only when the operational return makes sense.</p></div></article>
  </div>
  </div>
  </section>
  <section class="section solution-package-section" data-nav-label="Platform + Add-ons" id="architecture">
  <div class="page-shell">
  <div class="section-heading-split">
  <div data-reveal="">
  <p class="eyebrow">Base platform + optional add-ons</p>
  <h2 class="section-title">One platform. Add only what your operation needs.</h2>
  </div>
  <p class="section-copy" data-reveal="">Start with centralized company-store control. Then add workflow automation, integrations, procurement, AI, or custom logic only when it solves a real business problem.</p>
  </div>
  
  <article class="solution-base-card" data-reveal="">
  <div class="solution-base-card__top">
  <div><span class="solution-pill solution-pill--included">Included</span><span class="solution-card-kicker">Base Platform</span></div>
  <span class="solution-base-card__status">Your starting point</span>
  </div>
  <div class="solution-base-card__body">
  <div>
  <h3>One place to control every company store.</h3>
  <p>Centralize orders, reporting, store visibility, and operational status across your Shopify company-store portfolio. Use the base platform by itself if that solves the problem.</p>
  </div>
  <div class="solution-base-features">
  <div><strong>All stores</strong><span>One portfolio view</span></div>
  <div><strong>All orders</strong><span>Central order visibility</span></div>
  <div><strong>Reporting</strong><span>Portfolio + store level</span></div>
  <div><strong>Exceptions</strong><span>See what needs attention</span></div>
  </div>
  </div>
  </article>
  
  <div class="optional-addons-intro" data-reveal="">
  <span class="optional-addons-intro__icon">+</span>
  <div><small>Everything below is optional</small><strong>Add only what your business needs.</strong><p>No forced bundle. Pick one add-on, combine several, or run the base platform without any of them.</p></div>
  </div>
  
  <div class="addon-card-grid">
  <article class="solution-addon-card solution-addon-card--lavender" data-reveal="">
  <span class="solution-addon-card__bg-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 7h6v6H4zM14 4h6v6h-6zM14 14h6v6h-6zM10 10h4M17 10v4M10 10 7 7"></path></svg></span>
  <div class="solution-addon-card__top"><div class="solution-addon-card__identity"><span class="solution-addon-card__icon"><svg viewBox="0 0 24 24"><path d="M4 7h6v6H4zM14 4h6v6h-6zM14 14h6v6h-6zM10 10h4M17 10v4M10 10 7 7"></path></svg></span><span class="solution-pill">Optional add-on</span></div><span class="solution-addon-card__number">01</span></div>
  <h3>OMS &amp; order routing</h3>
  <p>Automate the repetitive work after an order arrives. Split orders by supplier, generate POs, submit supplier orders, route work to decorators, and flag exceptions.</p>
  <div class="solution-addon-tags"><span>Order split</span><span>POs</span><span>Decorator routing</span></div>
  <div class="solution-addon-card__best"><small>Useful when</small><strong>Your team manually decides where every order goes.</strong></div>
  </article>
  
  <article class="solution-addon-card solution-addon-card--mint" data-reveal="">
  <span class="solution-addon-card__bg-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M7 7h4v4H7zM13 13h4v4h-4zM11 9h5a3 3 0 0 1 3 3v1M13 15H8a3 3 0 0 1-3-3v-1"></path></svg></span>
  <div class="solution-addon-card__top"><div class="solution-addon-card__identity"><span class="solution-addon-card__icon"><svg viewBox="0 0 24 24"><path d="M7 7h4v4H7zM13 13h4v4h-4zM11 9h5a3 3 0 0 1 3 3v1M13 15H8a3 3 0 0 1-3-3v-1"></path></svg></span><span class="solution-pill">Optional add-on</span></div><span class="solution-addon-card__number">02</span></div>
  <h3>Supplier connections</h3>
  <p>Connect directly with approved suppliers when they provide suitable APIs or technical access. Automate product data, order submission, status updates, or another agreed workflow.</p>
  <div class="solution-addon-tags"><span>Supplier APIs</span><span>Order submission</span><span>Status sync</span></div>
  <div class="solution-addon-card__best"><small>Useful when</small><strong>Your team is re-entering supplier data or orders by hand.</strong></div>
  </article>
  
  <article class="solution-addon-card solution-addon-card--lime" data-reveal="">
  <span class="solution-addon-card__bg-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M5 5h14v14H5zM8 9h8M8 13h8M8 17h5"></path></svg></span>
  <div class="solution-addon-card__top"><div class="solution-addon-card__identity"><span class="solution-addon-card__icon"><svg viewBox="0 0 24 24"><path d="M5 5h14v14H5zM8 9h8M8 13h8M8 17h5"></path></svg></span><span class="solution-pill">Optional add-on</span></div><span class="solution-addon-card__number">03</span></div>
  <h3>PromoStandards integration</h3>
  <p>Use PromoStandards endpoints to connect supported supplier product, pricing, inventory, order, or status data into the workflow your stores need.</p>
  <div class="solution-addon-tags"><span>Product data</span><span>Pricing</span><span>Inventory</span><span>Orders</span></div>
  <div class="solution-addon-card__best"><small>Useful when</small><strong>You want standardized promo-industry data moving automatically.</strong></div>
  </article>
  
  <article class="solution-addon-card solution-addon-card--peach" data-reveal="">
  <span class="solution-addon-card__bg-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M5 6h14v12H5zM8 10h8M8 14h5M19 9l3 3-3 3"></path></svg></span>
  <div class="solution-addon-card__top"><div class="solution-addon-card__identity"><span class="solution-addon-card__icon"><svg viewBox="0 0 24 24"><path d="M5 6h14v12H5zM8 10h8M8 14h5M19 9l3 3-3 3"></path></svg></span><span class="solution-pill">Optional add-on</span></div><span class="solution-addon-card__number">04</span></div>
  <h3>PunchOut catalogs</h3>
  <p>Enable procurement-connected company stores when enterprise buyers need PunchOut. Scope the cXML session, cart return, authentication, PO flow, and buyer-system testing.</p>
  <div class="solution-addon-tags"><span>Coupa</span><span>SAP Ariba</span><span>Jaggaer</span><span>Oracle</span></div>
  <div class="solution-addon-card__best"><small>Useful when</small><strong>A corporate client wants to buy through procurement.</strong></div>
  </article>
  
  <article class="solution-addon-card solution-addon-card--finance" data-reveal="">
  <span class="solution-addon-card__bg-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M5 4h14v16H5zM8 8h8M8 12h8M8 16h5M12 4v16"></path></svg></span>
  <div class="solution-addon-card__top"><div class="solution-addon-card__identity"><span class="solution-addon-card__icon"><svg viewBox="0 0 24 24"><path d="M5 4h14v16H5zM8 8h8M8 12h8M8 16h5M12 4v16"></path></svg></span><span class="solution-pill">Optional add-on</span></div><span class="solution-addon-card__number">05</span></div>
  <h3>Accounting integration</h3>
  <p>Push clean commerce data into the finance or ERP system instead of recreating the transaction downstream. Integrations can be scoped for QuickBooks, Sage Intacct, NetSuite, or another practical API.</p>
  <div class="solution-addon-tags"><span>QuickBooks</span><span>Sage Intacct</span><span>NetSuite</span></div>
  <div class="solution-addon-card__best"><small>Useful when</small><strong>Your team re-enters orders into accounting or ERP software.</strong></div>
  </article>
  
  <article class="solution-addon-card solution-addon-card--ai" data-reveal="">
  <span class="solution-addon-card__bg-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M12 3v3M12 18v3M3 12h3M18 12h3M5.6 5.6l2.1 2.1M16.3 16.3l2.1 2.1M18.4 5.6l-2.1 2.1M7.7 16.3l-2.1 2.1"></path><circle cx="12" cy="12" r="4"></circle></svg></span>
  <div class="solution-addon-card__top"><div class="solution-addon-card__identity"><span class="solution-addon-card__icon"><svg viewBox="0 0 24 24"><path d="M12 3v3M12 18v3M3 12h3M18 12h3M5.6 5.6l2.1 2.1M16.3 16.3l2.1 2.1M18.4 5.6l-2.1 2.1M7.7 16.3l-2.1 2.1"></path><circle cx="12" cy="12" r="4"></circle></svg></span><span class="solution-pill solution-pill--dark">Optional add-on</span></div><span class="solution-addon-card__number">06</span></div>
  <h3>AI powered</h3>
  <p>Add AI where it creates a measurable operational or customer benefit. That can include OpenAI workflows, chatbots, calling agents, recommendation engines, or internal assistants.</p>
  <div class="solution-addon-tags"><span>OpenAI</span><span>Chatbots</span><span>Calling agents</span><span>Recommendations</span></div>
  <div class="solution-addon-card__best"><small>Useful when</small><strong>A repetitive decision or conversation can be handled intelligently.</strong></div>
  </article>
  
  <article class="solution-addon-card solution-addon-card--custom" data-reveal="">
  <span class="solution-addon-card__bg-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 6h7v5H4zM13 13h7v5h-7zM11 8h4a3 3 0 0 1 3 3v2M13 16H9a3 3 0 0 1-3-3v-2"></path></svg></span>
  <div class="solution-addon-card__top"><div class="solution-addon-card__identity"><span class="solution-addon-card__icon"><svg viewBox="0 0 24 24"><path d="M4 6h7v5H4zM13 13h7v5h-7zM11 8h4a3 3 0 0 1 3 3v2M13 16H9a3 3 0 0 1-3-3v-2"></path></svg></span><span class="solution-pill">Optional add-on</span></div><span class="solution-addon-card__number">07</span></div>
  <h3>Custom automation</h3>
  <p>Have a workflow nobody else solves well? Show us how your business operates. If there is a practical technical path, we can build the missing integration, dashboard, approval flow, internal tool, or automation layer.</p>
  <div class="solution-addon-tags"><span>Custom APIs</span><span>Approvals</span><span>Dashboards</span><span>Internal tools</span></div>
  <div class="solution-addon-card__best"><small>Your idea</small><strong>You think of the workflow. We build the missing layer.</strong></div>
  </article>
  </div>
  
  <p class="architecture-note" data-reveal="">Integrations are validated before scope is confirmed. API access, documentation, permissions, licensing, data quality, security requirements, and third-party limitations can affect what is practical.</p>
  </div>
  </section>
  <section class="section section--soft" data-nav-label="Order Flow" id="workflow">
  <div class="page-shell">
  <p class="eyebrow" data-reveal="">Example OMS-style workflow</p>
  <h2 class="section-title" data-reveal="">An order comes in. The workflow already knows where it should go.</h2>
  <p class="section-copy" data-reveal="">This is one example of what the automation add-on can do. Your rules can be different. The point is to remove the repetitive decisions and copying your operations team performs on every order.</p>
  <div class="workflow-board" data-reveal="">
  <div class="workflow-track">
  <article class="workflow-step"><span class="workflow-step__index">01</span><h3>Order enters from any company store</h3><p>Orders from multiple Shopify company stores arrive in the central platform.</p><span class="workflow-step__dot"></span></article>
  <article class="workflow-step"><span class="workflow-step__index">02</span><h3>Middleware checks your routing rules</h3><p>The workflow identifies the approved supplier, store rules, products, and required downstream path.</p><span class="workflow-step__dot"></span></article>
  <article class="workflow-step"><span class="workflow-step__index">03</span><h3>Order is split by supplier</h3><p>If one Shopify order contains products from multiple approved suppliers, the workflow can separate the work automatically.</p><span class="workflow-step__dot"></span></article>
  <article class="workflow-step"><span class="workflow-step__index">04</span><h3>PO and supplier order are created</h3><p>Purchase-order data can be generated and the supplier order can be submitted through the connected supplier API.</p><span class="workflow-step__dot"></span></article>
  <article class="workflow-step"><span class="workflow-step__index">05</span><h3>Work moves to the right decorator</h3><p>The order follows the decorator or fulfillment path configured for that supplier, product, store, or decoration requirement.</p><span class="workflow-step__dot"></span></article>
  <article class="workflow-step"><span class="workflow-step__index">06</span><h3>Your team sees everything in one dashboard</h3><p>Orders across all stores stay visible in one place, including flows that need human review.</p><span class="workflow-step__dot"></span></article>
  </div>
  </div>
  <p class="architecture-note" data-reveal="">Supplier order submission, status synchronization, and decorator automation depend on the APIs and technical access provided by each partner.</p>
  </div>
  </section>
  <section class="section" data-nav-label="Integrations" id="systems">
  <div class="page-shell">
  <div class="section-heading-split">
  <div data-reveal="">
  <p class="eyebrow">Integration layer</p>
  <h2 class="section-title section-title--small">Connect the systems your company-store operation actually uses.</h2>
  </div>
  <p class="section-copy" data-reveal="">The goal is not to replace useful software. It is to stop your team from moving the same information by hand between stores, suppliers, finance systems, procurement tools, and customer-facing experiences.</p>
  </div>
  <div class="systems-grid systems-grid--five">
  <article class="system-group" data-reveal=""><span class="system-group__label">Promo data</span><h3>SAGE + PromoStandards</h3><ul><li>SAGE connections</li><li>PromoStandards</li><li>Mapped product data</li><li>Pricing / inventory where supported</li></ul></article>
  <article class="system-group" data-reveal=""><span class="system-group__label">Finance &amp; ERP</span><h3>Accounting connections</h3><ul><li>QuickBooks</li><li>Sage Intacct</li><li>NetSuite</li><li>Other approved ERP / finance APIs</li></ul></article>
  <article class="system-group" data-reveal=""><span class="system-group__label">Procurement</span><h3>PunchOut environments</h3><ul><li>Coupa</li><li>SAP Ariba</li><li>Jaggaer</li><li>Oracle / compatible cXML</li></ul></article>
  <article class="system-group" data-reveal=""><span class="system-group__label">Production</span><h3>Suppliers + decorators</h3><ul><li>Direct supplier APIs</li><li>Supplier order submission</li><li>Order status</li><li>Decorator / fulfillment routing</li></ul></article>
  <article class="system-group" data-reveal=""><span class="system-group__label">AI + custom</span><h3>Build beyond the standard stack</h3><ul><li>OpenAI-powered workflows</li><li>Customer chatbots</li><li>AI calling agents</li><li>Recommendation engines</li></ul></article>
  </div>
  <div class="integration-callout" data-reveal=""><span class="integration-callout__mark">?</span><div><strong>Have another system or automation idea?</strong><p>Show us the workflow. If the software provides a usable API, webhook, file exchange, database, or other practical integration path, we can assess a custom connection.</p></div><a class="text-link" data-analytics-event="integration_automation_cta" href="../book-demo/?service=enterprise-company-store-automation&amp;intent=automation">Automate My Company <span>→</span></a></div>
  </div>
  </section>
  <section class="section section--tight customer-story-section customer-story-section--v5" data-nav-label="Success Story" id="success-story">
  <div class="page-shell">
  <div class="customer-story-v5__heading" data-reveal="">
  <p class="eyebrow">Customer story · Club Colors</p>
  <h2 class="section-title section-title--small">Built into the operation.<br/>Trusted like part of the team.</h2>
  <p class="section-copy">A long-term commerce and automation partnership supporting a complex promotional products operation at scale.</p>
  </div>
  <div class="customer-story-v5" data-reveal="">
  <article class="customer-story-v5__quote-card">
  <div class="customer-story-v5__quote-mark">“</div>
  <blockquote>
  <p>We’ve worked with Swishtag for several years, and they’ve become a key part of our team. They’ve helped us build enterprise level commerce workflows that support our business, and their work has been consistently reliable and high quality.</p>
  <p>What stands out most is that they don’t just execute. They think critically, offer helpful suggestions, and speak up when something could be improved.</p>
  <p>At this point, we truly see Swishtag as an extension of our own team, and their contributions have been invaluable.</p>
  </blockquote>
  <div class="customer-story-v5__attribution">
  <div aria-label="Club Colors logo" class="customer-story-v5__mark">
  <svg aria-hidden="true" role="img" viewBox="0 0 120 120"><defs><linearGradient id="clubGreenEnterpriseV5" x1="0" x2="1" y1="0" y2="1"><stop offset="0" stop-color="#A9D548"></stop><stop offset="1" stop-color="#4FAE62"></stop></linearGradient></defs><path d="M98 26A47 47 0 1 0 93 91L75 76a25 25 0 1 1 5-34Z" fill="url(#clubGreenEnterpriseV5)"></path><path d="M79 42A28 28 0 0 0 43 72l10-4a18 18 0 0 1 24-18Z" fill="#F2C84B"></path><path d="M77 49A22 22 0 0 0 48 74l9-4a13 13 0 0 1 17-13Z" fill="#E94A5A"></path><path d="M74 56A15 15 0 0 0 55 75l8-4a7 7 0 0 1 9-8Z" fill="#2B9BD7"></path></svg>
  </div>
  <div><strong>Club Colors</strong><span>Promotional products distributor · Client testimonial</span></div>
  </div>
  </article>
  <aside class="customer-story-v5__proof">
  <div class="customer-story-v5__proof-head"><span>Proof at scale</span><strong>Enterprise commerce technology that keeps evolving with the business.</strong></div>
  <div class="customer-story-v5__metric-grid">
  <div class="customer-story-v5__metric customer-story-v5__metric--mint"><strong>150+</strong><span>client stores supported through a centralized commerce ecosystem</span></div>
  <div class="customer-story-v5__metric customer-story-v5__metric--lavender"><strong>$40M+</strong><span>in orders powered through technology delivered by Swishtag</span></div>
  <div class="customer-story-v5__metric customer-story-v5__metric--peach"><strong>Several years</strong><span>of continued development, support, and operational partnership</span></div>
  </div>
  <div class="customer-story-v5__scope"><span>What this partnership demonstrates</span><div><b>Multi-store technology</b><b>Enterprise workflows</b><b>Ongoing support</b><b>Proactive recommendations</b></div></div>
  <a class="text-link customer-story-v5__link" data-analytics-event="success_story_cta" href="../book-demo/?service=enterprise-company-store-automation&amp;intent=automation">Automate My Company <span>→</span></a>
  </aside>
  </div>
  </div>
  </section>
  <section class="section onboarding-section" data-nav-label="Onboarding" id="process">
  <div class="page-shell">
  <div class="onboarding-heading" data-reveal="">
  <p class="eyebrow">How to start</p>
  <h2 class="section-title section-title--small">Show us where the operation hurts.<br/>We automate the highest-value pain first.</h2>
  <p class="section-copy">You do not need a finished technical specification. Bring us the real workflow, the stores involved, and the manual work your team keeps repeating. We map it, prioritize it, and build around the business return.</p>
  </div>
  <div class="onboarding-start" data-reveal="">
  <div class="onboarding-start__label"><span>Start here</span><strong>Bring us three things.</strong></div>
  <div class="onboarding-start__items">
  <div><span>01</span><strong>Your company-store list</strong><small>Standard Shopify, SAGE-powered, or both.</small></div>
  <div><span>02</span><strong>One real order workflow</strong><small>Show us what happens from order to supplier, decorator, finance, and fulfillment.</small></div>
  <div><span>03</span><strong>Your most painful manual tasks</strong><small>The repetitive work costing time, margin, or visibility.</small></div>
  </div>
  </div>
  <div class="onboarding-journey">
  <article class="onboarding-step" data-reveal=""><span class="onboarding-step__index">01</span><div class="onboarding-step__icon"><svg viewBox="0 0 24 24"><path d="M4 5h16v14H4zM8 9h8M8 13h5"></path></svg></div><small>Discover</small><h3>Map the operation</h3><p>We review your stores, teams, suppliers, decorators, systems, reporting, and the steps people perform manually today.</p><div class="onboarding-step__role"><b>Your part</b><span>Walk us through what really happens.</span></div></article>
  <article class="onboarding-step" data-reveal=""><span class="onboarding-step__index">02</span><div class="onboarding-step__icon"><svg viewBox="0 0 24 24"><path d="M5 19 19 5M7 5h5v5M12 14h5v5"></path></svg></div><small>Prioritize</small><h3>Find the highest-value pain</h3><p>We separate annoying work from expensive work and identify the workflow where automation can save the most time or protect the most margin.</p><div class="onboarding-step__role"><b>Our part</b><span>Recommend what to automate first, and what not to automate yet.</span></div></article>
  <article class="onboarding-step" data-reveal=""><span class="onboarding-step__index">03</span><div class="onboarding-step__icon"><svg viewBox="0 0 24 24"><path d="M4 6h16v12H4zM8 10h8M8 14h5"></path></svg></div><small>Centralize</small><h3>Create the control layer</h3><p>We establish the shared store, order, and reporting visibility your team needs so the operation has one reliable place to work from.</p><div class="onboarding-step__role"><b>Result</b><span>One dashboard instead of disconnected store-by-store visibility.</span></div></article>
  <article class="onboarding-step" data-reveal=""><span class="onboarding-step__index">04</span><div class="onboarding-step__icon"><svg viewBox="0 0 24 24"><path d="M6 12h12M12 6l6 6-6 6"></path></svg></div><small>Automate</small><h3>Remove the repetitive steps</h3><p>We build the selected workflow, such as order routing, POs, supplier connections, PunchOut, accounting sync, AI, or custom logic.</p><div class="onboarding-step__role"><b>Result</b><span>Less copying, fewer handoffs, and fewer places for mistakes.</span></div></article>
  <article class="onboarding-step" data-reveal=""><span class="onboarding-step__index">05</span><div class="onboarding-step__icon"><svg viewBox="0 0 24 24"><path d="M4 18V9M10 18V5M16 18v-7M22 18V7"></path></svg></div><small>Improve</small><h3>Measure, report, expand</h3><p>Once the first automation proves useful, we use the operational data to refine the workflow and decide what should be automated next.</p><div class="onboarding-step__role"><b>Next step</b><span>Expand only where the business case is clear.</span></div></article>
  </div>
  <div class="automation-impact" data-reveal="">
  <div class="automation-impact__header"><span>Why automate?</span><strong>Automation should remove friction and improve the business at the same time.</strong><p>Start where manual work is costing the most time, margin, or visibility. Then turn the first workflow improvement into a measurable operational win.</p></div>
  <div class="automation-impact__list">
  <article class="impact-row impact-row--lime"><div class="impact-row__lead"><span>01</span><strong>Protect profit margins</strong></div><p>Reduce repetitive labor, operational leakage, and expensive handoffs as the number of stores and orders grows.</p><div class="impact-row__result">Result: more margin stays in the business</div></article>
  <article class="impact-row impact-row--mint"><div class="impact-row__lead"><span>02</span><strong>Reduce manual work</strong></div><p>Move data, split orders, create downstream actions, and route work automatically where the workflow is repeating every day.</p><div class="impact-row__result">Result: fewer touchpoints and fewer mistakes</div></article>
  <article class="impact-row impact-row--peach"><div class="impact-row__lead"><span>03</span><strong>Save team time</strong></div><p>Free your account and operations teams from copying information and chasing routine steps across multiple company stores.</p><div class="impact-row__result">Result: more time for service, growth, and higher-value work</div></article>
  <article class="impact-row impact-row--lavender"><div class="impact-row__lead"><span>04</span><strong>Make better decisions</strong></div><p>Use one dashboard and shared reporting to understand performance, issues, and opportunities across the full company-store portfolio.</p><div class="impact-row__result">Result: faster decisions with better visibility</div></article>
  </div>
  </div>
  </div>
  </section>
  <section class="section" data-nav-label="FAQs" id="faq">
  <div class="page-shell faq-layout">
  <div class="faq-heading" data-reveal=""><p class="eyebrow">Frequently asked questions</p><h2 class="section-title section-title--small">What distributors usually want to know first.</h2><p class="section-copy">Start with the platform. Add the complexity only when it solves a real operational problem.</p><a class="text-link" href="../book-demo/?service=enterprise-company-store-automation&amp;intent=automation"><span style="width:auto;height:auto;background:none">Automate My Company</span><span>→</span></a></div>
  <div class="faq-list">
  <details class="faq-item" data-reveal="" open=""><summary>What is the base company store platform?</summary><p>The base platform gives your team one place to see and manage multiple Shopify company stores, including centralized order visibility, store-level reporting, portfolio reporting, and operational status. You can use it without buying workflow automation.</p></details>
  <details class="faq-item" data-reveal=""><summary>Can the platform manage both SAGE-powered and regular Shopify company stores?</summary><p>Yes. The centralized platform can be designed around a portfolio that includes SAGE-powered Shopify company stores and simpler Shopify company stores, based on the data and controls available in each setup.</p></details>
  <details class="faq-item" data-reveal=""><summary>Which promotional products data systems do you connect with?</summary><p>Our promotional products data connections are built around SAGE and PromoStandards. We can also assess direct custom connections with individual suppliers when suitable APIs or technical access are available.</p></details>
  <details class="faq-item" data-reveal=""><summary>Can you connect directly with our approved suppliers?</summary><p>Yes. If a supplier provides suitable API access, we can build a custom connection for product data, order submission, purchase orders, status updates, or another agreed workflow.</p></details>
  <details class="faq-item" data-reveal=""><summary>How does order workflow automation work?</summary><p>A Shopify order can enter the middleware, be checked against your rules, split by approved supplier, converted into supplier POs or API orders, routed to the correct decorator or fulfillment path, and monitored from the central dashboard.</p></details>
  <details class="faq-item" data-reveal=""><summary>Can PunchOut be added later?</summary><p>Yes. PunchOut is an optional paid capability. Add it when a corporate client requires a procurement-connected company store. We validate the buyer system, cXML flow, authentication, cart return, purchase-order requirements, and testing process before confirming scope.</p></details>
  <details class="faq-item" data-reveal=""><summary>Can you build automation around our own workflow?</summary><p>Yes. If your distributor operation has a workflow that does not fit an off-the-shelf app, we can map the process and build a custom automation layer where the required systems provide suitable technical access.</p></details>
  <details class="faq-item" data-reveal=""><summary>Can you integrate QuickBooks, Sage Intacct, or NetSuite?</summary><p>Yes, these can be scoped as paid finance or ERP integration add-ons, subject to API access, account permissions, required data mappings, licensing, and technical review. We can also assess other accounting or ERP platforms when a practical integration path is available.</p></details><details class="faq-item" data-reveal=""><summary>Can you build AI features or connect OpenAI?</summary><p>Yes. Custom add-ons can include OpenAI-powered workflows, customer chatbots, AI calling agents, recommendation engines, internal assistants, and other automation ideas. The final scope depends on the business workflow, data access, privacy requirements, and available APIs.</p></details></div>
  </div>
  </section>
  <section class="section final-cta-section">
  <div class="page-shell">
  <div class="final-cta" data-reveal="">
  <div class="final-cta__content">
  <p class="eyebrow eyebrow--ink">Ready to simplify the operation?</p>
  <h2>More company stores. Less operational chaos.</h2>
  <p>Show us how your stores, suppliers, decorators, finance systems, and client requirements work today. We will separate what belongs in the base platform from what is actually worth automating.</p>
  <div class="final-cta__actions">
  <a class="btn btn--primary" data-analytics-event="final_automation_cta" href="../book-demo/?service=enterprise-company-store-automation&amp;intent=automation"><span>Automate My Company</span><span aria-hidden="true">↗</span></a>
  <a class="btn btn--secondary" data-analytics-event="final_addons_click" href="#architecture">Explore Add-ons</a>
  </div>
  <div class="final-cta__benefits"><span>Base platform first</span><span>Paid add-ons as needed</span><span>Custom integrations when practical</span></div>
  </div>
  <div aria-hidden="true" class="final-visual">
  <div class="final-visual__orbit final-visual__orbit--one"></div>
  <div class="final-visual__orbit final-visual__orbit--two"></div>
  <div class="final-visual__core"><strong>Control Center</strong></div>
  <span class="final-visual__chip final-visual__chip--one">OMS</span>
  <span class="final-visual__chip final-visual__chip--two">SAGE</span>
  <span class="final-visual__chip final-visual__chip--three">PunchOut</span>
  <span class="final-visual__chip final-visual__chip--four">ERP</span>
  <span class="final-visual__chip final-visual__chip--five">AI</span>
  </div>
  </div>
  </div>
  </section>
  </main>
  <script slot="scripts" is:inline>
  (() => {
    const art = document.querySelector('.enterprise-art--dashboard');
    const browser = art?.querySelector('.dashboard-browser-mockup--simple');
    const trigger = art?.querySelector('.dashboard-browser-trigger');
    const viewport = art?.querySelector('.dashboard-browser-mockup__viewport--simple');
    const dashboard = art?.querySelector('.simple-dashboard-image');
    const grid = art?.querySelector('.control-center-grid');
    const nodes = art ? Array.from(art.querySelectorAll('.enterprise-node')) : [];
    if (!art || !browser || !trigger || !viewport || !dashboard || !grid || nodes.length !== 6) return;
  
    const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
    const duration = 780;
    let packed = false;
    let busy = false;
    let targets = [];
  
    const createGrid = () => {
      grid.innerHTML = '';
      nodes.forEach((node) => {
        const item = document.createElement('div');
        item.className = 'control-center-item';
        const icon = document.createElement('span');
        icon.className = 'control-center-item__icon';
        const sourceIcon = node.querySelector('.enterprise-node__icon');
        if (sourceIcon) icon.innerHTML = sourceIcon.innerHTML;
        const title = document.createElement('strong');
        title.textContent = node.querySelector('.enterprise-node__copy strong')?.textContent?.trim() || '';
        item.append(icon, title);
        grid.append(item);
      });
    };
  
    const measureTargets = () => {
      const items = Array.from(grid.children);
      targets = items.map(item => item.getBoundingClientRect());
    };
  
    const animateNodeToTarget = (node, target, inward) => {
      const rect = node.getBoundingClientRect();
      const dx = target.left + target.width / 2 - (rect.left + rect.width / 2);
      const dy = target.top + target.height / 2 - (rect.top + rect.height / 2);
      const scale = Math.min(target.width / rect.width, target.height / rect.height) * .9;
      const keyframes = inward
        ? [
            { translate: '0px 0px', scale: '1', opacity: 1 },
            { translate: `${dx}px ${dy}px`, scale: `${scale}`, opacity: 1, offset: .82 },
            { translate: `${dx}px ${dy}px`, scale: `${scale}`, opacity: 0 }
          ]
        : [
            { translate: `${dx}px ${dy}px`, scale: `${scale}`, opacity: 0 },
            { translate: `${dx}px ${dy}px`, scale: `${scale}`, opacity: 1, offset: .14 },
            { translate: '0px 0px', scale: '1', opacity: 1 }
          ];
  
      return node.animate(keyframes, {
        duration,
        easing: 'cubic-bezier(.18,.88,.24,1)',
        fill: 'both'
      });
    };
  
    const pack = async () => {
      if (busy || packed) return;
      busy = true;
      art.classList.add('is-transitioning', 'is-packing');
      trigger.setAttribute('aria-pressed', 'true');
      trigger.setAttribute('aria-label', 'Close the Control Center. Click to return the tools to their floating positions.');
  
      grid.style.visibility = 'hidden';
      grid.style.opacity = '0';
      measureTargets();
  
      const animations = nodes.map((node, i) => animateNodeToTarget(node, targets[i], true));
  
      const gridAnimation = grid.animate(
        [{ opacity: 0 }, { opacity: 0, offset: .55 }, { opacity: 1 }],
        { duration, easing: 'ease-out', fill: 'both' }
      );
      grid.style.visibility = 'visible';
  
      await Promise.allSettled([...animations.map(a => a.finished), gridAnimation.finished]);
      animations.forEach(a => a.cancel());
      gridAnimation.cancel();
      packed = true;
      art.classList.remove('is-packing', 'is-transitioning');
      art.classList.add('is-packed');
      grid.style.removeProperty('visibility');
      grid.style.removeProperty('opacity');
      busy = false;
    };
  
    const unpack = async () => {
      if (busy || !packed) return;
      busy = true;
      art.classList.add('is-transitioning', 'is-unpacking');
      art.classList.remove('is-packed');
      trigger.setAttribute('aria-pressed', 'false');
      trigger.setAttribute('aria-label', 'Open the Control Center. Click to bring the connected company-store tools inside the dashboard.');
  
      grid.style.visibility = 'visible';
      grid.style.opacity = '1';
      measureTargets();
  
      const animations = nodes.map((node, i) => animateNodeToTarget(node, targets[i], false));
      const gridAnimation = grid.animate(
        [{ opacity: 1 }, { opacity: 0, offset: .34 }, { opacity: 0 }],
        { duration: Math.round(duration * .72), easing: 'ease-in', fill: 'both' }
      );
  
      await Promise.allSettled([...animations.map(a => a.finished), gridAnimation.finished]);
      animations.forEach(a => a.cancel());
      gridAnimation.cancel();
      packed = false;
      art.classList.remove('is-unpacking', 'is-transitioning');
      grid.style.removeProperty('visibility');
      grid.style.removeProperty('opacity');
      busy = false;
    };
  
    const toggle = () => {
      window.swishtagTrack?.(packed ? 'control_center_close' : 'control_center_open', {
        component: 'hero_control_center'
      });
  
      if (reduceMotion.matches) {
        packed = !packed;
        art.classList.toggle('is-packed', packed);
        trigger.setAttribute('aria-pressed', String(packed));
        trigger.setAttribute('aria-label', packed
          ? 'Close the Control Center. Click to return the tools to their floating positions.'
          : 'Open the Control Center. Click to bring the connected company-store tools inside the dashboard.');
        return;
      }
      packed ? unpack() : pack();
    };
  
    createGrid();
    trigger.addEventListener('click', toggle);
  
    window.addEventListener('resize', () => {
      if (busy) return;
      if (packed) requestAnimationFrame(measureTargets);
    }, { passive: true });
  })();
  </script>
  <script slot="scripts" is:inline>
  (() => {
    const track = (eventName, parameters = {}) => {
      const payload = {
        page_type: 'enterprise_company_store_automation',
        ...parameters
      };
  
      if (typeof window.gtag === 'function') {
        window.gtag('event', eventName, payload);
      } else {
        window.dataLayer = window.dataLayer || [];
        window.dataLayer.push({ event: eventName, ...payload });
      }
  
      window.dispatchEvent(new CustomEvent('swishtag:analytics', {
        detail: { event: eventName, ...payload }
      }));
    };
  
    window.swishtagTrack = track;
  
    document.addEventListener('click', event => {
      const target = event.target.closest('[data-analytics-event]');
      if (!target) return;
  
      track(target.dataset.analyticsEvent, {
        link_text: target.textContent.trim().replace(/\s+/g, ' ').slice(0, 120),
        link_url: target.getAttribute('href') || ''
      });
    });
  
    document.querySelectorAll('.faq-item').forEach(item => {
      item.addEventListener('toggle', () => {
        if (!item.open) return;
        track('faq_open', {
          question: item.querySelector('summary')?.textContent?.trim() || ''
        });
      });
    });
  
    const seenAddons = new WeakSet();
    const addonObserver = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (!entry.isIntersecting || seenAddons.has(entry.target)) return;
        seenAddons.add(entry.target);
        track('addon_view', {
          addon_name: entry.target.querySelector('h3')?.textContent?.trim() || ''
        });
        addonObserver.unobserve(entry.target);
      });
    }, { threshold: .55 });
  
    document.querySelectorAll('.solution-addon-card').forEach(card => addonObserver.observe(card));
  
    const onboarding = document.getElementById('process');
    if (onboarding) {
      const onboardingObserver = new IntersectionObserver(entries => {
        if (!entries.some(entry => entry.isIntersecting)) return;
        track('onboarding_view');
        onboardingObserver.disconnect();
      }, { threshold: .3 });
      onboardingObserver.observe(onboarding);
    }
  })();
  </script>
</BaseLayout>
