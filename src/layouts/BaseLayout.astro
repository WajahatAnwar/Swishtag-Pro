---
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import HeaderBehavior from '../components/layout/HeaderBehavior.astro';
import '../styles/home.css';
import '../styles/header.css';

const {
  title = 'Swishtag',
  description = '',
  canonical,
  robots = 'index,follow',
} = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="theme-color" content="#FFFFFF" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="color-scheme" content="light" />
    <meta name="format-detection" content="telephone=no" />
    <link rel="preconnect" href="https://cdn.shopify.com" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
    {canonical && <link rel="canonical" href={canonical} />}
    <meta name="robots" content={robots} />
    <script defer src="/assets/site-shell.js"></script>
    <slot name="head" />
    <style is:global>
      @media (min-width: 901px) {
        .page-nav-wrap {
          top: 70px !important;
          pointer-events: none;
        }

        .page-nav-wrap > .page-shell,
        .page-nav-wrap .page-shell {
          width: min(var(--page), 1240px) !important;
          max-width: none !important;
        }

        .page-nav-shell {
          pointer-events: auto;
        }

        .site-header.is-nav-connected .modern-nav {
          border-radius: 24px 24px 0 0 !important;
          border-color: rgba(32,33,36,.08) !important;
          border-bottom-color: transparent !important;
          background: rgba(255,255,255,.97) !important;
          box-shadow: none !important;
        }

        .page-nav-wrap.is-stuck {
          padding-top: 0 !important;
          filter: drop-shadow(0 22px 42px rgba(32,33,36,.11));
        }

        .page-nav-wrap.is-stuck .page-nav-shell {
          margin-top: -1px !important;
          border-top-color: transparent !important;
          border-radius: 0 0 24px 24px !important;
          background: rgba(255,255,255,.97) !important;
          box-shadow: none !important;
        }
      }

      @media (min-width: 681px) and (max-width: 900px) {
        .page-nav-wrap {
          top: 68px !important;
        }
      }

      @media (max-width: 680px) {
        .page-nav-wrap {
          top: 64px !important;
        }
      }

      @media (min-width: 821px) {
        .site-header .desktop-nav__item {
          position: static !important;
        }

        .site-header .mega-menu {
          position: fixed !important;
          z-index: 1000 !important;
          top: 92px !important;
          left: 50% !important;
          width: min(1160px, calc(100vw - 42px)) !important;
          display: grid !important;
          grid-template-columns: minmax(230px, .78fr) minmax(380px, 1.25fr) minmax(280px, .92fr) !important;
          gap: 18px !important;
          padding: 18px !important;
          border: 1px solid rgba(32,33,36,.075) !important;
          border-radius: 30px !important;
          background:
            radial-gradient(circle at 82% 8%, rgba(153,254,237,.20), transparent 28%),
            radial-gradient(circle at 14% 100%, rgba(217,214,253,.24), transparent 30%),
            rgba(255,255,255,.97) !important;
          box-shadow:
            0 30px 90px rgba(32,33,36,.16),
            inset 0 1px 0 rgba(255,255,255,.96) !important;
          -webkit-backdrop-filter: blur(22px) !important;
          backdrop-filter: blur(22px) !important;
          opacity: 0 !important;
          visibility: hidden !important;
          pointer-events: none !important;
          transform: translate(-50%, 12px) scale(.985) !important;
          transform-origin: top center !important;
        }

        .site-header.is-scrolled .mega-menu {
          top: 78px !important;
        }

        .site-header .desktop-nav__item:hover > .mega-menu,
        .site-header .desktop-nav__item:focus-within > .mega-menu,
        .site-header .desktop-nav__item.is-open > .mega-menu {
          opacity: 1 !important;
          visibility: visible !important;
          pointer-events: auto !important;
          transform: translate(-50%, 0) scale(1) !important;
        }

        .site-header .mega-menu > .mega-menu__intro,
        .site-header .mega-menu > .mega-menu__content,
        .site-header .mega-menu > .mega-products,
        .site-header .mega-menu > .mega-industries,
        .site-header .mega-menu > .mega-visual {
          min-height: auto !important;
          border-radius: 22px !important;
        }

        .site-header .mega-menu__intro {
          position: relative !important;
          display: flex !important;
          flex-direction: column !important;
          overflow: hidden !important;
          padding: 25px 23px !important;
          color: #202124 !important;
          background:
            radial-gradient(circle at 88% 12%, rgba(194,255,2,.18), transparent 28%),
            linear-gradient(145deg, rgba(248,248,245,.98), rgba(255,255,255,.98)) !important;
          border: 1px solid rgba(32,33,36,.065) !important;
          box-shadow: none !important;
        }

        .site-header .mega-menu__intro::after {
          content: "" !important;
          position: absolute !important;
          right: -60px !important;
          bottom: -58px !important;
          width: 160px !important;
          height: 160px !important;
          border: 1px dashed rgba(32,33,36,.12) !important;
          border-radius: 50% !important;
          pointer-events: none !important;
        }

        .site-header .mega-menu__eyebrow {
          display: block !important;
          margin: 0 0 18px !important;
          color: #696A6D !important;
          font-size: .62rem !important;
          font-weight: 800 !important;
          letter-spacing: .13em !important;
          text-transform: uppercase !important;
        }

        .site-header .mega-menu__intro h2,
        .site-header .mega-menu__intro .mega-menu__title {
          margin: 0 !important;
          color: #202124 !important;
          font-size: clamp(1.75rem, 2.3vw, 2.55rem) !important;
          line-height: 1.02 !important;
          letter-spacing: -.05em !important;
          font-weight: 800 !important;
        }

        .site-header .mega-menu__intro p {
          margin: 17px 0 0 !important;
          color: #696A6D !important;
          font-size: .78rem !important;
          line-height: 1.58 !important;
        }

        .site-header .mega-menu__all-link {
          display: inline-flex !important;
          align-items: center !important;
          justify-content: space-between !important;
          gap: 18px !important;
          margin-top: auto !important;
          padding-top: 25px !important;
          border-top: 0 !important;
          color: #202124 !important;
          font-size: .72rem !important;
          font-weight: 760 !important;
        }

        .site-header .mega-menu__all-link span:last-child {
          width: 30px !important;
          height: 30px !important;
          display: grid !important;
          place-items: center !important;
          border-radius: 50% !important;
          background: #C2FF02 !important;
        }

        .site-header .mega-menu__content,
        .site-header .mega-products,
        .site-header .mega-industries {
          background: rgba(250,250,248,.92) !important;
          border: 1px solid rgba(32,33,36,.055) !important;
        }

        .site-header .mega-menu__content {
          display: block !important;
          padding: 12px !important;
        }

        .site-header .mega-menu__primary-links {
          display: grid !important;
          gap: 8px !important;
        }
      }
    </style>
  </head>
  <body>
    <Header />
    <slot />
    <Footer />
    <HeaderBehavior />
    <slot name="scripts" />
  </body>
</html>
